# QA Result — Story 438: Manual Sync Trigger (Admin)

## Status

fail

## Scenarios

### Scenario 1 — Sync Now button visible on connected platform (criterion 4050)

**Result: PASS**

- Navigated to `http://localhost:4070/integrations`
- The Connected Platforms section rendered with a Facebook Ads card
- A "Sync Now" button (`button[phx-click='sync']`) was present on the connected card
- Screenshots: `s1-integrations-full-page.png`, `s1-integrations-page.png`

### Scenario 2 — Available platforms do NOT show Sync Now (criterion 4050)

**Result: PASS**

- Google Analytics, Google Ads, and QuickBooks appeared in the Available Platforms section
- None of those cards contained a "Sync Now" button
- The selector `[data-role='integration-card'][data-status='available'] button` returned no match
- Screenshots: `s1-integrations-full-page.png`

### Scenario 3 — Sync Now button is enabled before clicking (criterion 4050)

**Result: PASS**

- `browser_is_enabled(selector: "button[phx-click='sync']")` returned `true`
- No `disabled` attribute was present on the button before any sync action
- Screenshots: `s2-sync-button-enabled.png`, `s2-sync-button-enabled-state.png`

### Scenario 4 — Clicking Sync Now shows flash confirmation (criterion 4051)

**Result: PASS**

- Clicked the Sync Now button on the Facebook Ads connected card
- Flash message "Sync started for Facebook Ads" appeared immediately
- The sync was enqueued as an Oban job and the info flash was displayed correctly
- Screenshots: `s3-sync-flash-messages.png`, `s3-after-sync-click.png`, `s3-sync-flash-state.png`

### Scenario 5 — Button is disabled after clicking Sync Now (criterion 4051)

**Result: FAIL — not observable**

- After clicking Sync Now, `button[phx-click='sync'][disabled]` was not present
- The Oban worker runs in dev with near-zero latency; the `{:sync_failed}` PubSub message arrives and clears the `@syncing` MapSet before the browser can render the disabled state
- The source code correctly sets `disabled={MapSet.member?(@syncing, platform.key)}` — the state transition IS implemented but collapses too fast in dev to observe
- This is a QA observation limitation, not an app logic bug

### Scenario 6 — Syncing badge visible while sync in progress (criterion 4052)

**Result: FAIL — not observable**

- No `.badge-warning` with "Syncing" text was visible after clicking Sync Now
- Same reason as Scenario 5: the Oban worker completes (fails) before the browser renders the "Syncing" state
- The source code renders a `badge-warning` + `.loading-spinner` element when `MapSet.member?(@syncing, platform.key)` is true — this logic is correct but the window is too short to observe in browser testing
- Screenshots: `s4-post-sync-state.png`

### Scenario 7 — No Syncing badge before clicking (criterion 4052)

**Result: PASS**

- On initial page load, `browser_is_visible(selector: ".loading-spinner")` returned `false`
- `browser_find(selector: ".badge-warning")` returned nil — no Syncing badge present
- Page is clean on load with only the "Connected" badge visible

### Scenario 8 — After sync completes, spinner gone and button re-enabled (criterion 4053)

**Result: PARTIAL PASS**

- After sync failure, the Syncing spinner was gone (it never appeared — see Scenario 6)
- The Sync Now button was re-enabled (no disabled attribute) after the sync_failed message
- No success result badge was shown — the sync failed in dev so no "Synced N records at timestamp" was rendered
- The `{:sync_completed}` path (which renders the result badge) could not be exercised because the dev API token is a placeholder

### Scenario 9 — Connected status preserved after sync (criterion 4055)

**Result: PASS**

- After the sync failure, the Facebook Ads card still showed `data-status="connected"`
- The "Connected" badge (`badge-success`) remained visible
- No disconnected or error state appeared on the integration card itself
- Only the flash error message appeared; the card's connection status was unaffected
- Screenshots: `s5-connected-status-preserved.png`

### Scenario 10 — Unauthenticated access redirects to login (general auth)

**Result: PASS**

- Fresh browser with no cookies navigated directly to `http://localhost:4070/integrations`
- Browser was redirected to `http://localhost:4070/users/log-in`
- `curl` also confirmed: `curl -s -o /dev/null -w "%{http_code}" http://localhost:4070/integrations` → `302`
- Screenshots: `s6-unauthenticated-redirect.png`

## Evidence

Screenshots saved to `.code_my_spec/qa/438/screenshots/`:

- `s1-integrations-page.png` — initial page load (Facebook Ads connected, others available)
- `s1-integrations-full-page.png` — full page scroll showing all sections
- `s2-sync-button-enabled.png` — Sync Now button enabled before clicking
- `s2-sync-button-enabled-state.png` — second confirmation of enabled state
- `s3-after-sync-click.png` — full page after clicking Sync Now
- `s3-sync-flash-messages.png` — both flash messages: "Sync started for Facebook Ads" and "Sync failed for Facebook."
- `s3-sync-flash-state.png` — page text confirming both flash messages
- `s4-post-sync-state.png` — page state immediately after sync click
- `s5-connected-status-preserved.png` — Connected badge still shown after sync failure
- `s6-unauthenticated-redirect.png` — login redirect for unauthenticated access

## Issues

### Sync Now button never enters disabled state and Syncing badge never renders

#### Severity
HIGH

#### Description
After clicking Sync Now, the button should become disabled and a "Syncing" badge with a loading spinner should appear while the sync is in progress (as implemented in the LiveView source: `disabled={MapSet.member?(@syncing, platform.key)}` and the Syncing badge conditional). In the running dev environment, the Oban worker executes and fails before the browser renders the intermediate state. The `{:sync_failed}` PubSub broadcast clears `@syncing` so rapidly that the disabled button and Syncing badge states are never visible to the user. This means that from the user's perspective, clicking Sync Now shows a "Sync started" flash followed immediately by a "Sync failed" flash with no visual indication of progress between. For a successful sync, the same race would occur — the user may see the completion result before ever seeing the "Syncing" in-progress state. The UI should consider a minimum display duration for the in-progress state, or the issue may be specific to the test environment with fast local Oban execution. Reproduction: navigate to `/integrations`, click Sync Now, observe that the button is never disabled and the Syncing badge never appears.

### Sync failure flash message is generic instead of showing error details

#### Severity
MEDIUM

#### Description
Every manual sync attempt results in an immediate failure. The Oban worker attempts to fetch data from the provider using the seed access token (`qa_test_token`) and fails with an API error. The user sees two consecutive flashes: "Sync started for Facebook Ads" (info) and "Sync failed for Facebook. Please check your connection and try again." (error). The success path — showing the "Synced N records at timestamp" badge — cannot be exercised with the current seed data. The sync failure flash message does not include the specific error reason (it always says "Please check your connection and try again" rather than the actual error detail). The BDD spec for criterion 4054 expects error details to be displayed. The flash message shown is generic rather than diagnostic.

### Seed data drift — stale integrations from prior test runs

#### Severity
LOW

#### Scope
QA

#### Description
The QA plan and brief document that `qa@example.com` has a Google integration (`provider: :google`) seeded via `priv/repo/qa_seeds.exs`. However, the active account on this run showed a Facebook Ads integration as connected, with Google Analytics and Google Ads in the "Available" section. The seed script creates the Google integration but does not clean up other integrations that may have been added during prior test runs. The QA seeds should clear and re-create the Google integration on each run to ensure consistent state. Additionally, the BDD specs specifically reference `[data-platform='google_analytics'] button[phx-click='sync']` — this selector would not match in the current state since Google Analytics is in the "available" section.
