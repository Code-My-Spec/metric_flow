# QA Result

Story 438 — Manual Sync Trigger (Admin)

## Status

fail

## Scenarios

### Scenario 1 — Sync Now button visible on connected integration (criterion 4050)

pass

Navigated to `http://localhost:4070/integrations`. The page loaded with a "Connected Platforms" section containing a Google card (`[data-role="integration-card"][data-platform="google"][data-status="connected"]`). The Google card contains a `button[phx-click="sync"]` with text "Sync Now". The "Available Platforms" section shows Facebook Ads and QuickBooks cards with `data-status="available"`. Neither available platform card contains a `button[phx-click="sync"]` — they only have a Connect button (`phx-click="initiate_connect"`).

Evidence: `.code_my_spec/qa/438/screenshots/s1-integrations-page.png`

### Scenario 2 — Sync Now button is enabled when no sync is in progress (criterion 4050)

pass

The `button[phx-click="sync"][phx-value-provider="google"]` does not have the `disabled` attribute. `browser_is_enabled` returned `true`. The button is clickable and ready.

Evidence: `.code_my_spec/qa/438/screenshots/s2-sync-button-enabled.png`

### Scenario 3 — Clicking Sync Now triggers sync and shows loading state (criteria 4051, 4052)

partial

Clicked the "Sync Now" button on the Google card. The "Sync started for Google" info flash appeared immediately — confirming the sync event handler fired, the DataSync.sync_integration/2 succeeded in creating the job, and the button's `@syncing` MapSet was populated with `:google`.

However, the Oban SyncWorker runs and completes (with a failure) so quickly in dev that the intermediate "Syncing" badge with spinner was never observable. By the time the screenshot was captured, `handle_info({:sync_failed, ...})` had already cleared the syncing state. The "Sync failed for google: all_providers_failed" error flash appeared alongside the "Sync started for Google" success flash.

The button was not observed in the disabled state for the same reason — the window between setting `@syncing` and clearing it is sub-second in dev.

Evidence: `.code_my_spec/qa/438/screenshots/s3-after-sync-click.png`, `.code_my_spec/qa/438/screenshots/s3-sync-flash-state.png`

### Scenario 4 — Success state after sync completes (criterion 4053)

fail

The sync worker completed with `{:error, :all_providers_failed}` because the stub access token (`qa_test_access_token`) cannot authenticate with real Google APIs. Both GoogleAnalytics and GoogleAds `fetch_metrics/2` returned errors. The error flash "Sync failed for google: all_providers_failed" was shown. No success flash with a record count appeared. No sync result badge was rendered (the badge only shows for successful syncs via `Map.get(@sync_results, platform.key)`).

This is an inherent limitation of the QA environment — stub tokens cannot complete live API syncs. The success path for criterion 4053 cannot be verified via browser automation without a real OAuth token or a dev-mode stub data provider. The BDD spex approach (simulating `{:sync_completed, ...}` messages directly via `send/2`) is the appropriate path for this criterion, as noted in the brief.

### Scenario 5 — Connected status preserved after manual sync (criterion 4055)

pass

After the failed sync attempt, the Google integration card still shows `[data-role="integration-card"][data-platform="google"][data-status="connected"]` with a "Connected" badge (`.badge-success`). No `[data-status="disconnected"]` element exists in the Google card. The "Sync Now" button is still present and `browser_is_enabled` returned `true`. The failed sync did not disconnect the integration.

Evidence: `.code_my_spec/qa/438/screenshots/s5-connected-status-preserved.png`

### Scenario 6 — Unauthenticated user cannot access the integrations page

pass

In a fresh browser session (new browser instance launched with no cookies), navigating to `http://localhost:4070/integrations` immediately redirected to `http://localhost:4070/users/log-in`. Confirmed via `browser_get_url()`.

Evidence: `.code_my_spec/qa/438/screenshots/s6-unauthenticated-redirect.png`

## Evidence

- `.code_my_spec/qa/438/screenshots/s1-integrations-page.png` — Full integrations page showing Connected Platforms (Google with Sync Now button) and Available Platforms (Facebook Ads, QuickBooks with Connect buttons only)
- `.code_my_spec/qa/438/screenshots/s2-sync-button-enabled.png` — Sync Now button in enabled state before clicking
- `.code_my_spec/qa/438/screenshots/s3-after-sync-click.png` — Immediate state after clicking Sync Now
- `.code_my_spec/qa/438/screenshots/s3-sync-flash-state.png` — Full page with both flash messages: "Sync started for Google" (info) and "Sync failed for google: all_providers_failed" (error), confirming sync fired and completed nearly instantly
- `.code_my_spec/qa/438/screenshots/s5-connected-status-preserved.png` — Google card still shows Connected badge after failed sync; Sync Now button still present and enabled
- `.code_my_spec/qa/438/screenshots/s6-unauthenticated-redirect.png` — Login page shown after unauthenticated access attempt to /integrations

## Issues

### Sync Now always fails with "all_providers_failed" in QA environment — sync success path untestable

#### Severity
MEDIUM

#### Scope
QA

#### Description

The QA seed script creates a Google integration with a stub access token (`qa_test_access_token`). When Sync Now is clicked, the Oban SyncWorker runs immediately and attempts real API calls to Google Analytics and Google Ads. Both fail because the stub token is not a valid OAuth bearer token. The `run_all_providers` path returns `{:error, :all_providers_failed}` and broadcasts `{:sync_failed, ...}` to the LiveView, which shows the error flash "Sync failed for google: all_providers_failed".

As a result, Scenario 4 (success state after sync completes) and the intermediate loading state in Scenario 3 cannot be fully verified via browser automation. The success flash, sync result badge, and post-completion button re-enable state all depend on a successful sync response from the data providers.

To make these scenarios testable in QA, one of the following approaches is needed:
- Seed the database with a real (non-expired) OAuth token for a test account
- Add a dev-mode stub data provider that returns fake metrics without making real API calls
- Use `mix spex` to run the BDD spex (criteria 4053, 4054) which simulate the async messages directly via `send(context.view.pid, {:sync_completed, ...})` — this is explicitly noted in the brief as the recommended path for those criteria

### "Sync failed for google: all_providers_failed" error message is not user-friendly

#### Severity
LOW

#### Scope
APP

#### Description

When a sync attempt fails because all data providers return errors, the LiveView receives `{:sync_failed, %{provider: :google, reason: :all_providers_failed}}`. The `handle_info/2` clause in `index.ex` renders this as the flash: "Sync failed for google: all_providers_failed".

Two issues:
1. The provider name renders as the raw atom string `"google"` rather than the display name "Google". The handler should look up the display name via `platform_display_name/2` (as the "sync" event handler does) rather than using the atom directly.
2. "all_providers_failed" is an internal error atom, not a user-facing message. A clearer message would be: "Sync failed for Google. Please check your connection and try again."

Reproduced by: logging in as `qa@example.com`, navigating to `http://localhost:4070/integrations`, clicking "Sync Now" on the Google card.
