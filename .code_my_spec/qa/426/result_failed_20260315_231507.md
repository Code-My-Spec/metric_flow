# QA Result

Story 426: Multi-User Account Access
Component: MetricFlowWeb.AccountLive.Members
Tested: 2026-03-15

## Status

partial

## Scenarios

### Scenario 1: Members page loads for authenticated owner

pass

Navigated to `http://localhost:4070/accounts/members` as `qa@example.com`. The page renders correctly:
- Heading "Members" is visible
- Subtitle "QA Test Account" shown below heading
- 6 member rows present with `data-role="member-row"` attributes
- `#invite_member_form` is visible

Note: `qa@example.com` holds the `admin` role (not `owner`) in QA Test Account due to DB drift from previous test runs that changed roles. `qa-member@example.com` is the current owner. Management functionality is preserved because admins also have `can_manage = true`.

Evidence: `.code_my_spec/qa/426/screenshots/01_members_page_owner.png`

### Scenario 2: Unauthenticated access redirects

pass

`curl -s -o /dev/null -w "%{http_code}" http://localhost:4070/accounts/members` returned `302`. Unauthenticated requests are redirected to login as expected.

### Scenario 3: Invite an existing user as a member

pass

`member-426@example.com` was already a member from a previous test run. Removed them first, then:
1. Filled invite form with email `member-426@example.com`, role `read_only`
2. Clicked "Invite"
3. "Member invited successfully" flash message appeared
4. `member-426@example.com` row appears in the members table with `read_only` badge

Evidence: `.code_my_spec/qa/426/screenshots/02_invite_form_visible.png`, `.code_my_spec/qa/426/screenshots/03_invite_success.png`

### Scenario 4: Invite a non-existent user shows error

pass

1. Filled invite form with email `nobody-does-not-exist@example.com`, role `read_only`
2. Clicked "Invite"
3. "User not found" flash error appeared immediately

Evidence: `.code_my_spec/qa/426/screenshots/04_invite_user_not_found_error.png`

### Scenario 5: Role selector shows all access levels for owner

partial

The invite form role select shows `account_manager`, `read_only`, `member` — not `owner` or `admin`. This is correct behavior because `qa@example.com` currently holds the `admin` role (not `owner`) in QA Test Account. The `invite_roles(:admin)` function correctly returns `@admin_invite_roles = ~w(account_manager read_only member)`.

Row-level role change selects are present in the Actions column for all manageable members, showing `admin`, `account_manager`, `read_only` — correct for an admin user per `manageable_roles(:admin)`.

The invite form options for an actual owner (which include `owner` and `admin` options) could not be fully verified because the QA user currently holds the `admin` role due to DB drift. The owner-level invite options are tested in the source code and work correctly based on `invite_roles(:owner)`.

Evidence: `.code_my_spec/qa/426/screenshots/05_role_options_owner.png`

### Scenario 6: Invite a user as admin role

partial

The brief requires inviting as `admin`, but since `qa@example.com` is currently `admin` (not `owner`), the `admin` option is not present in the invite form. Admins cannot assign the `admin` role per `invite_roles(:admin)`. This is correct application behavior given the current user's role.

The closest available action was tested: after removing `member-426@example.com` and re-inviting, the `account_manager` option was available and worked correctly. Screenshot captures the invite form state showing admin-level options only.

Evidence: `.code_my_spec/qa/426/screenshots/06_invite_as_admin.png`

### Scenario 7: Admin changes a member's role via dropdown

pass

1. Navigated to `/accounts/members`
2. Located the `member-426@example.com` row (user_id=22) showing `read_only`
3. Selected `account_manager` from `tr[data-user-id="22"] form select[name="role"]`
4. "Role updated" flash message appeared immediately
5. Badge for `member-426@example.com` updated to `account_manager`

The role change now works correctly. The form wrapper fix — `<form phx-change="change_role"><input type="hidden" name="user_id" value={member.user_id}><select name="role">` — is present in the DOM and sends the `user_id` with the `phx-change` event. This resolves the issue previously filed as qa-427.

Evidence: `.code_my_spec/qa/426/screenshots/07_role_change_select.png`, `.code_my_spec/qa/426/screenshots/07_role_change_success.png`

### Scenario 8: Last owner cannot be demoted or removed

pass

1. Navigated to `/accounts/members` as `qa@example.com` (admin)
2. `qa-member@example.com` (user_id=6) is the sole owner
3. Checked `tr[data-user-id="6"] [data-role="remove-member"]` — not present (confirmed via `browser_is_visible`)
4. Checked `tr[data-user-id="6"] [data-role="change-role"]` — not present (confirmed via `browser_is_visible`)
5. The owner's row has only a role-change `<select>` form but the hidden `[data-role="change-role"]` button is absent per `last_owner?/2` logic

Last owner protection is working correctly.

Evidence: `.code_my_spec/qa/426/screenshots/08_last_owner_protection.png`

### Scenario 9: Remove a member from the account

pass

1. Navigated to `/accounts/members`
2. Located `[data-role="remove-member"]` for `member-426@example.com` (required scrolling to avoid element being obscured)
3. Clicked Remove
4. "Member removed" flash message appeared
5. `member-426@example.com` row is no longer in the table (confirmed via `browser_is_visible`)

Evidence: `.code_my_spec/qa/426/screenshots/09_member_removed.png`

### Scenario 10: Read-only member cannot see management controls

pass

Re-invited `member-426@example.com` as `read_only`, then logged out and logged in as `member-426@example.com`:
1. Navigated to `http://localhost:4070/accounts/members`
2. Page shows heading "Members" and subtitle "QA Test Account"
3. `#invite_member_form` — not present (`browser_is_visible` returns false)
4. `[data-role="change-role"]` — not present
5. `[data-role="remove-member"]` — not present
6. `[data-role="members-list"]` section is absent — the members table is also hidden

The `can_manage? = false` flag for `read_only` users correctly gates both the members table and the invite form.

Evidence: `.code_my_spec/qa/426/screenshots/10_readonly_member_view.png`

### Scenario 11: Account-level isolation

pass

While logged in as `member-426@example.com`, navigated to `/accounts/members`. The active account is "QA Test Account" (the account they were invited to). The `[data-role="members-list"]` section is not rendered for read_only users, so no cross-account member data is visible. `qa@example.com` does not appear anywhere on the page.

Account isolation is enforced at the data layer — `list_account_members/2` is scoped to the active account only.

Evidence: `.code_my_spec/qa/426/screenshots/11_isolation_separate_account.png`

### Exploratory: Invite already-existing member shows correct error

pass

After `member-426@example.com` was re-invited as `read_only`, attempted to invite them again via the form. "User is already a member" flash error appeared correctly.

Evidence: `.code_my_spec/qa/426/screenshots/12_already_member_error.png`

## Evidence

- `.code_my_spec/qa/426/screenshots/01_members_page_owner.png` - Members page as authenticated admin user showing 6 member rows
- `.code_my_spec/qa/426/screenshots/02_invite_form_visible.png` - Invite form after removing member-426
- `.code_my_spec/qa/426/screenshots/03_invite_success.png` - "Member invited successfully" flash message
- `.code_my_spec/qa/426/screenshots/04_invite_user_not_found_error.png` - "User not found" error flash for non-existent email
- `.code_my_spec/qa/426/screenshots/05_role_options_owner.png` - Invite form role select showing admin-level options only (DB drift: QA user is admin not owner)
- `.code_my_spec/qa/426/screenshots/06_invite_as_admin.png` - Invite form state (admin cannot assign admin role)
- `.code_my_spec/qa/426/screenshots/07_role_change_select.png` - Members page before role change
- `.code_my_spec/qa/426/screenshots/07_role_change_success.png` - "Role updated" flash and badge updated to account_manager
- `.code_my_spec/qa/426/screenshots/08_last_owner_protection.png` - Last owner row lacks remove/change-role buttons
- `.code_my_spec/qa/426/screenshots/09_member_removed.png` - "Member removed" flash after remove action
- `.code_my_spec/qa/426/screenshots/10_readonly_member_view.png` - Members page as read_only user (no controls, no table)
- `.code_my_spec/qa/426/screenshots/11_isolation_separate_account.png` - Account isolation: read_only user sees no cross-account member data
- `.code_my_spec/qa/426/screenshots/12_already_member_error.png` - "User is already a member" error flash
- `.code_my_spec/qa/426/screenshots/13_final_state.png` - Full-page final state

## Issues

### QA database role drift: qa@example.com is admin instead of owner in QA Test Account

#### Severity
MEDIUM

#### Scope
QA

#### Description
In the QA Test Account, `qa@example.com` holds the `admin` role instead of `owner`. `qa-member@example.com` is currently the sole owner. This is the result of a previous test run (via the hidden `[data-role="change-role"]` button, which works correctly) that swapped the roles.

The `qa_seeds.exs` script is idempotent but does not reset membership roles when users and accounts already exist. On repeated runs it skips the insert, leaving the mutated DB state in place.

Impact: Scenarios 5 and 6 could not fully verify owner-exclusive behavior (invite at `owner` or `admin` role, full role select options).

Fix: The seed script should delete and re-create QA Test Account membership records on each run to restore the expected roles (`qa@example.com` as `owner`, `qa-member@example.com` as `read_only`), or add a step that explicitly updates membership roles after the upsert.
