# QA Result

Story 509: Sync Google Analytics 4 Data

## Status

partial

## Scenarios

### Scenario 1 — Page loads with all required sections

pass

Navigated to `http://localhost:4070/integrations/sync-history`. All required sections present:

- H1 "Sync History" — found
- Subtitle "View automated sync results and status" — found via `h1 + p`
- `[data-role="sync-schedule"]` — found
- `[data-role="date-range"]` — found
- `[data-role="sync-history"]` — found (populated with persisted entries from prior test runs)

Evidence: `.code_my_spec/qa/509/screenshots/01-sync-history-page.png`

### Scenario 2 — Schedule section describes GA4 and automated sync behavior

pass

Read text from `[data-role="sync-schedule"]`:

> "Daily at 2:00 AM UTC — retrieves metrics and financial data per provider, per day. Covers marketing providers (Google Ads, Facebook Ads, Google Analytics) and financial providers (QuickBooks). On first sync, all available historical data is backfilled. Failed syncs are automatically retried up to 3 times with exponential backoff."

All required text present:
- "Google Analytics" — found
- "Google Ads" — found
- "Facebook Ads" — found
- "QuickBooks" — found
- "Daily" (daily sync frequency) — found
- "backfilled" (backfill behavior) — found
- "retried" (retry behavior) — found
- `.badge.badge-info` labeled "Daily" — found

Evidence: `.code_my_spec/qa/509/screenshots/02-schedule-section.png`

### Scenario 3 — Date range section shows yesterday's date, today excluded

pass

Read text from `[data-role="date-range"]`:

> "Showing data through 2026-03-16 (yesterday — today excluded, incomplete day)"

- Yesterday's date `2026-03-16` in ISO 8601 format — found (today is 2026-03-17)
- "today excluded" — found
- "yesterday" — found

Evidence: `.code_my_spec/qa/509/screenshots/03-date-range.png`

### Scenario 4 — Empty state when no sync records exist

not_applicable

The QA database contains 36 persisted `sync_history` records from prior test runs. The empty state was not observable in this environment. The source code (lines 98–108 of `sync_history.ex`) was reviewed directly:

- Empty state card text "No sync history yet." — present in template
- Secondary text references "Initial Sync" and backfill behavior — present in template
- `data-sync-type="initial"` badge with `class="badge badge-ghost"` labeled "Initial Sync" — present in template

The template is correct. Empty state could not be exercised without resetting the database.

Evidence: `.code_my_spec/qa/509/screenshots/04-has-records-not-empty.png`

### Scenario 5 — Filter tabs are present and toggle correctly

pass

All three filter buttons present (`filter-all`, `filter-success`, `filter-failed`).

Default state:
- `[data-role="filter-all"]` class: `btn btn-sm btn-primary` (active)
- `[data-role="filter-success"]` class: `btn btn-sm btn-ghost` (inactive)
- `[data-role="filter-failed"]` class: `btn btn-sm btn-ghost` (inactive)

After clicking "Success":
- `[data-role="filter-success"]` class: `btn btn-sm btn-primary` (active)
- `[data-role="filter-all"]` class: `btn btn-sm btn-ghost` (reverted)

After clicking "Failed":
- `[data-role="filter-failed"]` class: `btn btn-sm btn-primary` (active)

After clicking "All":
- `[data-role="filter-all"]` class: `btn btn-sm btn-primary` (restored)

Filter toggling works correctly in all directions.

Evidence: `.code_my_spec/qa/509/screenshots/05-filter-failed-active.png`

### Scenario 6 — Unauthenticated access is redirected to login

pass

Clicked the "Log out" link (`data-method="delete"`), which redirected to `/users/log-in`. Then navigated directly to `http://localhost:4070/integrations/sync-history`. The browser landed on `http://localhost:4070/users/log-in` — the protected route correctly redirected the unauthenticated request.

Evidence: `.code_my_spec/qa/509/screenshots/06-unauthenticated-redirect.png`

### Scenario 7 — BDD spex suite execution

partial

Ran all 15 spex files individually (the `mix spex` glob fails when given a directory):

- criterion_4746 — pass (1 test, 0 failures)
- criterion_4747 — pass (1 test, 0 failures)
- criterion_4748 — pass (1 test, 0 failures)
- criterion_4750 — pass (1 test, 0 failures)
- criterion_4751 — pass (1 test, 0 failures)
- criterion_4752 — pass (1 test, 0 failures)
- criterion_4753 — pass (1 test, 0 failures)
- criterion_4754 — pass (1 test, 0 failures)
- criterion_4755 — pass (1 test, 0 failures)
- criterion_4756 — pass (1 test, 0 failures)
- criterion_4757 — pass (1 test, 0 failures)
- criterion_4758 — pass (1 test, 0 failures)
- criterion_4759 — pass (1 test, 0 failures)
- criterion_4760 — pass (1 test, 0 failures)
- criterion_4761 — FAILED TO LOAD: "a system limit has been reached"

14 of 15 spex files passed. criterion_4761 could not be executed due to a filesystem error: the filename is exactly 255 characters, which is the POSIX NAME_MAX limit. The Erlang VM cannot open a file at the maximum-length boundary on macOS. The file exists on disk and is readable by other tools, but `mix spex` (which uses the Erlang file loader) fails with the system limit error.

## Evidence

- `.code_my_spec/qa/509/screenshots/01-sync-history-page.png` — full-page view on initial load, populated with sync history entries
- `.code_my_spec/qa/509/screenshots/02-schedule-section.png` — schedule section showing "Daily" badge and provider coverage text
- `.code_my_spec/qa/509/screenshots/03-date-range.png` — date range section showing yesterday's date (2026-03-16)
- `.code_my_spec/qa/509/screenshots/04-has-records-not-empty.png` — sync history list with persisted records (empty state not observable)
- `.code_my_spec/qa/509/screenshots/05-filter-failed-active.png` — filter tabs with "Failed" active (btn-primary)
- `.code_my_spec/qa/509/screenshots/06-unauthenticated-redirect.png` — login page shown after unauthenticated access attempt

## Issues

### Spex criterion_4761 filename exceeds Erlang file loader limit (255 bytes)

#### Severity
MEDIUM

#### Scope
QA

#### Description
The spex file `test/spex/509_sync_google_analytics_4_data/criterion_4761_because_ga4s_runreport_endpoint_has_a_10-metric_limit_per_request_metrics_are_fetched_in_chunks_of_10_and_results_are_merged_by_date_before_storage_the_final_stored_data_is_equivalent_to_what_a_single-request_response_would_contain_spex.exs` has a filename that is exactly 255 characters long. On macOS, the filesystem allows filenames up to 255 characters, but the Erlang VM's file module fails with "a system limit has been reached" when attempting to load a file at this exact boundary. Running `mix spex` against the story 509 directory produces the error: `Failed to load [path]: a system limit has been reached`.

The file content is valid and the test scenarios inside it are meaningful. The fix is to shorten the filename — for example, by removing "the_final_stored_data_is_equivalent_to_what_a_single-request_response_would_contain" from the suffix.

### Sync history entries show provider "Google" instead of "Google Analytics"

#### Severity
HIGH

#### Description
The `sync_history` table contains 36 records. All "Google" provider entries use `provider: :google` rather than `provider: :google_analytics` or `provider: :google_ads`. In the `SyncHistory` LiveView, `@provider_names` maps `:google` to `"Google"` and `:google_analytics` to `"Google Analytics"`. Because the provider is stored as `:google`, all these entries display as "Google" in the sync history list rather than the expected "Google Analytics" or "Google Ads".

This makes it impossible for users to distinguish which Google platform synced (Google Analytics vs Google Ads). The SyncWorker appears to be using the integration's OAuth provider name (`:google`) rather than the data provider name (`:google_analytics`/`:google_ads`) when creating sync history records.

Reproduced at `http://localhost:4070/integrations/sync-history`. All non-Facebook entries show "Google" as the provider name.

### error_message column stores truncated Postgres error details as user-visible text

#### Severity
MEDIUM

#### Description
Several `sync_history` entries with `status: :failed` have `error_message` values like `"ERROR 22001 (string_data_right_truncation) value too long for type character varying(255)"`. This is a raw Postgres exception message being surfaced directly to users in the sync history UI via `[data-role="sync-error"]`. Users should see a human-readable description of what failed, not a raw database error string. The error indicates that during some prior sync attempts, the system tried to insert a value that exceeded 255 characters in a `varchar(255)` column — but the error message itself is what was stored and is now displayed as the failure reason.

Observed at `http://localhost:4070/integrations/sync-history` in the sync history list.
