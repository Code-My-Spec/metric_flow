# QA Result

Story 517: Sync Google Business Profile Performance Metrics

## Status

partial

## Scenarios

### Scenario 1: Unauthenticated redirect

pass

`curl -s -o /dev/null -w "%{http_code}" http://localhost:4070/integrations/sync-history` returned HTTP 302. Unauthenticated users are correctly redirected before reaching the LiveView.

### Scenario 2: Sync history page loads for authenticated user

pass

Navigated to `/integrations/sync-history` as `qa@example.com`. Page rendered with H1 "Sync History" and subtitle "View automated sync results and status".

Evidence: `.code_my_spec/qa/517/screenshots/s2-sync-history-page.png`

### Scenario 3: Schedule section content and provider coverage

partial

The `[data-role="sync-schedule"]` section rendered with heading "Automated Sync Schedule". The description text correctly mentions "Google Ads", "Facebook Ads", "Google Analytics", and "QuickBooks". However, "Google Business Profile" is NOT mentioned anywhere in the schedule description. The static text was written before this integration was implemented and has not been updated.

Exact schedule text: "Daily at 2:00 AM UTC — retrieves metrics and financial data per provider, per day. Covers marketing providers (Google Ads, Facebook Ads, Google Analytics) and financial providers (QuickBooks). On first sync, all available historical data is backfilled. Failed syncs are automatically retried up to 3 times with exponential backoff."

Evidence: `.code_my_spec/qa/517/screenshots/s3-schedule-section.png`

### Scenario 4: Date range section

pass

`[data-role="date-range"]` showed "Showing data through 2026-03-16 (yesterday — today excluded, incomplete day)" which matches the expected format (yesterday's date, 2026-03-17 − 1 = 2026-03-16).

Evidence: `.code_my_spec/qa/517/screenshots/s4-date-range.png`

### Scenario 5: Filter tabs present with correct data-role attributes

pass

All three filter buttons rendered with correct `data-role` attributes:
- `[data-role="filter-all"]` — class `btn btn-sm btn-primary` (active by default)
- `[data-role="filter-success"]` — class `btn btn-sm btn-ghost` (inactive)
- `[data-role="filter-failed"]` — class `btn btn-sm btn-ghost` (inactive)

After clicking "Success": `filter-success` became `btn-primary`, `filter-all` reverted to `btn-ghost`. After clicking "Failed": `filter-failed` became `btn-primary`. After clicking "All": `filter-all` returned to `btn-primary`.

Evidence: `.code_my_spec/qa/517/screenshots/s5-filter-all-active.png`, `.code_my_spec/qa/517/screenshots/s5b-filter-success-active.png`, `.code_my_spec/qa/517/screenshots/s5c-filter-failed-active.png`

### Scenario 6: Empty state when no sync history exists

skip

The QA seed account had existing sync history records from prior QA runs (seeds did not clear data since the Phoenix server was already running — re-seeding was not possible without stopping the server). The empty state (`[data-role="sync-history"]` with "No sync history yet.") could not be verified via browser. The source code review confirms the empty state block is present and will render when `@sync_history == [] and @sync_events == []`.

Evidence: `.code_my_spec/qa/517/screenshots/s6-sync-history-with-data.png`

### Scenario 7: google_business provider display name gap

fail

Source code inspection of `lib/metric_flow_web/live/integration_live/sync_history.ex` confirms that `@provider_names` does NOT include `:google_business` or `:google_business_reviews`. These providers will fall through to `derive_display_name/1` which produces "Google Business" and "Google Business Reviews" respectively. The display names themselves are acceptable and the BDD spex assertions accept these strings. However, the explicit omission from `@provider_names` is a gap — all other active providers are enumerated there.

Additionally, the schedule description omits Google Business Profile from the list of covered providers (see Scenario 3).

### Scenario 8: BDD spex suite (mix spex)

partial

9 of 11 spex files executed and passed. 2 files failed to load due to ExUnit test name length limit (255 characters):

- `criterion_4819_..._spex.exs` — spex title is 258 characters: "The following metrics are fetched as daily time series: BUSINESS_IMPRESSIONS_DESKTOP_MAPS, BUSINESS_IMPRESSIONS_DESKTOP_SEARCH, BUSINESS_IMPRESSIONS_MOBILE_MAPS, BUSINESS_CONVERSATIONS, BUSINESS_DIRECTION_REQUESTS, CALL_CLICKS, WEBSITE_CLICKS, BUSINESS_BOOKINGS, BUSINESS_FOOD_ORDERS, BUSINESS_FOOD_MENU_CLICKS"
- `criterion_4827_..._spex.exs` — spex title is 265 characters: "Google Business Profile performance integration is distinct from GMB Reviews — both use the same account and location config but call different APIs and store different data under different platformServiceType values ('mybusiness' vs 'mybusiness-reviews')"

Passing criteria: 4817, 4818, 4820, 4821, 4822, 4823, 4824, 4825, 4826 (9 tests, 0 failures)
Failing to load: 4819, 4827

## Issues

### google_business and google_business_reviews missing from @provider_names map

#### Severity

LOW

#### Description

`lib/metric_flow_web/live/integration_live/sync_history.ex` defines `@provider_names` with explicit display names for `google_ads`, `facebook_ads`, `google_analytics`, `google_search_console`, `quickbooks`, and `google`. The new Story 517 providers `:google_business` and `:google_business_reviews` are absent from this map. They fall through to `derive_display_name/1` which produces "Google Business" and "Google Business Reviews" — both acceptable — but all other active providers are explicitly enumerated. The map should be updated to include the new providers for consistency and to avoid silent fallthrough for future name changes.

#### Scope

app

---

### Schedule section does not mention Google Business Profile as a covered provider

#### Severity

LOW

#### Description

The `[data-role="sync-schedule"]` static description text was written before Story 517 was implemented. It reads: "Covers marketing providers (Google Ads, Facebook Ads, Google Analytics) and financial providers (QuickBooks)." Google Business Profile is now a supported provider and should be listed. Reproduction: visit `/integrations/sync-history` — the Automated Sync Schedule section does not mention Google Business Profile.

#### Scope

app

---

### Spex criterion 4819 and 4827 fail to load due to test name length exceeding 255 characters

#### Severity

MEDIUM

#### Description

Two spex files in `test/spex/517_sync_google_business_profile_performance_metrics/` cannot be loaded by ExUnit because their computed test names exceed the 255-character limit:

- Criterion 4819: spex title is 258 chars — "The following metrics are fetched as daily time series: BUSINESS_IMPRESSIONS_DESKTOP_MAPS, BUSINESS_IMPRESSIONS_DESKTOP_SEARCH, BUSINESS_IMPRESSIONS_MOBILE_MAPS, BUSINESS_CONVERSATIONS, BUSINESS_DIRECTION_REQUESTS, CALL_CLICKS, WEBSITE_CLICKS, BUSINESS_BOOKINGS, BUSINESS_FOOD_ORDERS, BUSINESS_FOOD_MENU_CLICKS"
- Criterion 4827: spex title is 265 chars — "Google Business Profile performance integration is distinct from GMB Reviews — both use the same account and location config but call different APIs and store different data under different platformServiceType values ('mybusiness' vs 'mybusiness-reviews')"

Error: `the computed name of a test must be shorter than 255 characters`. Both files need their `spex "..."` title shortened while preserving the meaning.

#### Scope

qa
