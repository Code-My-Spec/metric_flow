## Status

fail

## Scenarios

### Scenario 1 — Page loads and shows sync schedule

pass

H1 reads "Sync History". The `[data-role="sync-schedule"]` card is present and its description mentions "Google Business Profile" explicitly ("Covers marketing providers (Google Ads, Facebook Ads, Google Analytics, Google Business Profile, Google Search Console)..."). The `.badge-info` "Daily" badge is present. The `[data-role="date-range"]` shows "Showing data through 2026-03-17 (yesterday — today excluded, incomplete day)" — correct (server UTC date is 2026-03-18, so yesterday is 2026-03-17).

### Scenario 2 — Provider name mapping for google_business_reviews

pass

The `@provider_names` map in `sync_history.ex` line 26 includes `google_business_reviews: "Google Business Reviews"`. The page has persisted sync history entries (Google Ads, Google Analytics, Facebook Ads) — no `google_business_reviews` entries are persisted in the QA database. No empty-state card is shown. The `provider_display_name/1` function correctly maps the atom to "Google Business Reviews" as verified by the spex tests.

### Scenario 3 — Filter tabs are rendered and functional

pass

All three filter buttons are present: `[data-role="filter-all"]`, `[data-role="filter-success"]`, `[data-role="filter-failed"]`. "All" button has class `btn-primary` by default. Clicking "Success" makes it `btn-primary`. Clicking "Failed" makes it `btn-primary`. Clicking "All" restores `btn-primary` to "All". Filter behavior is functional.

### Scenario 4 — Unauthenticated access is redirected

pass

`curl -s -o /dev/null -w "%{http_code}" http://localhost:4070/integrations/sync-history` returned `302`.

### Scenario 5 — Persisted sync history entries render correctly

pass

Multiple `data-role="sync-history-entry"` elements are present. Each shows a `data-role="sync-provider"` span with the display name, a status badge (`.badge-success` or `.badge-error`), and for success entries a "records synced" count (e.g., "186 records synced"). No `google_business_reviews` persisted entry exists in the QA database — the provider name mapping is verified via spex.

### Scenario 6 — Failed filter shows only failed entries

pass

Clicking the "Failed" filter tab shows only entries with `data-status="failed"` (Google Ads failed, Google Analytics failed, Facebook Ads failed entries). Success entries are hidden from the list. Clicking "All" restores all entries.

### Scenario 7 — mix spex suite for Story 513

fail

10 of 11 spex tests pass. 1 failure:

**Failing test:** `criterion_4797` — "filtering by Failed shows only failed entries; filtering by Success shows only success entries" — `then_ "only the failed entry is shown"`

**Failure reason:** The assertion `refute html =~ "Success"` fails because the "Success" filter tab button (`<button data-role="filter-success">Success</button>`) is always present in the rendered HTML, even when the filter is set to "Failed". The spex checks for the string "Success" anywhere in the HTML, but the filter button itself contains that word. The application behavior is correct — the history entries are properly filtered — but the test assertion is too broad.

## Evidence

- `.code_my_spec/qa/513/screenshots/01-sync-history-page-load.png`
- `.code_my_spec/qa/513/screenshots/02-empty-state-or-history.png`
- `.code_my_spec/qa/513/screenshots/03-filter-tabs.png`
- `.code_my_spec/qa/513/screenshots/05-persisted-entries.png`
- `.code_my_spec/qa/513/screenshots/06-failed-filter.png`

## Issues

### criterion_4797 filter assertion is too broad — refutes "Success" string but filter button always renders it

#### Severity

LOW

#### Description

In `criterion_4797_sync_failures_for_individual_customers_..._spex.exs`, the scenario "filtering by Failed shows only failed entries" contains:

```elixir
refute html =~ "Success",
       "Expected the Success entry to be hidden after clicking the Failed filter, got: #{html}"
```

When the Failed filter is active, the sync history entries list correctly shows only failed entries. However, the filter tab button `<button data-role="filter-success">Success</button>` is always rendered in the HTML regardless of which filter is active. The `html =~ "Success"` match hits the button text, causing the refute to fail.

The application filtering behavior is correct. The spex assertion must be scoped to the history entries only, e.g., checking that no `data-role="sync-history-entry"` element contains "badge-success" rather than checking the full page HTML for the string "Success".

Reproduction: Run `mix spex test/spex/513_sync_google_business_profile_reviews/criterion_4797_...` — the third scenario ("filtering by Failed shows only failed entries") fails at the `then_ "only the failed entry is shown"` step.

#### Scope

qa
