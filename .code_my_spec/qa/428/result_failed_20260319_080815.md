# QA Result

Story 428 — Client Invites Agency or Individual User Access

## Status

fail

## Scenarios

### AC1-A — Invitation form is present on the page

pass

Navigated to `/accounts/invitations` as `qa@example.com`. The page renders with:
- H1 "Invite Members" visible
- `#invite_member_form` present
- `input[name='invitation[email]']` present
- `select[name='invitation[role]']` present
- `[data-role="submit-invite"]` button with text "Send Invitation" present

Note: On fresh page load, both the email and role fields show "can't be blank" validation errors before any user interaction. See Issue below.

Evidence: `.code_my_spec/qa/428/screenshots/01_invitations_page_initial.png`

### AC1-B — Send invitation to an external email address

pass

Filled `input[name='invitation[email]']` with `agency@externalfirm.com`, left role at default (Read Only), clicked "Send Invitation". Flash appeared: "Invitation sent to agency@externalfirm.com." — matches expected success message.

Evidence: `.code_my_spec/qa/428/screenshots/02_ac1b_external_invite_success.png`

### AC1-C — Send invitation to an existing user email address

pass

Filled email with `qa-member@example.com`, submitted form. Flash: "Invitation sent to qa-member@example.com." — success flash shown. The invitation also appeared in the pending invitations list.

Evidence: `.code_my_spec/qa/428/screenshots/03_ac1c_existing_user_invite_success.png`

### AC2 & AC3 — Email is delivered with secure link and 7-day expiry

pass

Navigated to `http://localhost:4070/dev/mailbox`. Found emails addressed to `qa-member@example.com`. Verified:
- Subject: "You've been invited to QA Test Account" (contains "invited")
- Body: contains `qa-member@example.com`
- Body: contains link path matching `/invitations/` (e.g., `http://localhost:4070/invitations/PqjpWd178kK4c1-OdOBvG6Ks7tGSgE6JMdw1ZReNO3I`)
- Body: contains "This invitation expires in 7 days."

Evidence: `.code_my_spec/qa/428/screenshots/04_ac2_mailbox.png`, `.code_my_spec/qa/428/screenshots/05_ac2_email_content.png`

### AC4 — Email and acceptance page show account name and access level

partial

Sent invitation to `qa-member@example.com` with role `admin`. Retrieved the token from `/dev/mailbox` (`K89w4tgR0pX8Jvmn-PXS-xIJk-mcFl3wc1dNlq1Cmuo`). Navigated to `/invitations/{token}` as the authenticated owner.

Acceptance page showed: "qa@example.com has invited you to join QA Test Account as a Admin." — account name "QA Test Account" and access level "Admin" are both present on the acceptance page.

Email subject: "You've been invited to QA Test Account" — account name is in the subject.
Email body: "You have been invited to join QA Test Account on MetricFlow." — account name in body.
Email body: does NOT explicitly mention the role/access level. The acceptance page shows the role, but the email text itself does not include it.

Evidence: `.code_my_spec/qa/428/screenshots/06_ac4_mailbox_admin_invite.png`, `.code_my_spec/qa/428/screenshots/07_ac4_acceptance_page.png`

### AC5-A — Role select offers all three access levels

pass

Inspected `select[name='invitation[role]']`. All three options present:
- `value="read_only"` — "Read Only"
- `value="admin"` — "Admin"
- `value="account_manager"` — "Account Manager"

Evidence: `.code_my_spec/qa/428/screenshots/08_ac5_role_select.png`

### AC5-B — Invitation can be sent with each role

pass

Sent invitations to three distinct emails with each role:
- `role-test-readonly@example.com` with `read_only` → "Invitation sent to role-test-readonly@example.com."
- `role-test-manager@example.com` with `account_manager` → "Invitation sent to role-test-manager@example.com."
- `role-test-admin@example.com` with `admin` → "Invitation sent to role-test-admin@example.com."

All three appear in the pending list with their correct role badges (Read Only, Account Manager, Admin).

Evidence: `.code_my_spec/qa/428/screenshots/09_ac5_all_roles_pending.png`

### AC6-A — Accepted invitation link cannot be reused

fail

Sent invitation to `qa-member@example.com` with role `read_only`. Retrieved token `PqjpWd178kK4c1-OdOBvG6Ks7tGSgE6JMdw1ZReNO3I`. Logged in as `qa-member@example.com` and navigated to the invitation URL. The acceptance page rendered with the correct content. Clicked "Accept Invitation" — was redirected to `/accounts` with flash "You already have access to this account."

Navigated to the same invitation URL a second time. The acceptance page still rendered — the form showed again as if the invitation was still valid. Clicked "Accept Invitation" again — same redirect and "You already have access to this account." flash appeared again.

Expected: second visit should redirect with error "invalid or has already been used." Instead, the page still renders the acceptance form for an already-accepted invitation.

Evidence: `.code_my_spec/qa/428/screenshots/11_ac6_acceptance_page_before_accept.png`, `.code_my_spec/qa/428/screenshots/12_ac6_after_accept.png`, `.code_my_spec/qa/428/screenshots/13_ac6_second_visit_accepted_link.png`, `.code_my_spec/qa/428/screenshots/14_ac6_second_accept_no_error.png`

### AC6-B — Valid pending invitation link shows acceptance page

pass

Retrieved token `6f8oeeEf_KTPHQyV5Gz75CvqTQWB1paSMrp93sSlrOQ` (for `role-test-admin@example.com`, role admin). Navigated to this URL unauthenticated (fresh browser session, no cookies). The page rendered with "You've been invited" heading, account name, role, and "Log In to Accept" / "Create an Account" buttons. The invitation content is visible to unauthenticated users.

Evidence: `.code_my_spec/qa/428/screenshots/15_ac6_valid_link_unauthenticated.png`

### AC7-A — Pending invitations list is visible after sending

pass

Sent invitation to `pending-view@agency.com` with role `read_only`. After submission:
- `[data-role="pending-invitations"]` section is visible
- `pending-view@agency.com` appears in the list (`[data-role="invitation-email"]`)
- Role badge "Read Only" shown
- "Expires Mar 26, 2026" expiry date shown
- "Cancel" button (`data-role="cancel-invitation"`) present for the invitation
- "Sent just now" timestamp shown

Evidence: `.code_my_spec/qa/428/screenshots/16_ac7_pending_list_visible.png`

### AC7-B — Owner can cancel a pending invitation

pass

Sent invitation to `tocancel@agency.com` with role `read_only`. Clicked the `[data-role="cancel-invitation"][data-email="tocancel@agency.com"]` button. Flash appeared: "Invitation to tocancel@agency.com cancelled." The invitation row was removed from `[data-role="pending-invitation-row"]` immediately.

Evidence: `.code_my_spec/qa/428/screenshots/17_ac7_cancel_success.png`

### AC7-C — Empty state shown when no invitations are pending

pass

On initial page load at the start of this QA session (before any invitations were sent), the pending invitations section showed: "No pending invitations." The empty state rendered correctly.

Evidence: `.code_my_spec/qa/428/screenshots/01_invitations_page_initial.png`

### AC8 — Multiple invitations appear in pending list simultaneously

pass

Sent `first@agency.com` with `read_only` and `second@agency.com` with `admin`. Both appeared in the pending list with correct role badges. The form reset after each submission and was ready for additional entries.

Evidence: `.code_my_spec/qa/428/screenshots/18_ac8_multiple_invitations.png`

### Exploratory — Input validation

pass

Submitted the form with an invalid email `not-a-valid-email` — validation error "must be a valid email address" appeared. Submitted with empty email — "can't be blank" appeared. Both validations work correctly on user interaction.

Evidence: `.code_my_spec/qa/428/screenshots/19_exploratory_invalid_email.png`, `.code_my_spec/qa/428/screenshots/20_exploratory_empty_email.png`

## Evidence

- `.code_my_spec/qa/428/screenshots/01_invitations_page_initial.png` — Initial page load showing "Invite Members" form and empty state
- `.code_my_spec/qa/428/screenshots/02_ac1b_external_invite_success.png` — Success flash after sending to external email
- `.code_my_spec/qa/428/screenshots/03_ac1c_existing_user_invite_success.png` — Success flash after inviting existing user
- `.code_my_spec/qa/428/screenshots/04_ac2_mailbox.png` — Dev mailbox showing invitation emails
- `.code_my_spec/qa/428/screenshots/05_ac2_email_content.png` — Email content with invitation link and 7-day expiry
- `.code_my_spec/qa/428/screenshots/06_ac4_mailbox_admin_invite.png` — Admin role invitation in dev mailbox
- `.code_my_spec/qa/428/screenshots/07_ac4_acceptance_page.png` — Acceptance page showing account name and "Admin" role
- `.code_my_spec/qa/428/screenshots/08_ac5_role_select.png` — Role select with all three options
- `.code_my_spec/qa/428/screenshots/09_ac5_all_roles_pending.png` — Pending list with all three roles
- `.code_my_spec/qa/428/screenshots/11_ac6_acceptance_page_before_accept.png` — Acceptance page before clicking Accept
- `.code_my_spec/qa/428/screenshots/12_ac6_after_accept.png` — After accepting: redirect to /accounts with "You already have access" flash
- `.code_my_spec/qa/428/screenshots/13_ac6_second_visit_accepted_link.png` — Second visit to accepted link still shows the form (bug)
- `.code_my_spec/qa/428/screenshots/14_ac6_second_accept_no_error.png` — Second accept attempt shows no invalidation error
- `.code_my_spec/qa/428/screenshots/15_ac6_valid_link_unauthenticated.png` — Valid pending link renders acceptance page for unauthenticated user
- `.code_my_spec/qa/428/screenshots/16_ac7_pending_list_visible.png` — Pending invitations list after sending
- `.code_my_spec/qa/428/screenshots/17_ac7_cancel_success.png` — Cancellation flash and removed invitation
- `.code_my_spec/qa/428/screenshots/18_ac8_multiple_invitations.png` — Multiple invitations in pending list
- `.code_my_spec/qa/428/screenshots/19_exploratory_invalid_email.png` — Invalid email validation error
- `.code_my_spec/qa/428/screenshots/20_exploratory_empty_email.png` — Empty email validation error
- `.code_my_spec/qa/428/screenshots/21_fresh_page_validation_errors.png` — Validation errors on fresh page load (bug)

## Issues

### Accepted invitation link is not invalidated after first use

#### Severity
HIGH

#### Description
After accepting an invitation link, navigating back to the same `/invitations/{token}` URL renders the acceptance page again instead of showing an error. Clicking "Accept Invitation" a second time redirects to `/accounts` with the flash "You already have access to this account." — but this only succeeds because the user is already a member. The token itself is not invalidated.

The expected behavior per AC6 is that a second visit to an accepted invitation link should show an error redirect with "invalid or has already been used." Instead, the page renders the full acceptance UI for an already-used token.

Reproduced with token `PqjpWd178kK4c1-OdOBvG6Ks7tGSgE6JMdw1ZReNO3I` for `qa-member@example.com`. The acceptance LiveView at `/invitations/:token` does not check whether the invitation has already been accepted before mounting.

Evidence: `.code_my_spec/qa/428/screenshots/13_ac6_second_visit_accepted_link.png`, `.code_my_spec/qa/428/screenshots/14_ac6_second_accept_no_error.png`

### Validation errors displayed on fresh page load without user interaction

#### Severity
MEDIUM

#### Description
When navigating to `/accounts/invitations` for the first time, the invite form immediately shows two "can't be blank" validation error messages — one for the email field and one for the role field — before the user has touched any input.

This is caused by `Invitations.change_invitation(scope, %{})` in `mount_for_role/4` producing a changeset with errors that are immediately rendered by the template. The form's `form_has_error?/2` helper returns true for both fields since the changeset validates required fields even on empty params.

The fix should suppress validation errors on the initial mount (e.g., using `action: nil` on the changeset, or tracking a `touched` state that enables error display only after the first submit or change event).

Reproduced by navigating to `http://localhost:4070/accounts/invitations` as `qa@example.com` on any fresh page load.

Evidence: `.code_my_spec/qa/428/screenshots/01_invitations_page_initial.png`, `.code_my_spec/qa/428/screenshots/21_fresh_page_validation_errors.png`

### Invitation email does not include the role/access level

#### Severity
LOW

#### Description
Per AC4, the invitation should include the client account name and access level. The email subject and body correctly include the account name ("QA Test Account"). However, the email body does not mention the role or access level granted to the recipient. Only the invitation acceptance page (at `/invitations/{token}`) shows the access level.

The email text reads: "You have been invited to join QA Test Account on MetricFlow. Click the link below to accept your invitation." — no mention of whether the invitee will be "Admin", "Account Manager", or "Read Only".

The acceptance page correctly shows the role ("qa@example.com has invited you to join QA Test Account as a Admin."), so AC4 is partially satisfied. The email itself should also include the access level for the user to make an informed decision before clicking the link.

Evidence: `.code_my_spec/qa/428/screenshots/05_ac2_email_content.png`, `.code_my_spec/qa/428/screenshots/06_ac4_mailbox_admin_invite.png`
