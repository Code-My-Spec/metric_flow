# QA Result

Story 428 — Client Invites Agency or Individual User Access

## Status

fail

## Scenarios

### AC1 Scenario A — Invitation form is present on the page

pass

Navigated to `http://localhost:4070/accounts/invitations` as `qa@example.com`. The page renders with:
- H1 heading "Invite Members" visible
- Form `#invite_member_form` present
- Email input `input[name='invitation[email]']` present with placeholder "colleague@example.com"
- Role select `select[name='invitation[role]']` present
- Submit button `[data-role="submit-invite"]` with text "Send Invitation" present

Evidence: `screenshots/01-invitations-page-initial.png`

### AC1 Scenario B — Send invitation to an external email address

pass

Filled `agency-new@externalfirm.com` in the email field, left role at default (Read Only), clicked "Send Invitation". A success flash appeared: "Invitation sent to agency-new@externalfirm.com."

Evidence: `screenshots/02-ac1-send-external-invitation.png`

### AC1 Scenario C — Send invitation to an existing user email address

pass

Filled `qa-member@example.com` in the email field, left role as Read Only, submitted. Flash appeared: "Invitation sent to qa-member@example.com." The invitation appeared in the pending invitations list.

Evidence: `screenshots/03-ac1-send-existing-user-invitation.png`

### AC2 & AC3 — Invitation email delivered with secure link and 7-day expiry

pass

Navigated to `http://localhost:4070/dev/mailbox`. Emails are present with subject "You've been invited to QA Test Account". The email body contains:
- Recipient email address (`qa-member@example.com`)
- A link path matching `/invitations/` (e.g., `http://localhost:4070/invitations/zjkPp-9w7_u4RJ8oRUXGXECjVBkR0QyXFkEUysHXTzw`)
- "This invitation expires in 7 days."

Evidence: `screenshots/04-dev-mailbox.png`, `screenshots/05-mailbox-email-detail.png`

### AC4 — Email and acceptance page show account name and role

pass

Sent an invitation to `admin-test@example.com` with role `admin`. Verified in mailbox:
- Subject: "You've been invited to QA Test Account" — contains account name
- Body: "You have been invited to join QA Test Account on MetricFlow."

Navigated to the acceptance link. The acceptance page showed:
- "qa@example.com has invited you to join QA Test Account as a Admin."
- Account name "QA Test Account" visible
- Role "Admin" visible

Evidence: `screenshots/07-mailbox-admin-invite-email.png`, `screenshots/08-ac4-admin-acceptance-page.png`

### AC5 Scenario — Role select offers all three access levels

pass

Inspected `select[name='invitation[role]']`. All three options present:
- `value="read_only"` labeled "Read Only"
- `value="admin"` labeled "Admin"
- `value="account_manager"` labeled "Account Manager"

Evidence: `screenshots/09-ac5-role-select.png`

### AC5 Scenario — Invitation can be sent with each role

pass

Sent three invitations:
1. `ac5-readonly@example.com` with `read_only` — flash: "Invitation sent to ac5-readonly@example.com."
2. `ac5-manager@example.com` with `account_manager` — flash: "Invitation sent to ac5-manager@example.com."
3. `ac5-admin@example.com` with `admin` — flash: "Invitation sent to ac5-admin@example.com."

All three appear in pending list with correct role badges: "Read Only", "Account Manager", "Admin".

Evidence: `screenshots/10-ac5-all-three-roles-sent.png`

### AC6 Scenario — Accepted invitation link cannot be reused

partial

1. Sent an invitation to `qa-member@example.com` with role `read_only`. Retrieved token from mailbox: `zjkPp-9w7_u4RJ8oRUXGXECjVBkR0QyXFkEUysHXTzw`.
2. Quit and relaunched browser. Logged in as `qa-member@example.com`.
3. Navigated to `/invitations/zjkPp-9w7_u4RJ8oRUXGXECjVBkR0QyXFkEUysHXTzw` — acceptance page rendered with "Accept Invitation" button.
4. Clicked "Accept Invitation". Redirected to `/accounts` with flash "You already have access to this account." (qa-member was already a member from prior test runs).
5. Navigated again to the same token URL.
6. Redirected to `/` (home page) — the token is invalidated. **However, no error flash was shown.** The expected flash "This invitation link is invalid or has already been used." was not rendered.

The link is correctly invalidated after use (redirects away), but the error flash message is lost during the LiveView mount redirect. See Issues section.

Evidence: `screenshots/13-ac6-acceptance-page-member.png`, `screenshots/14-ac6-after-acceptance.png`, `screenshots/15-ac6-reuse-accepted-link.png`, `screenshots/16-ac6-reuse-link-redirect.png`

### AC6 Scenario — Valid pending invitation link shows acceptance page

pass

Sent invitation to `link-test@example.com`, retrieved token `0hkiQnx6lfzEWD6Ak4AF9AsL2r4zP5cdBVbjo5_ozLY`. Quit and relaunched browser (unauthenticated). Navigated to `/invitations/0hkiQnx6lfzEWD6Ak4AF9AsL2r4zP5cdBVbjo5_ozLY`. The acceptance page loaded with:
- "You've been invited"
- "qa@example.com has invited you to join QA Test Account as a Read Only."
- "Log In to Accept" and "Create an Account" buttons (unauthenticated state)

Evidence: `screenshots/17-ac6-unauthenticated-valid-link.png`

### AC7 Scenario — Pending invitations list is visible after sending

pass

Sent invitation to `pending-view@agency.com` with role `read_only`. After submission:
- `[data-role="pending-invitations"]` section is visible
- `pending-view@agency.com` appears in `[data-role="invitation-email"]`
- Role badge "Read Only" shown
- Sent time and Expiry date ("Expires Mar 26, 2026") visible
- Cancel button `[data-role="cancel-invitation"][data-email="pending-view@agency.com"]` present

Note: no explicit "pending" status text exists in the invitation row. The section is titled "Pending Invitations" but individual rows do not include a status badge or text. This does not affect functionality.

Evidence: `screenshots/18-ac7-pending-invitations-list.png`

### AC7 Scenario — Owner can cancel a pending invitation

pass

Sent invitation to `tocancel@agency.com`. Clicked cancel button `[data-role="cancel-invitation"][data-email="tocancel@agency.com"]`. Flash appeared: "Invitation to tocancel@agency.com cancelled." The invitation row was immediately removed from the pending list. No `[data-role="cancel-invitation"][data-email="tocancel@agency.com"]` element found after cancellation.

Evidence: `screenshots/19-ac7-cancel-invitation.png`

### AC7 Scenario — Empty state shown when no invitations are pending

not tested

The test account has many pending invitations from prior test runs. Cancelling all of them manually was out of scope for this session. The empty state text "No pending invitations." is present in the source code (`lib/metric_flow_web/live/invitation_live/send.ex`, line 103) and renders via `<div :if={@pending_invitations == []}>`. Verified via code review.

### AC8 Scenario — Multiple invitations appear in the pending list

pass

Sent `first@agency.com` with `read_only`, then `second@agency.com` with `admin`. Both appeared in the pending list with correct role badges: "Read Only" for first, "Admin" for second. The invite form reset to empty after each submission, ready for additional entries.

Evidence: `screenshots/20-ac8-multiple-invitations.png`

## Evidence

- `screenshots/01-invitations-page-initial.png` — initial invitations page load
- `screenshots/02-ac1-send-external-invitation.png` — success flash after sending external invitation
- `screenshots/03-ac1-send-existing-user-invitation.png` — success flash sending to existing user
- `screenshots/04-dev-mailbox.png` — dev mailbox showing invitation emails
- `screenshots/05-mailbox-email-detail.png` — email detail with body, link, and expiry
- `screenshots/07-mailbox-admin-invite-email.png` — admin invitation email in mailbox
- `screenshots/08-ac4-admin-acceptance-page.png` — acceptance page showing account name and Admin role
- `screenshots/09-ac5-role-select.png` — role select with all three options
- `screenshots/10-ac5-all-three-roles-sent.png` — pending list after sending all three roles
- `screenshots/13-ac6-acceptance-page-member.png` — acceptance page loaded as qa-member
- `screenshots/14-ac6-after-acceptance.png` — redirect to /accounts after acceptance
- `screenshots/15-ac6-reuse-accepted-link.png` — second visit to accepted link (home page, no flash)
- `screenshots/16-ac6-reuse-link-redirect.png` — confirmed redirect to home with no flash message
- `screenshots/17-ac6-unauthenticated-valid-link.png` — valid link unauthenticated shows log-in options
- `screenshots/18-ac7-pending-invitations-list.png` — pending invitations list with all fields
- `screenshots/19-ac7-cancel-invitation.png` — cancellation flash and removed invitation row
- `screenshots/20-ac8-multiple-invitations.png` — two invitations with different roles in list
- `screenshots/21-final-invitations-page.png` — full page at end of test session

## Issues

### Flash message lost when revisiting an accepted or invalid invitation link

#### Severity
MEDIUM

#### Description
When navigating to an invitation link that has already been accepted (or is otherwise invalid), the `accept.ex` LiveView mount redirects to `/` with `put_flash(:error, "This invitation link is invalid or has already been used.")`. However, the error flash message does not appear on the destination page (`/`). The user is silently redirected to the home page with no feedback.

Reproduction steps:
1. Accept an invitation (the token is now consumed).
2. Navigate again to `http://localhost:4070/invitations/{same-token}`.
3. Observe: browser lands on `http://localhost:4070/` with no visible flash message.

Expected: error flash "This invitation link is invalid or has already been used." is visible on the home page after redirect.

The code at `lib/metric_flow_web/live/invitation_live/accept.ex` lines 114–118 calls `put_flash` before `redirect(to: "/")`. The flash may be lost because the home route (`/`) is a different LiveView that does not carry the flash from the previous socket. This may require using `Phoenix.LiveView.redirect` with flash carried in the session, or switching the destination to a route that processes the LiveView session flash correctly.
