# QA Result

Story 443: Create and Save Custom Reports — Dashboard Editor

Tested: 2026-03-20
Tester: QA Agent (Claude Sonnet 4.6)
App URL: http://localhost:4070
Auth: qa@example.com

## Status

partial

## Scenarios

### Scenario 1: Unauthenticated access is blocked

pass

Navigated to `http://localhost:4070/dashboards/new` without logging in. The app redirected to `http://localhost:4070/users/log-in` as expected. The editor was not accessible unauthenticated.

Evidence: `.code_my_spec/qa/443/screenshots/01-unauthenticated-redirect.png`

### Scenario 2: New Dashboard editor page loads for authenticated user

pass

After login, Phoenix preserved the originally requested URL and loaded `/dashboards/new` directly. The editor loaded with:
- Heading: "New Dashboard" (h1)
- `data-role="dashboard-name-input"` present with placeholder "My Dashboard"
- `data-role="save-dashboard-btn"` with text "Save Dashboard" present
- Cancel link (`href="/dashboard"`) present

Note: The Cancel link points to `/dashboard` (the analytics metrics view), not `/dashboards` (the custom reports list). This is a bug — see Issues.

Evidence: `.code_my_spec/qa/443/screenshots/02-new-dashboard-editor.png`

### Scenario 3: Template chooser is shown on the blank new-dashboard page

pass

On fresh load of `/dashboards/new`, the `data-role="template-chooser"` section was visible with:
- Intro text: "Start from a template or blank canvas"
- Template card for "Marketing Overview" (`data-role="template-card-marketing_overview"`)
- Template card for "Financial Summary" (`data-role="template-card-financial_summary"`)
- "Blank Canvas" card (`data-role="template-card-blank"`)
- Empty canvas state (`data-role="empty-canvas"`) visible

The name input had no error styling on fresh load (correct — `has_name_error?/1` properly checks `action: nil`).

Evidence: `.code_my_spec/qa/443/screenshots/03-template-chooser.png`

### Scenario 4: Selecting a template populates the canvas

pass

Clicked `data-role="template-card-marketing_overview"`. Result:
- 4 visualization cards appeared: Clicks (bar), Spend (line), Impressions (area), ROAS (line)
- Empty canvas state (`data-role="empty-canvas"`) disappeared
- Template chooser (`data-role="template-chooser"`) disappeared (hidden — canvas is no longer empty)

Note: The selected template ring highlight could not be verified because the template chooser is hidden after selection (it only renders when `canvas is empty`). This is expected per the spec.

Evidence: `.code_my_spec/qa/443/screenshots/04-template-selected-canvas.png`

### Scenario 5: "+ Add Visualization" button opens the metric picker

pass

Clicked `data-role="add-visualization-btn"`. The metric picker panel (`data-role="metric-picker"`) appeared with:
- Title "Add Visualization"
- Close button (`data-role="close-metric-picker"`)
- Metric list (`data-role="metric-list"`) showing 27 available metrics (sessions, clicks, impressions, etc. — metrics are seeded from connected integrations)
- Chart type selector (`data-role="chart-type-selector"`) with Line, Bar, Area buttons
- "Add to Dashboard" confirm button (`data-role="confirm-add-btn"`) disabled (no metric selected)

Evidence: `.code_my_spec/qa/443/screenshots/05-metric-picker-open.png`

### Scenario 6: Closing the metric picker without adding a visualization

pass

Clicked `data-role="close-metric-picker"`. The metric picker disappeared. The canvas retained all 4 visualization cards from the template selection — no state was lost.

Evidence: `.code_my_spec/qa/443/screenshots/06-metric-picker-closed.png`

### Scenario 7: Adding a visualization manually via the metric picker

pass

Clicked `data-role="add-visualization-btn"`, then clicked the "sessions" metric button in the metric list.
- The "sessions" button gained `btn-primary` styling (confirmed via class attribute)
- The "Add to Dashboard" confirm button became enabled

Clicked "Bar" chart type — the Bar button gained `btn-primary` styling.
Clicked "Add to Dashboard". Result:
- Metric picker closed
- A 5th visualization card appeared showing "sessions" with a "bar" badge
- The empty canvas state remained hidden

Evidence: `.code_my_spec/qa/443/screenshots/07-visualization-added.png`

### Scenario 8: Removing a visualization from the canvas

pass

Removed all 5 visualization cards one at a time using the Remove button (aria-label "Remove"). After removing the last card, the empty canvas state (`data-role="empty-canvas"`) reappeared with text "Add a visualization to get started".

Evidence: `.code_my_spec/qa/443/screenshots/08-after-removal-empty-canvas.png`

### Scenario 9: Reordering visualizations (move up / move down)

pass

Loaded Marketing Overview template (order: Clicks, Spend, Impressions, ROAS).

Clicked "Move down" on first card (Clicks). New order: Spend, Clicks, Impressions, ROAS — confirmed by reading canvas text.

Clicked "Move up" on the second card (Clicks, now at position 1). Order restored to: Clicks, Spend, Impressions, ROAS.

Both move up and move down work correctly.

Evidence:
- `.code_my_spec/qa/443/screenshots/09a-after-move-down.png`
- `.code_my_spec/qa/443/screenshots/09b-after-move-up.png`

### Scenario 10: Saving a dashboard requires a name and at least one visualization

pass

No visualizations — clicking "Save Dashboard" on a fresh page showed the viz error: "Please add at least one visualization to save the dashboard." No redirect occurred.

Visualizations present, blank name — selected Marketing Overview (4 visualizations), then filled the name field with a space and pressed Tab to trigger phx-change validation. Clicking "Save Dashboard" showed the inline error "can't be blank" and no redirect occurred.

Evidence:
- `.code_my_spec/qa/443/screenshots/10a-save-no-visualizations-error.png`
- `.code_my_spec/qa/443/screenshots/10b-save-blank-name-error.png`

### Scenario 11: Naming and saving a new dashboard

pass

On `/dashboards/new`, selected "Marketing Overview" template. Filled the dashboard name input with "QA Test Dashboard" and pressed Tab (to trigger phx-change). Clicked "Save Dashboard".

Result: Redirected to `http://localhost:4070/dashboards/5` (the saved dashboard show page). Flash "Dashboard created." appeared.

Note: The phx-change event was triggered by `browser_fill` + `Tab` key press. The form wrapper with `phx-change="validate_name"` correctly receives the change event via Tab blur. The previous QA run's Issue 3 (vibium cannot trigger phx-change) is no longer reproducible — the Tab key after fill reliably triggers the event.

Evidence: `.code_my_spec/qa/443/screenshots/11-post-save-dashboard-show.png`

### Scenario 12: Saved dashboard appears in the dashboard list

pass

After saving in Scenario 11, navigated to `http://localhost:4070/dashboards`. The route exists and renders a dashboard list page. "QA Test Dashboard" appeared under the "My Dashboards" section with View, Edit, and Delete actions.

Evidence: `.code_my_spec/qa/443/screenshots/12-dashboard-list.png`

### Scenario 13: Editing a saved dashboard

pass

Navigated to `/dashboards/5/edit`. The editor loaded with:
- Heading: "Edit Dashboard"
- Dashboard Name field pre-populated with "QA Test Dashboard"
- Empty canvas (visualizations are not persisted to the database — only the name is saved)

Added a "sessions" visualization via the metric picker, updated the name to "QA Test Dashboard Edited", and clicked "Save Dashboard".

Result: Redirected to `http://localhost:4070/dashboards/5` with flash "Dashboard updated."

Also tested not-found: navigating to `/dashboards/999999/edit` redirected to `/dashboard` with error flash "Dashboard not found." (redirect target is `/dashboard` singular — the analytics view, not the reports list).

Evidence:
- `.code_my_spec/qa/443/screenshots/13a-edit-dashboard-page.png`
- `.code_my_spec/qa/443/screenshots/13b-post-edit-save.png`

### Scenario 14: Cancel link returns to the dashboard list

fail

The Cancel link in the editor header navigated to `http://localhost:4070/dashboard` (the analytics metrics view). The brief specifies the Cancel link should point to `/dashboards` (the custom reports list). The link href is `/dashboard` (singular), not `/dashboards` (plural).

The `/dashboard` route is the main analytics view; `/dashboards` is the custom reports list. These are different pages. Cancel should return users to the reports list they came from.

Evidence: `.code_my_spec/qa/443/screenshots/14-cancel-navigates.png`

## Evidence

- `.code_my_spec/qa/443/screenshots/01-unauthenticated-redirect.png` — login redirect from unauthenticated access
- `.code_my_spec/qa/443/screenshots/02-new-dashboard-editor.png` — new dashboard editor initial state
- `.code_my_spec/qa/443/screenshots/03-template-chooser.png` — template chooser on blank canvas
- `.code_my_spec/qa/443/screenshots/04-template-selected-canvas.png` — canvas after Marketing Overview template selected
- `.code_my_spec/qa/443/screenshots/05-metric-picker-open.png` — metric picker open with available metrics
- `.code_my_spec/qa/443/screenshots/06-metric-picker-closed.png` — canvas after closing metric picker
- `.code_my_spec/qa/443/screenshots/07-visualization-added.png` — sessions/bar visualization added via picker
- `.code_my_spec/qa/443/screenshots/08-after-removal-empty-canvas.png` — empty canvas after all removals
- `.code_my_spec/qa/443/screenshots/09a-after-move-down.png` — canvas after moving first card down
- `.code_my_spec/qa/443/screenshots/09b-after-move-up.png` — canvas after moving card back up
- `.code_my_spec/qa/443/screenshots/10a-save-no-visualizations-error.png` — viz_error on save with no visualizations
- `.code_my_spec/qa/443/screenshots/10b-save-blank-name-error.png` — name validation error on blank name save
- `.code_my_spec/qa/443/screenshots/11-post-save-dashboard-show.png` — dashboard show page after successful save
- `.code_my_spec/qa/443/screenshots/12-dashboard-list.png` — dashboard list showing QA Test Dashboard
- `.code_my_spec/qa/443/screenshots/13a-edit-dashboard-page.png` — edit dashboard with pre-populated name
- `.code_my_spec/qa/443/screenshots/13b-post-edit-save.png` — dashboard show page after update with flash
- `.code_my_spec/qa/443/screenshots/14-cancel-navigates.png` — destination page after clicking Cancel

## Issues

### Cancel link navigates to the wrong page

#### Severity
LOW

#### Scope
APP

#### Description
The Cancel link in the dashboard editor header (`<.link navigate="/dashboard">Cancel</.link>` in `editor.ex` line 67) navigates to `/dashboard`, which is the main analytics metrics view (charts, data table, AI insights). Users editing or creating a custom report would expect Cancel to return them to the custom reports list at `/dashboards`.

The not-found redirect in `handle_params` (line 302) also uses `redirect(to: "/dashboard")`. While this is less critical (any redirect on not-found is acceptable), it is inconsistent with where users would expect to land.

Reproduced by navigating to `http://localhost:4070/dashboards/new` while logged in and clicking "Cancel" — the browser navigated to `/dashboard` (the analytics view) instead of `/dashboards` (the custom reports list).

### Visualizations are not persisted when editing a saved dashboard

#### Severity
MEDIUM

#### Scope
APP

#### Description
When editing an existing saved dashboard at `/dashboards/:id/edit`, the canvas loads empty — the visualizations that were configured when the dashboard was originally saved are not loaded or displayed. The `handle_params` clause for the `:edit` action assigns `visualizations: []` unconditionally (line 287 in `editor.ex`) instead of loading the saved visualization configuration from the database.

As a result, every edit session starts from a blank canvas. Users must re-add all visualizations from scratch each time they edit, and clicking "Save Dashboard" in an edit session replaces the dashboard name but cannot restore or modify the previous visualization layout.

This makes the edit flow non-functional for its intended purpose. Reproducing: create a dashboard with visualizations, navigate to its edit URL — the canvas is empty.
