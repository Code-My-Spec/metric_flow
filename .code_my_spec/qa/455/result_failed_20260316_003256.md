# QA Result

## Status

fail

## Scenarios

### Scenario 1 — Owner sees the Danger Zone delete section (criterion 4167, 4175)

**Result: pass**

Logged in as `qa@example.com` and navigated to `http://localhost:4070/accounts/settings`. The "Delete Account" section with `data-role="delete-account"` is visible. The heading "Delete Account" is rendered with class `text-error` (red). The "Transfer Ownership" section with `data-role="transfer-ownership"` is also visible. The delete form contains both inputs: "Type the account name to confirm" (`input[name="account_name_confirmation"]`) and "Your password" (`input[name="password"]`), plus a red "Delete Account" button.

Evidence: `.code_my_spec/qa/455/screenshots/01-settings-full-page.png`, `.code_my_spec/qa/455/screenshots/01-scenario1-danger-zone.png`

### Scenario 2 — Member (non-owner) does NOT see the delete section (criterion 4167, 4175)

**Result: pass**

`qa-member@example.com` was already a member of "QA Test Account" with `read_only` role. Role was changed to `admin` via the Members page role selector. Logged in as `qa-member@example.com` and navigated to `http://localhost:4070/accounts/settings`. The `[data-role="delete-account"]` element is not present in the DOM (confirmed via `browser_is_visible` returning false). The `[data-role="transfer-ownership"]` form is also absent. The page shows General Settings (editable for admins), Leave Account section, and Agency sections — but no Danger Zone. Expected behavior confirmed.

Evidence: `.code_my_spec/qa/455/screenshots/02-member-role-changed-to-admin.png`, `.code_my_spec/qa/455/screenshots/02-scenario2-member-settings.png`

### Scenario 3 — Warning text is present before any interaction (criterion 4171)

**Result: pass**

The delete form at `[data-role="delete-account"] p` renders the exact text: "This action is permanent and cannot be undone. This deletion is irreversible — all account data, members, and integrations will be deleted." The text contains both "permanent" and "irreversible" as required by the acceptance criterion.

Evidence: `.code_my_spec/qa/455/screenshots/03-scenario3-warning-text.png`

### Scenario 4 — Delete rejected when account name does not match (criterion 4169)

**Result: pass**

Filled `input[name="account_name_confirmation"]` with "Wrong Name" and `input[name="password"]` with "hello world!". Clicked the "Delete Account" button. Flash error "Account name does not match" appeared. URL remained at `http://localhost:4070/accounts/settings`. Deletion did not proceed.

Evidence: `.code_my_spec/qa/455/screenshots/04-scenario4-wrong-name-flash.png`

### Scenario 5 — Delete rejected when password is incorrect (criterion 4170)

**Result: pass**

Filled `input[name="account_name_confirmation"]` with "QA Test Account" and `input[name="password"]` with "WrongPassword123!". Clicked "Delete Account". Flash error "Incorrect password" appeared. URL remained at `http://localhost:4070/accounts/settings`. Deletion did not proceed.

Evidence: `.code_my_spec/qa/455/screenshots/05-scenario5-wrong-password-flash.png`

### Scenario 6 — Delete rejected when password is empty (criterion 4170)

**Result: pass**

Filled `input[name="account_name_confirmation"]` with "QA Test Account" and left the password field empty. Clicked "Delete Account". Flash error "Password is required" appeared. URL remained at `http://localhost:4070/accounts/settings`. Deletion did not proceed.

Evidence: `.code_my_spec/qa/455/screenshots/06-scenario6-empty-password-flash.png`

### Scenario 7 — Successful account deletion redirects to accounts list with confirmation message (criterion 4169, 4170, 4172, 4174)

**Result: partial pass — deletion succeeds with redirect and flash, but confirmation email is NOT sent**

Filled `input[name="account_name_confirmation"]` with "QA Test Account" (exact match) and `input[name="password"]` with "hello world!". Clicked "Delete Account". The page redirected to `/accounts` with the info flash: "Account deleted. A confirmation email has been sent." The accounts list showed "No accounts found." — "QA Test Account" is gone.

However, navigating to `http://localhost:4070/dev/mailbox` showed 0 messages. No confirmation email was delivered to `qa@example.com`. The flash message promises an email that is never sent — this is a bug (see Issues).

Evidence: `.code_my_spec/qa/455/screenshots/07-scenario7-before-deletion.png`, `.code_my_spec/qa/455/screenshots/07-scenario7-after-deletion.png`, `.code_my_spec/qa/455/screenshots/07-scenario7-dev-mailbox.png`

### Scenario 8 — Member cannot access deleted account (criterion 4172, 4173)

**Result: pass**

After deletion, logged in as `qa-member@example.com` and navigated to `/accounts`. The accounts list shows 4 accounts from other test data (Client Alpha, Client Account Manager, Client Read Only, Client Beta) but "QA Test Account" does not appear. All access grants were revoked when the account was deleted.

Evidence: `.code_my_spec/qa/455/screenshots/08-scenario8-member-accounts-after-deletion.png`

### Scenario 9 — Owner cannot access settings of deleted account (criterion 4172)

**Result: pass**

After deletion, logged back in as `qa@example.com` and navigated to `http://localhost:4070/accounts/settings`. The server redirected to `/accounts` (the `mount` function found no accounts and redirected). The accounts page shows "No accounts found." The deleted account is no longer accessible.

Evidence: `.code_my_spec/qa/455/screenshots/09-scenario9-owner-settings-after-deletion.png`

## Evidence

- `.code_my_spec/qa/455/screenshots/00-accounts-list.png` — Accounts list before testing; QA Test Account visible and active
- `.code_my_spec/qa/455/screenshots/01-settings-full-page.png` — Full settings page as owner showing all sections
- `.code_my_spec/qa/455/screenshots/01-scenario1-danger-zone.png` — Danger Zone delete section visible for owner
- `.code_my_spec/qa/455/screenshots/02-member-role-changed-to-admin.png` — Members page after changing qa-member@example.com to admin
- `.code_my_spec/qa/455/screenshots/02-scenario2-member-settings.png` — Settings page as admin member — no delete or transfer ownership sections
- `.code_my_spec/qa/455/screenshots/03-scenario3-warning-text.png` — Warning text in delete form visible before interaction
- `.code_my_spec/qa/455/screenshots/04-scenario4-wrong-name-flash.png` — Flash error "Account name does not match"
- `.code_my_spec/qa/455/screenshots/05-scenario5-wrong-password-flash.png` — Flash error "Incorrect password"
- `.code_my_spec/qa/455/screenshots/06-scenario6-empty-password-flash.png` — Flash error "Password is required"
- `.code_my_spec/qa/455/screenshots/07-scenario7-before-deletion.png` — Full settings page before deletion
- `.code_my_spec/qa/455/screenshots/07-scenario7-after-deletion.png` — Accounts list after deletion with flash confirmation
- `.code_my_spec/qa/455/screenshots/07-scenario7-dev-mailbox.png` — Dev mailbox showing 0 messages (no email sent)
- `.code_my_spec/qa/455/screenshots/08-scenario8-member-accounts-after-deletion.png` — Member's accounts list; QA Test Account absent
- `.code_my_spec/qa/455/screenshots/09-scenario9-owner-settings-after-deletion.png` — Owner redirected to /accounts after trying to access settings

## Issues

### Deletion confirmation email is never sent despite flash message claiming it was

#### Severity
MEDIUM

#### Scope
APP

#### Description
When the owner successfully deletes a team account, the flash message reads: "Account deleted. A confirmation email has been sent." However, no email is sent. The dev mailbox at `/dev/mailbox` showed 0 messages after account deletion.

Code review confirms the issue: `Accounts.delete_account/2` (delegated to `AccountRepository.delete_account/2`) does not invoke any mailer. It only broadcasts a PubSub event `{:deleted, account}`. The flash message in `do_delete_account/3` in `lib/metric_flow_web/live/account_live/settings.ex` (line 599) claims an email is sent, but no email delivery code exists in the deletion path.

Reproduced at: `http://localhost:4070/accounts/settings`, deleting "QA Test Account" with credentials `qa@example.com` / `hello world!`. Check `/dev/mailbox` immediately after — 0 messages.

Either the flash message should be corrected to remove the email claim, or a confirmation email should be implemented and sent to the account owner on deletion.
