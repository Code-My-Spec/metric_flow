# QA Result

Story 520: Fetch and Select Google Business Profile Locations Across Multiple Accounts

## Status

fail

## Scenarios

### Scenario 1: Route and page access — /integrations/connect/google_business/accounts

**Result: PASS**

Navigating to `http://localhost:4070/integrations/connect/google_business/accounts` while logged in as `qa@example.com` rendered the accounts selection page correctly. The page did NOT redirect away. The h2 heading showed "Google Business — Select Accounts". No "Re-authenticate", "re-authenticate", "Re-connect", or "re-connect" text was present. The page uses a manual entry form since the live GBP API is not available with the test token.

Evidence: `.code_my_spec/qa/520/screenshots/01-gbp-accounts-page.png`

### Scenario 2: Location list UI — single flat list, no account tabs

**Result: PASS (with note)**

No `[data-role='account-tab']` or `[data-role='account-switcher']` elements were present — confirmed via `browser_is_visible`. The page rendered `[data-role='manual-entry']` and `[data-role='manual-property-input']` for manual location ID entry. A `[data-role='account-list']` would only render when `@accounts != []`, which requires a real GBP API response. The manual entry form is the correct fallback when the API cannot be called with a test token.

Evidence: `.code_my_spec/qa/520/screenshots/02-gbp-location-list.png`

### Scenario 3: Location row fields — account name, title, address, store code

**Result: PARTIAL (API not available in QA)**

The page rendered the manual entry form only — no location rows from the API. Inspection of the rendered HTML confirmed no `[data-role='location-title']`, `[data-role='location-address']`, `[data-role='location-store-code']`, or `[data-role='location-account-name']` attributes in the output. The source template uses generic field bindings (`property.name`, `property.account`, `property.id`) in `data-role="account-option"` items (connect.ex lines 259–279), but these are generic account fields, not GBP-specific location fields with the required data-role attributes.

The acceptance criteria (criteria 4855–4856) specify distinct `data-role` attributes for location fields — these are not present in the current template.

Evidence: `.code_my_spec/qa/520/screenshots/03-gbp-location-row-fields.png`

### Scenario 4: Save selection and success confirmation

**Result: PASS**

Filled `[data-role='manual-property-input']` with `accounts/123/locations/loc-001` and clicked `[data-role='save-selection']`. The form submitted and navigated to `/integrations/connect/google_business` with the flash message "Account selection saved successfully." The detail page confirmed the saved value: "Location ID: accounts/123/locations/loc-001". Navigating back to the accounts page showed the manual input pre-filled with `accounts/123/locations/loc-001`.

Evidence: `.code_my_spec/qa/520/screenshots/04-gbp-save-selection.png`, `.code_my_spec/qa/520/screenshots/05-gbp-save-confirmed.png`

### Scenario 5: Update location selection without re-authenticating

**Result: PASS**

After Scenario 4, navigated back to `http://localhost:4070/integrations/connect/google_business/accounts`. The URL contained no `accounts.google.com` or `oauth` segment — no OAuth redirect occurred. The manual input was pre-filled with `accounts/123/locations/loc-001`. Filled a new value `accounts/123/locations/loc-002` and saved. The detail page confirmed "Location ID: accounts/123/locations/loc-002" with a "Account selection saved successfully." flash. No re-authentication was required.

Evidence: `.code_my_spec/qa/520/screenshots/06-gbp-update-selection.png`

### Scenario 6: Missing/deleted location flagging (criterion 4862)

**Result: FAIL**

Updated the integration's `provider_metadata` to set `included_locations` to `["accounts/123/locations/deleted-loc-1"]` via a direct DB update using `mix run --no-start`. Navigated to the accounts page. The page pre-filled the manual input with `accounts/123/locations/deleted-loc-1` but showed no warning, flag, or UI indicator that the configured location is missing or unavailable. No `[data-role='missing-location']`, `[data-role='location-warning']`, `[data-role='location-unavailable']` elements were found. No text containing "missing", "unavailable", "removed", or "revoked" was present.

This feature is not implemented. Missing-location flagging requires the API to be called (to compare configured locations against fetched ones), which in turn requires the full `fetch_provider_accounts` / `list_google_business_locations` integration to return results. With a test token the API returns no locations, so there is nothing to compare against.

Additionally, the `save_account_selection` handler saves a single string to `included_locations`, but `included_locations` per the acceptance criteria should be an array. When the metadata is set to a JSON array by other means and loaded back, the form value attribute receives the array value. This data type mismatch (string vs array) is a latent bug.

Evidence: `.code_my_spec/qa/520/screenshots/07-gbp-missing-location-flag.png`

### Scenario 7: Unauthenticated access redirect

**Result: PASS**

Launched a new browser session (no cookies). Navigated to `http://localhost:4070/integrations/connect/google_business/accounts`. The URL changed to `http://localhost:4070/users/log-in` — correctly redirected to the login page. The location selection UI was not shown.

Evidence: `.code_my_spec/qa/520/screenshots/08-gbp-unauthenticated-redirect.png`

### Scenario 8: Integrations page shows google_business integration

**Result: PASS**

Navigated to `http://localhost:4070/integrations`. The page listed 6 platforms including "Google Business" with description "Business profile locations and reviews", status "Connected", and controls "Sync Now", "Edit Accounts", "Manage", "Disconnect". The `@data_platforms` list in `lib/metric_flow_web/live/integration_live/index.ex` includes `:google_business` (confirmed at line 22).

Evidence: `.code_my_spec/qa/520/screenshots/09-gbp-integrations-list.png`

### Scenario 9: Connect grid includes google_business provider

**Result: PASS**

Navigated to `http://localhost:4070/integrations/connect`. The platform grid showed all 6 providers including "Google Business" with description "Business profile locations and reviews" and a "Connected" badge with "Reconnect" button. The provider is in `@canonical_providers` and `@provider_metadata` in `connect.ex`, so it appears in the grid.

Evidence: `.code_my_spec/qa/520/screenshots/10-gbp-connect-grid.png`

## Evidence

- `.code_my_spec/qa/520/screenshots/01-gbp-accounts-page.png` — accounts page renders at `/integrations/connect/google_business/accounts`
- `.code_my_spec/qa/520/screenshots/02-gbp-location-list.png` — manual entry form shown (no account tabs or switchers)
- `.code_my_spec/qa/520/screenshots/03-gbp-location-row-fields.png` — page with manual entry only (no fetched location rows)
- `.code_my_spec/qa/520/screenshots/04-gbp-save-selection.png` — manual input filled with test location ID before save
- `.code_my_spec/qa/520/screenshots/05-gbp-save-confirmed.png` — detail page after save showing "Account selection saved successfully." and the saved location ID
- `.code_my_spec/qa/520/screenshots/06-gbp-update-selection.png` — accounts page with updated location ID (no re-auth required)
- `.code_my_spec/qa/520/screenshots/07-gbp-missing-location-flag.png` — accounts page with deleted-loc-1 pre-filled, no warning shown
- `.code_my_spec/qa/520/screenshots/08-gbp-unauthenticated-redirect.png` — login page shown after unauthenticated access attempt
- `.code_my_spec/qa/520/screenshots/09-gbp-integrations-list.png` — integrations index showing 6 platforms including Google Business
- `.code_my_spec/qa/520/screenshots/10-gbp-connect-grid.png` — connect grid showing Google Business with Connected badge

## Issues

### Missing-location flagging not implemented (criterion 4862)

#### Severity
HIGH

#### Description
When a previously configured location ID is no longer returned by the GBP API (e.g., a deleted or revoked location), the accounts page shows no warning, flag, or indicator. The manual entry field is pre-filled with the old location ID with no UI feedback that it may be invalid. The acceptance criterion requires the system to flag missing/unavailable locations rather than silently pre-filling the stale ID.

Reproduced by setting `included_locations` to `["accounts/123/locations/deleted-loc-1"]` in the integration's `provider_metadata`, then navigating to `/integrations/connect/google_business/accounts`. The field shows the stale ID with no warning.

This requires the GBP API fetch to return results in order to compare configured locations against live ones. The feature is blocked until `list_google_business_locations` returns data.

### Location row data-role attributes missing from template (criteria 4855–4856)

#### Severity
MEDIUM

#### Description
The `render_account_selection/1` template in `connect.ex` uses generic field bindings (`property.name`, `property.account`, `property.id`) in `data-role="account-option"` items. The acceptance criteria specify distinct `data-role` attributes for GBP-specific location fields: `data-role="location-title"`, `data-role="location-address"`, `data-role="location-store-code"`, and `data-role="location-account-name"`. These attributes are absent from the current template, meaning automated tests based on the acceptance criteria selectors will fail when location data is available.

### save_account_selection saves included_locations as a string instead of an array

#### Severity
MEDIUM

#### Description
The `save_account_selection` handler in `connect.ex` saves the manual entry text as a single string to the `included_locations` key (via `metadata_key_for_provider(:google_business)` returning `"included_locations"`). The acceptance criteria describe `included_locations` as an array of location IDs to support multi-location selection. When the metadata is externally set to a JSON array and then loaded by the LiveView, `get_in(integration.provider_metadata, ["included_locations"])` returns the array, which is passed to `@manual_property_id`. The form's `value` attribute then receives a list. After saving via the UI, the array is overwritten with a single string.

For multi-location selection (described in the acceptance criteria), the save handler must collect and store an array of IDs rather than a single string.

### OAuth callback URL for google_business not registered in Google Cloud Console

#### Severity
LOW

#### Scope
DOCS

#### Description
Clicking "Reconnect" for Google Business on the connect grid redirects to Google OAuth but fails with `redirect_uri_mismatch` — the callback URL `http://localhost:4070/integrations/oauth/callback/google_business` is not registered as an authorized redirect URI in the Google Cloud Console project. This is expected for localhost testing. The production/UAT environment needs this URI added to the OAuth client configuration before the Google Business OAuth flow can complete end-to-end.
