# QA Result

## Status

fail

## Scenarios

### Scenario 1: No-data empty state

**Result: FAIL**

Logged in as `qa-empty@example.com` (personal account only, no team membership) and navigated to `http://localhost:4070/correlations`. The page crashes with an `ArgumentError` instead of showing the no-data empty state. The crash occurred in the same location as the previous run — `CorrelationsRepository.list_correlation_jobs/1` — which does not guard against a nil `account_id`.

The prior fix applied to `get_latest_completed_job` (lines 103–105 in the repository) correctly handles nil account_id, but `list_correlation_jobs` at line 64–70 directly passes nil to `where(account_id: ^nil)` without a guard, causing Ecto to raise `ArgumentError`. The `mount/3` rescue block only catches `Ecto.NoResultsError`, so the crash propagates to a 500 error page.

Evidence: `.code_my_spec/qa/447/screenshots/no-data-state-crash.png`

### Scenario 2: Page header and mode toggle present

**Result: PASS**

As `qa@example.com` at `/correlations`:
- `h1` reads "Correlations"
- Subtitle reads "Which metrics drive your goal?"
- `[data-role="mode-toggle"]` is present
- `[data-role="mode-raw"]` has class `btn btn-primary btn-sm` (active by default)
- `[data-role="mode-smart"]` button exists
- `[data-role="run-correlations"]` button exists and is enabled

Evidence: `.code_my_spec/qa/447/screenshots/page-header.png`

### Scenario 3: Raw mode — correlation results table displayed

**Result: PARTIAL**

As `qa@example.com` at `/correlations`:
- `[data-role="raw-mode"]` is visible
- `[data-role="correlation-results"]` wrapper is present
- `[data-role="correlation-summary"]` is shown
- `[data-role="goal-metric"]` shows "Goal: revenue"
- `[data-role="last-calculated"]` shows "Last calculated 1 hours ago"
- `[data-role="data-window"]` shows "Data window: 2026-03-18 to 2026-03-18"
- `[data-role="data-points"]` shows "0 data points" — the most recent completed job was a real run that completed with 0 data points. The seeded job with 90 data points is older and not selected as the latest.

The `get_latest_correlation_summary` function correctly filters results by the latest job's ID, but because a newer real job (with no results) completed after the seeds were run, the seeded data is not shown. The summary bar and raw-mode section render correctly, but the expected "90 data points" is not visible.

Evidence: `.code_my_spec/qa/447/screenshots/raw-mode-summary.png`

### Scenario 4: Results table — metric entries with coefficient and optimal lag

**Result: FAIL**

The results table is empty ("No correlations match the selected filter."). The five seeded correlation rows (`clicks`, `spend`, `impressions`, `income`, `sessions`) are linked to the older seeded job. The latest completed job has 0 results. Since `get_latest_correlation_summary` now correctly scopes results to the latest job, no results are shown.

Specific checks could not be performed:
- No `[data-metric="clicks"]` row visible
- No strength badges, lag cells, or platform badges visible
- The table renders correctly with all column headers

Evidence: `.code_my_spec/qa/447/screenshots/results-table.png`

### Scenario 5: Correlation results include all metric types without segregation

**Result: PASS**

- `[data-role="marketing-correlations"]` does not exist (confirmed not visible)
- `[data-role="financial-correlations"]` does not exist (confirmed not visible)
- No segregated sections are rendered — the single unified table is used

Evidence: `.code_my_spec/qa/447/screenshots/unified-results.png`

### Scenario 6: Sorting the results table

**Result: PASS**

- Clicking `[data-sort-col="metric_name"]` sets `data-sort-active="true"` on the Metric header and shows ↓ arrow
- Clicking again toggles direction to ↑
- Clicking `[data-sort-col="lag"]` sets `data-sort-active="true"` on the Lag header

Sort logic is functional even with an empty result set.

Evidence: `.code_my_spec/qa/447/screenshots/sorted-by-metric.png`, `.code_my_spec/qa/447/screenshots/sorted-by-lag.png`

### Scenario 7: Platform filter

**Result: PARTIAL**

- `[data-role="platform-filter"]` is present
- "All Platforms" button has `btn-primary` class (active by default)
- No provider-specific filter buttons are rendered because the latest job has no results (no distinct providers to derive filter options from)
- The filter UI renders correctly but cannot be fully exercised without result rows

Evidence: `.code_my_spec/qa/447/screenshots/filtered-google-ads.png`

### Scenario 8: Run Now button — insufficient data response

**Result: BLOCKED**

Could not be tested. Navigating to `/correlations` as `qa-empty@example.com` crashes the application (see Scenario 1). The 500 error page prevents any user interaction.

### Scenario 9: Smart mode — opt-in card

**Result: PASS**

As `qa@example.com`, clicking `[data-role="mode-smart"]`:
- `[data-role="smart-mode"]` section is visible
- "Smart Mode" H2 heading is present
- Explanatory paragraph shown before enabling AI suggestions
- `[data-role="enable-ai-suggestions"]` button is present

Evidence: `.code_my_spec/qa/447/screenshots/smart-mode-optin.png`

### Scenario 10: Smart mode — enable AI suggestions and submit feedback

**Result: PASS**

- Clicking `[data-role="enable-ai-suggestions"]` shows `[data-role="ai-suggestions-enabled"]` badge with text "AI Suggestions enabled"
- `[data-role="ai-recommendations"]` block is visible with "AI Recommendations" H3 heading
- `[data-role="ai-feedback-section"]` is present
- `[data-role="feedback-helpful"]` and `[data-role="feedback-not-helpful"]` buttons are shown
- Clicking `[data-role="feedback-helpful"]` shows `[data-role="feedback-confirmation"]` with checkmark badge and "Thanks for your feedback" message
- Feedback buttons are hidden after submission

Evidence: `.code_my_spec/qa/447/screenshots/smart-mode-enabled.png`, `.code_my_spec/qa/447/screenshots/smart-mode-feedback-submitted.png`

### Scenario 11: Job running banner (visual state)

**Result: PASS**

- `[data-role="run-correlations"]` button exists and is enabled (not disabled) when no job is running
- No job-running banner shown (no active job in :pending or :running state)
- Button is interactive and not disabled

Evidence: `.code_my_spec/qa/447/screenshots/run-correlations-button.png`

## Evidence

- `.code_my_spec/qa/447/screenshots/no-data-state-crash.png` — 500 error when qa-empty@example.com accesses /correlations (Scenario 1 failure)
- `.code_my_spec/qa/447/screenshots/page-header.png` — Correlations page header with mode toggle, Configure Goals link, and Run Now button
- `.code_my_spec/qa/447/screenshots/raw-mode-summary.png` — Raw mode summary bar (goal metric, last calculated, data window, 0 data points)
- `.code_my_spec/qa/447/screenshots/results-table.png` — Results table showing empty state (no results from latest job)
- `.code_my_spec/qa/447/screenshots/unified-results.png` — Unified table layout (no segregated marketing/financial sections)
- `.code_my_spec/qa/447/screenshots/sorted-by-metric.png` — Table sorted by metric name (descending, sort-active confirmed)
- `.code_my_spec/qa/447/screenshots/sorted-by-lag.png` — Table sorted by lag column (sort-active confirmed)
- `.code_my_spec/qa/447/screenshots/filtered-google-ads.png` — Platform filter showing only "All Platforms" (no provider buttons without results)
- `.code_my_spec/qa/447/screenshots/smart-mode-optin.png` — Smart mode opt-in card before enabling AI suggestions
- `.code_my_spec/qa/447/screenshots/smart-mode-enabled.png` — Smart mode with AI suggestions enabled and recommendations shown
- `.code_my_spec/qa/447/screenshots/smart-mode-feedback-submitted.png` — Feedback confirmation after clicking Helpful
- `.code_my_spec/qa/447/screenshots/run-correlations-button.png` — Run Now button enabled (no active job)

## Issues

### Correlations page crashes for users with no team account (list_correlation_jobs nil guard missing)

#### Severity
HIGH

#### Description
When a user with no team account (personal account only) navigates to `/correlations`, the page renders a 500 error instead of the expected no-data empty state. This issue persists from the previous QA run — the partial fix applied to `get_latest_completed_job` did not extend to `list_correlation_jobs`.

**Reproduction steps:**
1. Log in as `qa-empty@example.com` (personal account only, no team membership)
2. Navigate to `http://localhost:4070/correlations`

**Error:** `ArgumentError: nil given for account_id. comparison with nil is forbidden`

**Stack trace location:** `lib/metric_flow/correlations/correlations_repository.ex:68` in `list_correlation_jobs/1`

**Root cause:** `list_correlation_jobs/1` (lines 63–70) calls `get_account_id(scope)` which returns nil for a personal-only user, then directly passes nil to `where(account_id: ^account_id)`. Unlike `get_latest_completed_job` (which was fixed to check for nil), `list_correlation_jobs` has no nil guard.

**Fix required:** Add the same nil guard pattern used in `get_latest_completed_job`:
```elixir
def list_correlation_jobs(%Scope{} = scope) do
  case get_account_id(scope) do
    nil -> []
    account_id ->
      CorrelationJob
      |> where(account_id: ^account_id)
      |> order_by(desc: :inserted_at)
      |> Repo.all()
  end
end
```

**Affected file:** `lib/metric_flow/correlations/correlations_repository.ex:63–70`

### QA seed 447 is not idempotent when real jobs run after seeding

#### Severity
MEDIUM

#### Scope
QA

#### Description
The `priv/repo/qa_seeds_447.exs` seed script creates a completed `CorrelationJob` if none exists. Once a real correlation worker run creates a newer completed job (with 0 results for the test account), the seed script reuses the older seeded job on subsequent runs. The `get_latest_correlation_summary` function returns the newest completed job, which is now the real empty job, hiding the seeded results.

**Observed behavior:** `[data-role="data-points"]` shows "0 data points" and no correlation rows appear in the results table — the five seeded results (clicks, spend, impressions, income, sessions) are not displayed.

**Root cause:** The seed idempotency check at line 60–66 returns the first existing completed job (ordered by `inserted_at desc`). After a real run adds a newer completed job, the seed's existing-job check returns the old seeded job (still the same branch), but the LiveView picks up the newer real job as the latest.

**Fix:** The seed script should always insert a new job with `completed_at` set to `DateTime.utc_now()` (ensuring it is the most recent) and delete or supersede any older seeded jobs. Alternatively, use a sentinel marker (e.g., a specific `goal_metric_name` value like `"qa_test_revenue"`) so the seeds always reset to a known state rather than reusing any existing completed job.

**Affected file:** `priv/repo/qa_seeds_447.exs:60–96`
