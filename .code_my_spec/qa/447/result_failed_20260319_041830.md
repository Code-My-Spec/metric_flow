# QA Result

## Status

fail

## Scenarios

### Scenario 1: No-data empty state

**Result: FAIL**

Logged in as `qa-empty@example.com` (personal account only, no team membership) and navigated to `http://localhost:4070/correlations`. The page crashed with an `ArgumentError` instead of showing the no-data empty state.

The crash occurs in `CorrelationsRepository.get_latest_completed_job/1`, which calls `get_account_id/1` → `MetricFlow.Accounts.get_personal_account_id(scope)`. This returns `nil` for a user with no accounts, and Ecto panics when `nil` is passed to `where(account_id: ^nil)`.

The `rescue Ecto.NoResultsError` block in `CorrelationLive.Index.mount/3` does not catch `ArgumentError`, so the crash propagates to a 500 error page.

Evidence: `.code_my_spec/qa/447/screenshots/no-data-state-crash.png`

### Scenario 2: Page header and mode toggle present

**Result: PASS**

As `qa@example.com` at `/correlations`:
- `h1` reads "Correlations"
- Subtitle reads "Which metrics drive your goal?"
- `[data-role="mode-toggle"]` is present
- `[data-role="mode-raw"]` has class `btn btn-primary btn-sm` (active by default)
- `[data-role="mode-smart"]` button exists
- `[data-role="run-correlations"]` button exists and is enabled

Evidence: `.code_my_spec/qa/447/screenshots/page-header.png`

### Scenario 3: Raw mode — correlation results table displayed

**Result: PASS (with observation)**

As `qa@example.com` at `/correlations`:
- `[data-role="raw-mode"]` is visible
- `[data-role="correlation-results"]` wrapper is present
- `[data-role="correlation-summary"]` is shown
- `[data-role="goal-metric"]` shows "Goal: revenue"
- `[data-role="last-calculated"]` shows "Last calculated 49 minutes ago"
- `[data-role="data-window"]` shows "Data window: 2026-03-18 to 2026-03-18"
- `[data-role="data-points"]` shows "0 data points" — the most recent completed job (from a real run ~49 min ago) completed with 0 data points, not 90. The seeded job with 90 data points is older and not the latest.

Evidence: `.code_my_spec/qa/447/screenshots/raw-mode-summary.png`

### Scenario 4: Results table — metric entries with coefficient and optimal lag

**Result: PASS**

As `qa@example.com`:
- `[data-role="results-table"]` is present
- `[data-metric="clicks"]` row for Google Ads shows coefficient `0.82`, strength badge "Strong", lag "7 days", platform "Google Ads"
- `[data-metric="income"]` row for QuickBooks shows coefficient `-0.38` with class `text-error`, lag "Same day" (optimal_lag == 0), strength badge "Weak", platform "QuickBooks"
- All seeded rows are present in the table

Evidence: `.code_my_spec/qa/447/screenshots/results-table.png`

### Scenario 5: Unified results — no segregated sections

**Result: PASS**

- `[data-role="marketing-correlations"]` does not exist (confirmed nil)
- `[data-role="financial-correlations"]` does not exist (confirmed nil)
- Financial metrics (income/QuickBooks) and marketing metrics (clicks/Google Ads) appear in the same unified table

Evidence: `.code_my_spec/qa/447/screenshots/unified-results.png`

### Scenario 6: Sorting the results table

**Result: PASS**

- Clicking `[data-sort-col="metric_name"]` sets `data-sort-active="true"` on the Metric header and shows ↓ arrow
- Clicking again toggles direction to ↑
- Clicking `[data-sort-col="lag"]` sets `data-sort-active="true"` on the Lag header

Evidence: `.code_my_spec/qa/447/screenshots/sorted-by-metric.png`, `.code_my_spec/qa/447/screenshots/sorted-by-lag.png`

### Scenario 7: Platform filter

**Result: PASS**

- `[data-role="platform-filter"]` is present with buttons: All Platforms, Derived, Facebook Ads, Google Ads, Google Analytics, QuickBooks
- "All Platforms" button has `btn-primary` class (active by default)
- Clicking "Google Ads" shows only 2 rows: clicks and spend (both Google Ads)
- Clicking "All Platforms" restores all 27 rows

Evidence: `.code_my_spec/qa/447/screenshots/filtered-google-ads.png`

### Scenario 8: Run Now button — insufficient data response

**Result: NOT TESTED**

Could not be tested because `qa-empty@example.com` navigating to `/correlations` crashes the application (see Scenario 1). The error page prevents any user interaction with the page.

### Scenario 9: Smart mode — opt-in card

**Result: PASS**

As `qa@example.com`, clicking `[data-role="mode-smart"]`:
- `[data-role="smart-mode"]` section becomes visible
- "Smart Mode" heading is present
- Opt-in card text shown before enabling AI suggestions
- `[data-role="enable-ai-suggestions"]` button is present

Evidence: `.code_my_spec/qa/447/screenshots/smart-mode-optin.png`

### Scenario 10: Smart mode — enable AI suggestions and submit feedback

**Result: PASS**

- Clicking `[data-role="enable-ai-suggestions"]` shows `[data-role="ai-suggestions-enabled"]` badge with "AI Suggestions enabled"
- `[data-role="ai-recommendations"]` block is visible with "AI Recommendations" heading
- `[data-role="ai-feedback-section"]` is present
- `[data-role="feedback-helpful"]` and `[data-role="feedback-not-helpful"]` buttons are shown
- Clicking `[data-role="feedback-helpful"]` shows `[data-role="feedback-confirmation"]` with checkmark badge
- Feedback buttons disappear after submission

Evidence: `.code_my_spec/qa/447/screenshots/smart-mode-enabled.png`, `.code_my_spec/qa/447/screenshots/smart-mode-feedback-submitted.png`

### Scenario 11: Job running banner (visual state)

**Result: PASS**

- `[data-role="run-correlations"]` button exists and is enabled (not disabled) when no job is running
- No spinner or job-running banner shown (no active job)

Evidence: `.code_my_spec/qa/447/screenshots/run-correlations-button.png`

## Evidence

- `.code_my_spec/qa/447/screenshots/page-header.png` — Correlations page header with mode toggle and Run Now button
- `.code_my_spec/qa/447/screenshots/raw-mode-summary.png` — Raw mode summary bar showing goal metric, last calculated, data window
- `.code_my_spec/qa/447/screenshots/results-table.png` — Full results table with all correlation rows
- `.code_my_spec/qa/447/screenshots/unified-results.png` — Unified table with mixed financial and marketing metrics
- `.code_my_spec/qa/447/screenshots/sorted-by-metric.png` — Table sorted by metric name (descending)
- `.code_my_spec/qa/447/screenshots/sorted-by-lag.png` — Table sorted by lag
- `.code_my_spec/qa/447/screenshots/filtered-google-ads.png` — Table filtered to Google Ads only (2 rows)
- `.code_my_spec/qa/447/screenshots/smart-mode-optin.png` — Smart mode opt-in card before enabling AI suggestions
- `.code_my_spec/qa/447/screenshots/smart-mode-enabled.png` — Smart mode with AI suggestions enabled and recommendations shown
- `.code_my_spec/qa/447/screenshots/smart-mode-feedback-submitted.png` — Feedback confirmation after clicking Helpful
- `.code_my_spec/qa/447/screenshots/run-correlations-button.png` — Run Now button enabled (no active job)
- `.code_my_spec/qa/447/screenshots/no-data-state-crash.png` — 500 error when qa-empty@example.com accesses /correlations
- `.code_my_spec/qa/447/screenshots/duplicate-results-bug.png` — Full page showing duplicate metric rows from multiple jobs

## Issues

### Correlations page crashes for users with no team account

#### Severity
HIGH

#### Description
When a user with no team account (personal account only) navigates to `/correlations`, the page renders a 500 error instead of the expected no-data empty state.

**Reproduction steps:**
1. Log in as `qa-empty@example.com` (personal account only, no team membership)
2. Navigate to `http://localhost:4070/correlations`

**Error:** `ArgumentError: nil given for account_id. comparison with nil is forbidden`

**Root cause:** `CorrelationsRepository.get_latest_completed_job/1` calls `get_account_id/1` → `MetricFlow.Accounts.get_personal_account_id(scope)`. For a user with no team account, this returns `nil`. Ecto panics when `nil` is passed to `where(account_id: ^nil)`. The `mount/3` function only rescues `Ecto.NoResultsError`, not `ArgumentError`.

The fix should either: (a) check for `nil` account_id in the repository before querying, returning `nil` immediately; or (b) handle the nil case in `get_latest_correlation_summary/1` before calling the repository; or (c) broaden the rescue in `mount/3`.

**Affected file:** `lib/metric_flow/correlations/correlations_repository.ex:99-105`

### Correlation results table shows results from all jobs, not only the latest

#### Severity
MEDIUM

#### Description
The `/correlations` page displays correlation results from ALL completed jobs for the account, not just the most recent one. This causes duplicate metric entries when multiple correlation jobs have run.

**Observed behavior:** The table shows 27 rows including duplicate entries for screenPageViews, newUsers, clicks, ctr, cost, impressions, conversions, average_cpc, bounceRate, and averageSessionDuration — each appearing twice with slightly different coefficients (from two different job runs).

**Root cause:** In `MetricFlow.Correlations.get_latest_correlation_summary/1` (line 95 of `lib/metric_flow/correlations.ex`), the results are loaded with:
```elixir
results = CorrelationsRepository.list_correlation_results(scope, [])
```
This fetches all results for the account without filtering by `correlation_job_id`. The summary header metadata (last calculated, data window, data points) comes from only the latest job, but the table rows mix results from all jobs.

**Expected behavior:** Only results linked to the latest completed job should be displayed.

**Fix:** Pass `job.id` to `list_correlation_results/2` (or add a filter option) so results are scoped to the specific job being displayed.

**Affected file:** `lib/metric_flow/correlations.ex:95`
