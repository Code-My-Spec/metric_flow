# QA Result

## Status

partial

## Scenarios

### Scenario 1: Access to goal metrics from the correlations menu

fail

Navigated to `http://localhost:4070/correlations`. The page loaded the correlations index showing existing correlation data (goal: revenue, coefficients for multiple metrics). Searched for:

- `[data-role='configure-goals']` — not found
- `a[href='/correlations/goals']` — not found
- Text "Goal Metrics", "Configure Goals", "Set Goals" — none found

The correlations index page has no navigation entry point to the goal metrics configuration page. Users cannot discover or access `/correlations/goals` from the UI — they would need to know the URL directly.

Evidence: `.code_my_spec/qa/446/screenshots/01_correlations_index.png`

### Scenario 2: Direct navigation to goal metrics page

pass

Navigated directly to `http://localhost:4070/correlations/goals`. The page loaded correctly with:

- H1 "Goal Metric" — present
- Subtitle "Choose the metric the correlation engine targets." — present
- `<select name="goal_metric_name">` dropdown — present and populated with 25 metric names
- Save Goal button `[data-role="save-goal"]` — present and enabled
- Cancel button `[data-role="cancel"]` — present and enabled

Evidence: `.code_my_spec/qa/446/screenshots/02_goals_page.png`

### Scenario 3: Empty state when no metrics are synced

partial — cannot test

The QA Test Account has synced metric data (25 distinct metric names from prior integration runs). The empty state path — "No metrics available. Connect your integrations and sync data before configuring a goal." — could not be exercised without purging metric data from the database.

The source code was reviewed and the empty state branch is implemented correctly: when `metric_names == []`, the disabled placeholder option and "Connect Integrations" link are rendered, and the Save Goal button is disabled. This path was not tested via the browser.

No seeds exist to reset the account to a zero-metrics state.

### Scenario 4: Unauthenticated redirect

pass

Verified via curl (unauthenticated HTTP request, no session cookie):

```
curl -s -o /dev/null -w "%{http_code} %{redirect_url}" http://localhost:4070/correlations/goals
302 http://localhost:4070/users/log-in
```

An unauthenticated request to `/correlations/goals` returns HTTP 302 and redirects to `/users/log-in` as expected.

Evidence: `.code_my_spec/qa/446/screenshots/04_unauthenticated_redirect.png` (login page shown)

### Scenario 5: Cancel button navigates away

pass

Navigated to `/correlations/goals`. Clicked the Cancel button `[data-role="cancel"]`. The browser navigated immediately to `http://localhost:4070/correlations` without saving any changes. The correlations index loaded correctly.

Evidence: `.code_my_spec/qa/446/screenshots/05_cancel_navigates_to_correlations.png`

### Scenario 6: System stores goal metrics per account

pass

Metric data was available in the QA account (25 metrics synced from prior integration tests). Navigated to `/correlations/goals`. The dropdown was pre-selected to "revenue" (the existing goal from the latest correlation summary). Selected "sessions" from the dropdown and clicked Save Goal.

Result: flash message "Goal metric saved. Correlation analysis started." appeared and the page redirected to `/correlations`. The correlations index showed a "Correlation analysis is running" banner, confirming the job was enqueued.

Evidence: `.code_my_spec/qa/446/screenshots/07_save_goal_flash_and_redirect.png`

### Scenario 7: Goal metric selection persists

pass

After saving "sessions" in Scenario 6, navigated back to `/correlations/goals`. The dropdown was pre-selected to "sessions", confirming the goal was persisted and pre-loaded from the latest correlation summary on mount.

Evidence: `.code_my_spec/qa/446/screenshots/08_goal_persists_after_save.png`

## Evidence

- `.code_my_spec/qa/446/screenshots/01_correlations_index.png` — Correlations index page, no link to goals configuration
- `.code_my_spec/qa/446/screenshots/02_goals_page.png` — Goals page on direct navigation, form with populated dropdown
- `.code_my_spec/qa/446/screenshots/03_goals_page_with_metrics.png` — Login page (visited during auth re-check)
- `.code_my_spec/qa/446/screenshots/04_unauthenticated_redirect.png` — Login page shown after unauthenticated redirect
- `.code_my_spec/qa/446/screenshots/05_cancel_navigates_to_correlations.png` — Correlations index after Cancel clicked
- `.code_my_spec/qa/446/screenshots/06_goals_page_preselected.png` — Goals page with "revenue" pre-selected before save
- `.code_my_spec/qa/446/screenshots/07_save_goal_flash_and_redirect.png` — Correlations index after save, showing flash message
- `.code_my_spec/qa/446/screenshots/08_goal_persists_after_save.png` — Goals page showing "sessions" pre-selected after save
- `.code_my_spec/qa/446/screenshots/09_exploratory_second_save.png` — Correlations page after second save (analysis completed first run)
- `.code_my_spec/qa/446/screenshots/10_goals_page_final_state.png` — Goals page in final state with "revenue" restored

## Issues

### No navigation entry point to Goal Metrics from the Correlations page

#### Severity
HIGH

#### Scope
APP

#### Description
The `/correlations` index page provides no way to navigate to `/correlations/goals`. There is no button, link, or menu item referencing goal metrics configuration. Users cannot access the goal metric selection feature without knowing the direct URL.

The brief's setup notes acknowledge this gap: "The `/correlations` index page (`CorrelationLive.Index`) does not currently contain a navigation link or button to `/correlations/goals`."

This is a critical discoverability issue. The entire goal metrics feature is unreachable from the UI navigation. Reproduction: log in as any user, navigate to `/correlations` — no path to goal configuration exists.

### Empty state cannot be tested without metric data reset capability

#### Severity
LOW

#### Scope
QA

#### Description
Scenario 3 (empty state when no metrics are synced) could not be executed because the QA Test Account has 25 synced metric names from prior integration test runs. No seed script or QA utility exists to reset the account to a zero-metrics state. The empty state branch in `CorrelationLive.Goals` was verified via code review only, not via browser test.

To make this scenario testable, a QA reset script should be added that truncates `metric_values` for the QA account (or a dedicated empty-account fixture should be provided).
