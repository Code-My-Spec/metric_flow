# QA Result

## Status

fail

## Scenarios

### A1: Smart mode toggle is present on /correlations

pass

Navigated to `http://localhost:4070/correlations`. In Raw mode, `[data-role='mode-smart']` button was present and `[data-role='enable-ai-suggestions']` was not visible. After clicking the Smart button, `[data-role='enable-ai-suggestions']` became visible. Both checks passed.

Evidence: `.code_my_spec/qa/450/screenshots/A1-correlations-raw-mode.png`, `.code_my_spec/qa/450/screenshots/A1-correlations-smart-mode.png`

### A2: Enabling AI Suggestions shows recommendations on /correlations

pass

Clicked `[data-role='enable-ai-suggestions']` in Smart mode. `[data-role='ai-suggestions-enabled']` appeared with text "AI Suggestions enabled". `[data-role='ai-recommendations']` became visible and contained: "revenue", "ROI", "budget", "correlation strength" (business context) and "increase", "optimize", "reduce" (actionable language). All checks passed.

Evidence: `.code_my_spec/qa/450/screenshots/A2-ai-suggestions-enabled.png`

### A3: Feedback buttons appear and "Helpful" submits on /correlations

pass

`[data-role='feedback-helpful']` and `[data-role='feedback-not-helpful']` were visible. `[data-role='feedback-helper-text']` read "Was this helpful or not helpful? Your feedback helps improve future suggestions." Clicked "Helpful". `[data-role='feedback-confirmation']` appeared with text "Thanks for your feedback — helps improve future suggestions." Feedback buttons disappeared. All checks passed.

Evidence: `.code_my_spec/qa/450/screenshots/A3-feedback-buttons.png`, `.code_my_spec/qa/450/screenshots/A3-feedback-confirmation.png`

### A4: "Not helpful" feedback submits on /correlations

pass

Reloaded `/correlations`, switched to Smart mode, enabled AI suggestions. Clicked `[data-role='feedback-not-helpful']`. `[data-role='feedback-confirmation']` appeared. Passed.

Evidence: `.code_my_spec/qa/450/screenshots/A4-not-helpful-confirmation.png`

### B1: Insights list renders with populated data on /insights

pass

Navigated to `http://localhost:4070/insights` as `qa@example.com`. H1 "AI Insights" was present. Subtitle "Actionable recommendations generated from your correlation analysis" was present. `[data-role='insights-list']` was visible with 5 `[data-role='insight-card']` elements. First card's `[data-role='insight-content']` contained "0.82", "clicks", "revenue". Cards contained actionable language including "Increase", "optimize", "budget". All checks passed.

Evidence: `.code_my_spec/qa/450/screenshots/B1-insights-page-full.png`

### B2: Type filter bar is functional on /insights

pass

`[data-role='type-filter']` was present with buttons: All, Budget Increase, Budget Decrease, Optimization, Monitoring, General. Clicked "Budget Increase" — only 1 card remained (type badge "Budget Increase"). The "Budget Increase" button had class `btn btn-primary btn-sm`. Clicked "All" — all 5 cards returned. All checks passed.

Evidence: `.code_my_spec/qa/450/screenshots/B2-budget-increase-filter.png`

### B3: Insight cards display all required fields

pass

Verified on first card (budget_increase, confidence 0.85):
- `[data-role='insight-summary']` present with summary text
- `[data-role='insight-content']` present with full recommendation text
- `[data-role='insight-type-badge']` present showing "Budget Increase"
- `[data-role='insight-confidence-badge']` present showing "85% confidence" with class `badge-success` (>= 70%)
- `[data-role='insight-generated-at']` present
- `[data-role='insight-correlation-ref']` present showing "Based on correlation result #47"

Verified on third card (monitoring, confidence 0.55): confidence badge had class `badge-ghost` (< 70%). All checks passed.

### B4: Feedback buttons are present on each insight card

pass

All 5 insight cards had `[data-role='ai-feedback-section']` sections. Each section contained `[data-role='feedback-helpful']` ("Helpful") and `[data-role='feedback-not-helpful']` ("Not helpful") buttons. `[data-role='feedback-helper-text']` read "Your feedback helps improve future suggestions." on each card.

Evidence: `.code_my_spec/qa/450/screenshots/B4-feedback-section.png`

### B5: "Helpful" feedback records and shows confirmation on /insights

pass

Clicked `[data-role='feedback-helpful']` on the first card. `[data-role='feedback-confirmation']` replaced the buttons with text "Thanks for your feedback — helps improve future suggestions." No error flash appeared. Feedback buttons were no longer visible.

Evidence: `.code_my_spec/qa/450/screenshots/B5-helpful-feedback-submitted.png`

### B6: "Not helpful" feedback records and shows confirmation on /insights

pass

Clicked `[data-role='feedback-not-helpful']` on the second card. `[data-role='feedback-confirmation']` replaced the buttons with the correct confirmation text. Passed.

Evidence: `.code_my_spec/qa/450/screenshots/B6-not-helpful-feedback-submitted.png`

### B7: AI personalization note is visible when insights exist

pass

`[data-role='ai-personalization-note']` was visible with text "AI suggestions learn from your feedback and improve over time."

Evidence: `.code_my_spec/qa/450/screenshots/B7-personalization-note.png`

### B8: Feedback persists on page reload

pass

After submitting feedback in B5 and B6, navigated to `/correlations` and back to `/insights`. First card still showed `[data-role='feedback-confirmation']` (not the Helpful/Not helpful buttons). Third card (not yet rated) still showed the feedback buttons. Persistence confirmed.

Evidence: `.code_my_spec/qa/450/screenshots/B8-after-reload.png`

### B9: Empty state — no insights

fail

The brief specified `qa-member@example.com` as the empty-state user, but this user belongs to "QA Test Account" (a team account shared with `qa@example.com`) and sees all 5 seeded insights. The empty-state UI could not be reached via this user.

The seed script (`qa_seeds_450.exs`) references `qa-empty@example.com` as the intended empty-state user and states it is "Created by qa_seeds.exs", but `qa_seeds.exs` does not create this user. When attempting to log in as `qa-empty@example.com`, login succeeded (the user may exist from a prior run), but navigating to `/insights` produced a 500 error: `ArgumentError: nil given for account_id` — because `get_personal_account_id/1` returns nil for users with no account memberships, and `AiRepository.list_insights/2` passes this nil value directly into an Ecto query without nil-guarding.

The empty state UI (`data-role='no-insights-state'`, `data-role='no-insights-state'` heading, "Run Correlations" button) was never reached. Two bugs found (see Issues).

Evidence: `.code_my_spec/qa/450/screenshots/B9-empty-state-crash.png`, `.code_my_spec/qa/450/screenshots/B9-member-empty-state.png`

### B10: Empty filter state — filter with no matches

partial

All 5 suggestion types (budget_increase, optimization, monitoring, budget_decrease, general) have exactly one seeded insight each, so every filter button returns at least one result. No filter produces zero results with the current seed data. The empty filter state (`[data-role='no-filter-results-state']`) and `[data-role='clear-filter']` "Show All" button could not be triggered. This scenario was not testable with the given seed data.

### C1: Dashboard AI info buttons

pass (implementation gap documented)

Navigated to `http://localhost:4070/dashboard` as `qa@example.com`. No `[data-role='ai-info-button']` elements were present anywhere on the page, including after scrolling through the full dashboard. The dashboard does not currently render AI info buttons. This is an expected implementation gap per the brief.

Evidence: `.code_my_spec/qa/450/screenshots/C1-dashboard.png`, `.code_my_spec/qa/450/screenshots/C1-dashboard-scrolled.png`

## Evidence

- `.code_my_spec/qa/450/screenshots/A1-correlations-raw-mode.png` — correlations page in Raw mode showing mode-smart button
- `.code_my_spec/qa/450/screenshots/A1-correlations-smart-mode.png` — correlations page in Smart mode showing enable-ai-suggestions button
- `.code_my_spec/qa/450/screenshots/A2-ai-suggestions-enabled.png` — AI suggestions enabled badge and recommendations section visible
- `.code_my_spec/qa/450/screenshots/A3-feedback-buttons.png` — feedback-helpful and feedback-not-helpful buttons visible
- `.code_my_spec/qa/450/screenshots/A3-feedback-confirmation.png` — feedback-confirmation shown after clicking Helpful
- `.code_my_spec/qa/450/screenshots/A4-not-helpful-confirmation.png` — feedback-confirmation shown after clicking Not helpful
- `.code_my_spec/qa/450/screenshots/B1-insights-page-full.png` — full-page screenshot of /insights with all 5 cards
- `.code_my_spec/qa/450/screenshots/B2-budget-increase-filter.png` — type filter showing only Budget Increase card
- `.code_my_spec/qa/450/screenshots/B4-feedback-section.png` — feedback buttons visible on insight cards
- `.code_my_spec/qa/450/screenshots/B5-helpful-feedback-submitted.png` — feedback-confirmation shown on first card after Helpful click
- `.code_my_spec/qa/450/screenshots/B6-not-helpful-feedback-submitted.png` — feedback-confirmation shown on second card after Not helpful click
- `.code_my_spec/qa/450/screenshots/B7-personalization-note.png` — ai-personalization-note visible below insights list
- `.code_my_spec/qa/450/screenshots/B8-after-reload.png` — first card still shows confirmation on reload; other cards still show buttons
- `.code_my_spec/qa/450/screenshots/B9-empty-state-crash.png` — 500 error when navigating to /insights as user with no account
- `.code_my_spec/qa/450/screenshots/B9-member-empty-state.png` — qa-member@example.com sees all 5 insights (not empty state)
- `.code_my_spec/qa/450/screenshots/C1-dashboard.png` — dashboard without AI info buttons
- `.code_my_spec/qa/450/screenshots/C1-dashboard-scrolled.png` — dashboard scrolled, no AI info buttons found

## Issues

### /insights crashes with 500 when user has no account membership

#### Severity
HIGH

#### Description
When a user with no `account_members` records navigates to `/insights`, the LiveView crashes with:

```
ArgumentError: nil given for `account_id`. comparison with nil is forbidden as it is unsafe.
```

`AiRepository.list_insights/2` calls `Accounts.get_personal_account_id(scope)` which returns `nil` when the user has no account memberships. This nil is then passed directly to `where(account_id: ^account_id)`, raising an ArgumentError instead of returning an empty list.

The fix should nil-guard in `AiRepository.list_insights/2`: if `account_id` is nil, return `[]` immediately. Alternatively, `get_personal_account_id/1` should be documented as possibly returning nil, and all callers should handle the nil case.

Reproduced by logging in as `qa-empty@example.com` (a user with no account) and navigating to `http://localhost:4070/insights`.

### Dashboard AI info buttons not implemented (AC3/AC4 gap)

#### Severity
LOW

#### Description
The acceptance criteria for this story include "Each chart can have an AI info button" and "Clicking AI button shows insights". No `[data-role='ai-info-button']` elements are present on the dashboard at `http://localhost:4070/dashboard`. The dashboard renders charts and a data table, but has no AI info entry points. This was noted as a potential gap in the brief. Documenting as an implementation gap for tracking.

### qa-empty@example.com not created by qa_seeds.exs

#### Severity
MEDIUM

#### Scope
QA

#### Description
`qa_seeds_450.exs` states in its summary output: "Empty-state user: qa-empty@example.com — Created by qa_seeds.exs — no additional seeding required." However, `qa_seeds.exs` does not create `qa-empty@example.com`. This user either does not exist or was created outside of the normal seed flow.

The brief for story 450 correctly uses `qa-member@example.com` for empty-state testing, but `qa-member@example.com` belongs to "QA Test Account" (the shared team account) and sees all 5 insights seeded for `qa@example.com`. There is no isolated user account with zero insights available through the current seed infrastructure.

`qa_seeds_450.exs` should be updated to create and reference a properly isolated user with its own empty personal account, or `qa_seeds.exs` should be updated to create `qa-empty@example.com` with a confirmed account and no insights. The empty-state scenario (B9) could not be fully validated because of this gap.

### Brief incorrectly specifies qa-member@example.com for empty-state testing

#### Severity
LOW

#### Scope
QA

#### Description
The brief for story 450 (`brief.md`) lists `qa-member@example.com` as the user to use for empty-state testing (Scenario B9) and says to clear cookies and log in as this user. However, `qa-member@example.com` is a member of the "QA Test Account" team account, which has 5 seeded insights. This user will always see insights and cannot reach the empty state UI.

The brief should be updated to reference the correct empty-state user, or note that a separate seed step is required to create an isolated user with no insights.

### B10 empty filter state not testable with current seed data

#### Severity
INFO

#### Scope
QA

#### Description
Scenario B10 requires at least one suggestion type with zero matching insights to trigger `[data-role='no-filter-results-state']`. The current seed data in `qa_seeds_450.exs` creates exactly one insight for each of the five suggestion types (budget_increase, optimization, monitoring, budget_decrease, general), so every filter button returns at least one result. The empty filter state UI was never rendered.

To make B10 testable, the seeds should either omit one suggestion type, or the brief should note that this scenario requires manual data manipulation.
