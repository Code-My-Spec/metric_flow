# QA Result

Story 437 — Automated Daily Data Sync

## Status

partial

## Scenarios

### Scenario 1 — Page loads and renders core structure

pass

Navigated to `http://localhost:4070/integrations/sync-history` after login as `qa@example.com`. Verified:

- H1 text is "Sync History"
- Page title is "Sync History · Phoenix Framework"
- Subtitle "View automated sync results and status" is present
- `[data-role="sync-schedule"]` element is visible
- Schedule section contains "2:00 AM UTC"
- Schedule section contains "Daily"
- `.badge.badge-info` labeled "Daily" is visible

Evidence: `01_page_load.png`

### Scenario 2 — Schedule section communicates backfill and retry behavior

pass

Verified the schedule section text:

- Contains "backfilled" (matches "backfill" criterion): "On first sync, all available historical data is backfilled."
- Contains "retried up to 3 times" (matches "3 times" and "retry" criteria): "Failed syncs are automatically retried up to 3 times with exponential backoff."

Evidence: `02_schedule_section.png`

### Scenario 3 — Financial and marketing providers mentioned in schedule section

pass

Verified:

- "QuickBooks" is present in the schedule section text
- "Google Ads", "Facebook Ads", and "Google Analytics" are all present in the schedule section text
- No `[data-role="financial-sync-history"]` element exists on the page (returns false)
- No `[data-role="marketing-sync-history"]` element exists on the page (returns false)
- Unified `[data-role="sync-history"]` list is the only history container

Evidence: `03_providers_coverage.png`

### Scenario 4 — Date range bar excludes today

pass

Verified:

- `[data-role="date-range"]` is present
- Date range text shows "2026-03-14" (yesterday, since today is 2026-03-15)
- Text is "Showing data through 2026-03-14 (yesterday — today excluded, incomplete day)"
- Today's date (2026-03-15) is NOT shown as the end date
- Text contains "yesterday — today excluded, incomplete day"

Evidence: `04_date_range.png`

### Scenario 5 — Empty state when no sync history

fail

The brief states the QA owner user should have no connected integrations and no sync history after seeding. However, the database already contained 2 persisted sync history entries for the qa@example.com user:

1. Google Ads — Success — 342 records synced — Completed at Mar 04, 2026 02:03 UTC
2. Google Ads — Failed — API rate limit exceeded — Date: 2026-03-03

Because `sync_history` is non-empty, the empty state panel ("No sync history yet.") is NOT shown, and the `[data-sync-type="initial"]` badge is NOT visible.

The `[data-role="sync-history"]` element is present and shows `[data-role="sync-history-entry"]` elements (count: 2), contradicting the expected empty state.

This is a QA data isolation issue — the seed script does not clear existing sync history for the QA user before re-seeding.

Evidence: `05_empty_state.png` (shows actual state with entries, not empty state)

### Scenario 6 — Filter buttons present and default state is "All" active

pass

Verified:

- All three filter buttons ("All", "Success", "Failed") are visible
- `[data-role="filter-all"]` has class `btn btn-sm btn-primary` (active)
- `[data-role="filter-success"]` has class `btn btn-sm btn-ghost` (inactive)
- `[data-role="filter-failed"]` has class `btn btn-sm btn-ghost` (inactive)

Evidence: `06_filter_buttons.png`

### Scenario 7 — Filter button click toggles active state

pass

Clicked `[data-role="filter-success"]`:

- `[data-role="filter-success"]` now has class `btn btn-sm btn-primary` (active)
- `[data-role="filter-all"]` now has class `btn btn-sm btn-ghost` (inactive)

Clicked `[data-role="filter-failed"]`:

- `[data-role="filter-failed"]` now has class `btn btn-sm btn-primary` (active)
- `[data-role="filter-success"]` now has class `btn btn-sm btn-ghost` (inactive)

Clicked `[data-role="filter-all"]` to reset. Filter state restored to "All" active.

Evidence: `07_filter_toggle.png`

### Scenario 8 — Unauthenticated access redirects to login

pass

Used curl to check unauthenticated access:

```
curl -s -o /dev/null -w "%{http_code}" http://localhost:4070/integrations/sync-history
```

Result: `302` — redirects to login as expected.

### Scenario 9 — Page mentions "metrics" and "financial data" in body text

pass

Full page text contains:

- "metrics" — present in schedule description: "retrieves metrics and financial data per provider, per day"
- "financial" — present in same sentence
- "QuickBooks" — present as the named financial provider

Evidence: `09_metrics_financial_mention.png`

### Scenario 10 — Page structure integrity check

pass

Verified all required structural elements are present:

- H1 text is exactly "Sync History"
- `[data-role="sync-schedule"]` is present
- `[data-role="date-range"]` is present
- `[data-role="sync-history"]` is present
- `[data-role="filter-all"]` is present
- `[data-role="filter-success"]` is present
- `[data-role="filter-failed"]` is present

Evidence: `10_full_page.png` (full-page screenshot)

## Evidence

- `.code_my_spec/qa/437/screenshots/01_page_load.png` — Initial page load showing H1, subtitle, schedule card, date range
- `.code_my_spec/qa/437/screenshots/02_schedule_section.png` — Schedule section with backfill and retry text
- `.code_my_spec/qa/437/screenshots/03_providers_coverage.png` — Provider names and unified history list
- `.code_my_spec/qa/437/screenshots/04_date_range.png` — Date range showing yesterday's date with exclusion note
- `.code_my_spec/qa/437/screenshots/05_empty_state.png` — State with existing sync history entries (empty state NOT shown)
- `.code_my_spec/qa/437/screenshots/06_filter_buttons.png` — Filter buttons with "All" active
- `.code_my_spec/qa/437/screenshots/07_filter_toggle.png` — Filter buttons after toggling back to "All"
- `.code_my_spec/qa/437/screenshots/09_metrics_financial_mention.png` — Page with "metrics" and "financial" text visible
- `.code_my_spec/qa/437/screenshots/10_full_page.png` — Full-page screenshot showing all structural elements

## Issues

### QA seed script does not clear existing sync history for the QA user

#### Severity
MEDIUM

#### Scope
QA

#### Description
Scenario 5 tests the empty state ("No sync history yet.") which is only shown when both `sync_history` and `sync_events` are empty. The brief states that after seeding, the QA owner user should have no sync history.

However, `priv/repo/qa_seeds.exs` creates users and account records but does not delete any existing sync history for the QA user. On repeated QA runs (or after prior test scenarios that generated history), the `data_sync_history` (or equivalent) table retains entries for `qa@example.com`, causing the empty state to be bypassed.

To fix: add an idempotent cleanup step in `priv/repo/qa_seeds.exs` that deletes all sync history records scoped to the QA test user before re-seeding, so each QA run starts with a clean slate for empty-state tests.

Reproduction: run `qa_seeds.exs` after any prior QA session that produced sync history entries, then navigate to `/integrations/sync-history` — the empty state panel is not shown.
