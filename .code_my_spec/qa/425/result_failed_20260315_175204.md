# QA Result

Story 425: User Login and Session Management

## Status

fail

## Scenarios

### Scenario 1: Password login with valid credentials

**Result: pass**

Navigated to `http://localhost:4070/users/log-in`. The page rendered with the heading "Log in",
both forms present (`#login_form_magic` and `#login_form_password`), the dev mail adapter info
banner, and both session-length submit buttons visible in the password form.

Filled in `qa@example.com` / `hello world!` in `#login_form_password`, clicked the
"Log in and stay logged in" button. The browser performed a LiveView `submit_password` event,
which set `trigger_submit: true` and triggered a native POST to `UserSessionController`. Server
responded HTTP 302 to `/`. User is now authenticated (email and "Log out" link visible in nav).

The "Welcome back!" flash was set in the response but faded before it could be captured in text
output — the home page layout does not display flash (see Issues section). Redirect to `/`
confirmed by `browser_get_url`.

Evidence: `.code_my_spec/qa/425/screenshots/01_login_page.png`, `.code_my_spec/qa/425/screenshots/02_login_success.png`

### Scenario 2: Magic link login

**Result: pass**

Logged out, navigated to `http://localhost:4070/users/log-in`. Filled `qa@example.com` into
`#login_form_magic input[type=email]` and clicked "Log in with email". The LiveView handled the
`submit_magic` event, called `Users.deliver_login_instructions/2`, and pushed a navigate back to
`/users/log-in`. The page text shows the expected info flash:

> "If your email is in our system, you will receive instructions for logging in shortly."

The flash message is ambiguous by design — it does not reveal whether the address is registered.

Evidence: `.code_my_spec/qa/425/screenshots/03_magic_link_sent.png`

### Scenario 3: Failed login — wrong password

**Result: pass**

Filled `qa@example.com` with password `WrongPassword999!` in `#login_form_password`. Clicked
"Log in and stay logged in". Server returned HTTP 302 to `/users/log-in`. The page rendered with
the error flash "Invalid email or password". Correct behavior — wrong password shows error.

Evidence: `.code_my_spec/qa/425/screenshots/04_login_error_wrong_password.png`

### Scenario 4: Failed login — unregistered email

**Result: pass**

Filled `nonexistent@example.com` / `SomePassword123!` in `#login_form_password`. Server returned
HTTP 302 to `/users/log-in` with the same error flash "Invalid email or password". The message
is identical to the wrong-password case — no email enumeration. Correct behavior.

Evidence: `.code_my_spec/qa/425/screenshots/05_login_error_unregistered.png`

### Scenario 5: Failed login — empty credentials

**Result: pass**

Left the `#login_form_password` email and password fields empty and clicked the submit button.
The browser's HTML5 required-field validation prevented the form from submitting (the email input
has `required` attribute). The page stayed on `/users/log-in` without a server round-trip. No
crash or unexpected behavior.

Evidence: `.code_my_spec/qa/425/screenshots/06_login_error_empty.png`

### Scenario 6: Session persists across tabs

**Result: pass**

After logging in as `qa@example.com`, navigated to `/users/settings`. Page showed "Account
Settings" heading and the email `qa@example.com` in the nav and email field. Navigated to
`/accounts`. Page loaded "Your Accounts" content showing "QA Test Account" — no redirect to
login. Session cookie persisted across page navigations as expected.

Evidence: `.code_my_spec/qa/425/screenshots/07_settings_authenticated.png`, `.code_my_spec/qa/425/screenshots/08_accounts_authenticated.png`

### Scenario 7: Log out from settings page

**Result: partial (flash not shown)**

Navigated to `/users/settings`. The "Log out" link is visible in the navigation dropdown
(`a[href='/users/log-out']`). Clicked the link. Server handled `DELETE /users/log-out` via
`UserSessionController.delete/2`, which calls `UserAuth.log_out_user/1`.

After logout, the browser redirected to `http://localhost:4070/`. URL confirmed as `/`.
User is no longer authenticated (nav shows "Register/Log in"). The session token and
remember_me cookie were deleted. However, the "Logged out successfully." flash message set by
the controller was NOT displayed on the home page.

Root cause: `UserAuth.log_out_user/1` redirects to `~p"/"` (home page), which is rendered by
`PageController` using `root.html.heex`. Neither the root layout nor the `home.html.heex`
template includes flash rendering. The flash message is silently dropped. See Issues section.

Evidence: `.code_my_spec/qa/425/screenshots/09_settings_with_logout_link.png`, `.code_my_spec/qa/425/screenshots/10_logged_out.png`

### Scenario 8: Access protected page after logout

**Result: pass**

After logging out, navigated to `http://localhost:4070/users/settings`. The server redirected
to `/users/log-in`. The page rendered with the error flash "You must log in to access this page."
This confirms the session was invalidated — the protected route correctly rejected the
unauthenticated request.

Evidence: `.code_my_spec/qa/425/screenshots/11_post_logout_redirect.png`

### Scenario 9: Unauthenticated access redirects to login

**Result: pass**

`curl -s -o /dev/null -w "%{http_code}" http://localhost:4070/users/settings` returned `302`.

In the browser (no active session from scenario 8 continuing), navigating to
`/users/settings` redirected to `/users/log-in` with the error flash
"You must log in to access this page." The `user_return_to` session value was also set to
`/users/settings` (verified by the session redirecting there after login in scenario 1 replay).

Evidence: `.code_my_spec/qa/425/screenshots/12_unauthenticated_redirect.png`

### Scenario 10: Remember Me buttons visible on login page

**Result: pass**

Both session-length buttons are present in `#login_form_password`:
- "Log in and stay logged in" button: `name="user[remember_me]"`, `value="true"` (confirmed via
  `browser_get_attribute`)
- "Log in only this time" button: no `name` attribute (omits `remember_me` from submission)

Evidence: `.code_my_spec/qa/425/screenshots/13_remember_me_buttons.png`

### Scenario 11: Login with Remember Me sets cookie

**Result: pass**

Using curl with a fresh session cookie and properly URL-encoded CSRF token:

```bash
curl -d "_csrf_token=${CSRF}&user%5Bemail%5D=qa%40example.com&user%5Bpassword%5D=hello+world%21&user%5Bremember_me%5D=true" \
  http://localhost:4070/users/log-in
```

Response: HTTP 302 to `/`. Response headers included:
```
set-cookie: _metric_flow_web_user_remember_me=SFMyNTY...; max-age=1209600; HttpOnly; SameSite=Lax
```

The `_metric_flow_web_user_remember_me` cookie was set with a 14-day expiry (`max-age=1209600`).
The session flash decoded to "Welcome back!".

Note: The `!` in the password must be percent-encoded as `%21` in curl form data. Using
`hello+world!` (unencoded `!`) causes the login to fail with "Invalid email or password" even
though the credentials are correct. See Issues section.

### Scenario 12: Login without Remember Me does not set cookie

**Result: pass**

Using curl without the `remember_me` field:

```bash
curl -d "_csrf_token=${CSRF}&user%5Bemail%5D=qa%40example.com&user%5Bpassword%5D=hello+world%21" \
  http://localhost:4070/users/log-in
```

Response: HTTP 302 to `/`. The `_metric_flow_web_user_remember_me` cookie was NOT present in the
response or the cookie jar file. Only the session cookie `_metric_flow_key` was set.

## Evidence

- `.code_my_spec/qa/425/screenshots/01_login_page.png` — login page with both forms and both session buttons
- `.code_my_spec/qa/425/screenshots/02_login_success.png` — home page after successful login (user authenticated in nav)
- `.code_my_spec/qa/425/screenshots/03_magic_link_sent.png` — login page with "If your email is in our system..." info flash
- `.code_my_spec/qa/425/screenshots/04_login_error_wrong_password.png` — login page with "Invalid email or password" flash (wrong password)
- `.code_my_spec/qa/425/screenshots/05_login_error_unregistered.png` — login page with "Invalid email or password" flash (unregistered email)
- `.code_my_spec/qa/425/screenshots/06_login_error_empty.png` — login page with HTML5 required-field validation (empty credentials)
- `.code_my_spec/qa/425/screenshots/07_settings_authenticated.png` — account settings page showing authenticated user email
- `.code_my_spec/qa/425/screenshots/08_accounts_authenticated.png` — accounts page loaded without redirect (session persists)
- `.code_my_spec/qa/425/screenshots/09_settings_with_logout_link.png` — settings page showing "Log out" link in nav dropdown
- `.code_my_spec/qa/425/screenshots/10_logged_out.png` — home page after logout (user no longer authenticated)
- `.code_my_spec/qa/425/screenshots/11_post_logout_redirect.png` — login page after accessing settings post-logout (flash: "You must log in")
- `.code_my_spec/qa/425/screenshots/12_unauthenticated_redirect.png` — login page after unauthenticated access (flash: "You must log in")
- `.code_my_spec/qa/425/screenshots/13_remember_me_buttons.png` — login page showing both remember me buttons with correct attributes

## Issues

### "Logged out successfully." flash never displayed after logout

#### Severity
MEDIUM

#### Scope
APP

#### Description
When a user logs out via the "Log out" link, `UserSessionController.delete/2` calls
`put_flash(:info, "Logged out successfully.")` and then `UserAuth.log_out_user/1` which
redirects to `~p"/"` (the home page).

The home page is rendered by `PageController.home/2` using the `root.html.heex` layout.
Neither `root.html.heex` nor `home.html.heex` includes flash rendering (no `<.flash_group>`,
`<.flash>`, or any flash display component). The flash message is stored in the session cookie
but is never rendered to the user.

This means users receive no confirmation feedback when logging out — a usability issue that
could cause confusion about whether the logout action was successful.

Reproduced consistently: log in as `qa@example.com`, navigate to `/users/settings`, click
"Log out". The browser redirects to `/` but no "Logged out successfully." message appears.

Fix options:
1. Add `<.flash_group flash={@flash} />` to `root.html.heex` so flash works on all controller routes
2. Redirect logout to a LiveView route (e.g. `/users/log-in`) that renders flash correctly
3. Add flash rendering to the `home.html.heex` template

### Brief curl commands fail due to unencoded `!` in password

#### Severity
LOW

#### Scope
QA

#### Description
The brief's Scenario 11 and 12 curl commands use the form data encoding
`user[password]=hello+world!` where `!` is not percent-encoded. In URL-encoded form bodies, `!`
must be encoded as `%21` when submitting via curl's `-d` flag. Using the unencoded `!` causes the
server to receive the wrong password string, and the login returns "Invalid email or password"
even though the credentials are valid.

The correct encoding is `user%5Bpassword%5D=hello+world%21`.

The browser login works correctly because the browser handles form encoding automatically.

Fix: Update the brief's curl examples for Scenarios 11 and 12 to use `hello+world%21` instead
of `hello+world!`.

### POST /users/log-in with email-only body returns HTTP 500

#### Severity
LOW

#### Scope
APP

#### Description
Sending `POST /users/log-in` with `user[email]` but no `user[password]` field returns HTTP 500.
The controller's `create/3` clause at line 34 of `user_session_controller.ex` pattern-matches
with `%{"email" => email, "password" => password} = user_params`, which raises a `MatchError`
when `"password"` is absent from the map.

In normal browser usage this path is never reached — the magic link form uses
`phx-submit="submit_magic"` which is handled by the LiveView process and never POSTs to the
controller. However, a direct HTTP POST to the endpoint (e.g. from a script or malformed
request) crashes rather than returning a graceful 400 or redirecting with an error flash.

To reproduce:
```bash
curl -s -o /dev/null -w "%{http_code}" \
  -X POST http://localhost:4070/users/log-in \
  -b <session-cookies> \
  -d "_csrf_token=<valid-token>&user[email]=qa@example.com"
# Returns: 500
```

Fix: Add a catch-all `create/3` clause or guard that handles the email-only case gracefully
with an error redirect rather than a crash.
