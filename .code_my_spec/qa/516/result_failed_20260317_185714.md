# QA Result

## Status

partial

## Scenarios

### Scenario 1 — Unauthenticated redirect

pass

Verified with curl: `curl -s -o /dev/null -w "%{http_code}" http://localhost:4070/integrations/sync-history` returned `302`. Unauthenticated access to the sync history page correctly redirects to the login page.

BDD spex (criterion_4806, scenario 2) also confirms this via LiveViewTest: `live(anon_conn, "/integrations/sync-history")` returns `{:error, {:redirect, %{to: "/users/log-in"}}}`.

### Scenario 2 — Page loads for authenticated user

pass

After logging in as `qa@example.com`, navigated to `/integrations/sync-history`. All expected elements were present:

- H1 "Sync History" — confirmed via `browser_get_text(selector: "h1")`
- Subtitle "View automated sync results and status" — visible in page text
- `[data-role="sync-schedule"]` card present with H2 "Automated Sync Schedule"
- Description contains "Daily at 2:00 AM UTC"
- `.badge.badge-info` labeled "Daily" — confirmed via `browser_find`
- `[data-role="date-range"]` present, showing "Showing data through 2026-03-16 (yesterday — today excluded, incomplete day)"

Screenshot: `.code_my_spec/qa/516/screenshots/02-page-loaded.png`

### Scenario 3 — Empty state shown when no syncs have run

fail

The page showed existing sync history entries rather than the empty state. The database contained many persisted sync records from previous test runs (multiple Google and Facebook Ads sync entries). The seed script brief notes it clears sync history, but the base `qa_seeds.exs` does not truncate the `sync_jobs` table — sync history accumulated from previous QA sessions for stories 437/438 persists.

The empty state structure (`.mf-card` with "No sync history yet." text and `.badge.badge-ghost` with `data-sync-type="initial"`) is confirmed in the source code at `sync_history.ex` lines 98–108. It is shown only when both `sync_history` and `sync_events` are empty.

Screenshot: `.code_my_spec/qa/516/screenshots/03-empty-state.png` (shows populated state, not empty state)

### Scenario 4 — Filter tabs are rendered and functional

pass

All three filter buttons were present with correct initial state:
- `[data-role="filter-all"]` had class `btn btn-sm btn-primary` (active)
- `[data-role="filter-success"]` had class `btn btn-sm btn-ghost` (inactive)
- `[data-role="filter-failed"]` had class `btn btn-sm btn-ghost` (inactive)

Clicking "Success": `filter-success` became `btn-primary`, `filter-all` became `btn-ghost`. Pass.
Clicking "Failed": `filter-failed` became `btn-primary`. Pass.
Clicking "All": `filter-all` became `btn-primary` again. Pass.

Screenshot: `.code_my_spec/qa/516/screenshots/04-filter-tabs.png`

### Scenario 5 — Google Search Console provider display name (BDD spex)

partial

Criterion 4806 (system fetches data using GSC API): PASS — 1 test, 0 failures. The LiveView correctly renders a success entry for `provider: :google_search_console` when a `{:sync_completed, ...}` message is sent. The `derive_display_name/1` fallback produces "Google Search Console" correctly.

Criterion 4807 (data fetched per site URL): FAIL — 1 test, 1 failure. The second scenario's `when_` step returns `{:ok, %{}}` instead of `{:ok, context}`, losing the `:view` key. The subsequent `then_` step crashes with `KeyError: key :view not found in %{}`.

### Scenario 6 — @provider_names map missing :google_search_console (architectural note)

pass (informational)

Confirmed via source code inspection: `@provider_names` at `sync_history.ex` lines 20–26 contains `google_ads`, `facebook_ads`, `google_analytics`, `quickbooks`, and `google`. The `:google_search_console` atom is not present.

The `provider_display_name/1` function falls through to `derive_display_name/1` which converts `:google_search_console` to "Google Search Console" correctly. This is confirmed by the passing BDD spex (criterion_4806). The provider is not explicitly registered — it relies on string manipulation as a fallback.

Screenshot: `.code_my_spec/qa/516/screenshots/06-sync-history-final.png`

## Evidence

- `.code_my_spec/qa/516/screenshots/02-page-loaded.png` — Sync History page loaded for authenticated user showing all structural elements
- `.code_my_spec/qa/516/screenshots/03-empty-state.png` — Page showing populated sync history (not empty state — seeds did not clear history)
- `.code_my_spec/qa/516/screenshots/04-filter-tabs.png` — Filter tabs after cycling through All → Success → Failed → All
- `.code_my_spec/qa/516/screenshots/06-sync-history-final.png` — Full-page sync history with multiple Google and Facebook Ads entries

## Issues

### Criterion 4807 spex — when_ step discards context, causing KeyError in then_ step

#### Severity
HIGH

#### Scope
QA

#### Description
In `test/spex/516_sync_google_search_console_data/criterion_4807_data_is_fetched_per_site_url_googleconsolesiteurl_configured_per_customer_in_configjson_customers_without_a_site_url_are_skipped_spex.exs`, the second scenario's `when_` block (line 59–62) returns `{:ok, %{}}` instead of `{:ok, context}`:

```elixir
when_ "no Google Search Console sync events are broadcast ...", _context do
  {:ok, %{}}
end
```

This discards the entire accumulated context, including the `:view` key set by the previous `given_` step. The `then_` step on line 64 then calls `context.view` on the empty map and crashes with `KeyError: key :view not found in %{}`.

Fix: change `{:ok, %{}}` to `{:ok, context}` to pass the context through unchanged.

### Seed script does not clear sync history — empty state cannot be tested

#### Severity
LOW

#### Scope
QA

#### Description
The brief states "seeds clear all sync history records" and that the page will show the empty state after seeding. However, `priv/repo/qa_seeds.exs` does not truncate or clear the `sync_jobs` table. Sync history from previous QA sessions (stories 437 and 438) persists in the database, so the empty state was not visible during testing.

To reproduce the empty state for Scenario 3, the seed script should include a step to delete all `sync_jobs` records for the QA account, or the brief should instruct the QA agent to manually clear the table before testing.

### :google_search_console not registered in @provider_names map

#### Severity
LOW

#### Scope
APP

#### Description
The `@provider_names` map in `MetricFlowWeb.IntegrationLive.SyncHistory` (line 20–26 of `sync_history.ex`) explicitly maps five providers: `google_ads`, `facebook_ads`, `google_analytics`, `quickbooks`, and `google`. The `:google_search_console` atom is absent.

At runtime, an unmatched provider falls through to `derive_display_name/1`, which converts underscores to spaces and capitalizes each word — producing "Google Search Console" correctly. However, this relies on implicit string manipulation rather than an explicit entry. If the atom name ever changes (e.g., `:gsc` or `:search_console`), the display name will silently break without a compiler warning.

Recommendation: add `google_search_console: "Google Search Console"` to the `@provider_names` map to make the display name explicit and consistent with the other providers.
