# QA Result

Story 434: Connect Marketing Platform via OAuth

## Status

fail

## Scenarios

### Scenario 1 — Platform selection page lists all supported providers

PASS

Navigated to `http://localhost:4070/integrations/connect`. The page loaded with heading "Connect a Provider" and subtitle "Authenticate with your marketing providers to start syncing data". Three provider cards rendered in a grid:

- `data-platform="facebook_ads"` — "Facebook", "Facebook and Instagram advertising", "Connected" badge, "Reconnect" button
- `data-platform="google"` — "Google", "Google Ads and Google Analytics", "Connected" badge, "Reconnect" button
- `data-platform="quickbooks"` — "QuickBooks", "Financial accounting and bookkeeping", "Not connected" badge, "Connect" button

No "Re-authenticate" text on the page.

Screenshot: `.code_my_spec/qa/434/screenshots/01-platform-selection.png`

### Scenario 2 — Connect buttons are present for each provider

PASS

All three provider-scoped connect button selectors matched:

- `[data-platform='google'] [data-role='connect-button']` — present, labeled "Reconnect"
- `[data-platform='facebook_ads'] [data-role='connect-button']` — present, labeled "Reconnect"
- `[data-platform='quickbooks'] [data-role='connect-button']` — present, labeled "Connect"

Screenshot: `.code_my_spec/qa/434/screenshots/02-connect-buttons.png`

### Scenario 3 — Per-provider detail view shows OAuth initiation link

PASS

**Google** (`/integrations/connect/google`):
- Provider name "Google" visible in card heading
- `[data-role='oauth-connect-button']` present, labeled "Reconnect"
- `target="_blank"` attribute confirmed
- `href` is a valid Google OAuth authorization URL beginning with `https://accounts.google.com/o/oauth2/v2/auth?...` including `scope=...googleapis.com/auth/analytics.readonly+...googleapis.com/auth/adwords`
- "Back to integrations" link present

**Facebook** (`/integrations/connect/facebook_ads`):
- Provider name "Facebook" visible
- `[data-role='oauth-connect-button']` present, labeled "Reconnect"
- `href` is a valid Facebook OAuth URL beginning with `https://www.facebook.com/v4.0/dialog/oauth?...`
- "Back to integrations" link present

Screenshots:
- `.code_my_spec/qa/434/screenshots/03-google-detail.png`
- `.code_my_spec/qa/434/screenshots/03b-facebook-detail.png`

### Scenario 4 — Detail view does not show re-authenticate option

PASS

On `/integrations/connect/google`:
- "Re-authenticate" does not appear in page text
- "Re-connect" (with hyphen) does not appear — button is labeled "Reconnect" (no hyphen)
- Page renders without error

### Scenario 5 — Account selection page renders correctly

PASS (partial — bug found for non-Google providers)

**Google accounts** (`/integrations/connect/google/accounts`):
- Page loaded (google integration exists for qa@example.com)
- Heading: "Google — Select Accounts"
- Chooser text: "Choose which GA4 property to sync data from."
- `[data-role='account-selection']` form element present
- `[data-role='save-selection']` button labeled "Save Selection" present
- `[data-role='manual-property-input']` input with placeholder "properties/123456789" present
- No `[data-role='account-list']` — no GA4 properties returned by API (manual entry mode shown instead)

**Facebook accounts** (`/integrations/connect/facebook_ads/accounts`):
- Page loaded (facebook_ads integration exists)
- Heading: "Facebook — Select Accounts" — correct
- Chooser text: "Choose which ad account to sync data from." — correct
- Field label: "Ad Account ID" — correct
- Help text: "Find this in Google Analytics under Admin → Property Settings" — **INCORRECT** (Google-specific text shown for a Facebook provider)

**QuickBooks accounts** (`/integrations/connect/quickbooks/accounts`):
- Redirected to `/integrations/connect/quickbooks` with error flash "Please connect quickbooks first before selecting accounts." — correct behavior (no existing integration)

Screenshots:
- `.code_my_spec/qa/434/screenshots/05-google-accounts.png`
- `.code_my_spec/qa/434/screenshots/05b-facebook-accounts.png`

### Scenario 6 — Integration not saved before OAuth completion

PASS

Logged in as `qa-member@example.com` (no existing integrations). Navigated to `/integrations/connect/google`:
- "Google" heading visible
- "Not connected" badge shown
- "Integration saved" does not appear
- "Integration active" does not appear
- `[data-role='oauth-connect-button']` labeled "Connect Google" present (not connected state)

Screenshot: `.code_my_spec/qa/434/screenshots/06-not-saved-before-oauth.png`

### Scenario 7 — OAuth callback error result view

PASS

Navigated to `http://localhost:4070/integrations/oauth/callback/google?error=access_denied`. Controller redirected to `/integrations/connect/google` with error flash. Result view rendered:
- "Connection Failed" heading visible (with error icon ✗)
- Error message: "Access was denied. Please try again if you want to connect."
- "Try again" link present, pointing to `/integrations/connect/google`
- "Back to integrations" link present
- "Integration saved" and "successfully connected" do not appear

Screenshot: `.code_my_spec/qa/434/screenshots/07-callback-error-result.png`

### Scenario 8 — Error callback with error_description

PASS

Navigated to `http://localhost:4070/integrations/oauth/callback/google?error=access_denied&error_description=User+denied+access`. Same error result view rendered:
- "Connection Failed" heading visible
- Error message: "Access was denied. Please try again if you want to connect."
- "Try again" and "Back to integrations" links present

Note: The `error_description` parameter ("User denied access") is not surfaced in the flash message — the app uses a fixed message for `access_denied`. This is acceptable behavior.

Screenshot: `.code_my_spec/qa/434/screenshots/08-access-denied-error.png`

### Scenario 9 — Unauthenticated user is redirected

PASS

curl check: `curl -s -o /dev/null -w "%{http_code}" http://localhost:4070/integrations/connect` returned `302`. Redirect destination: `http://localhost:4070/users/log-in`.

Browser (fresh session, no cookies): navigating to `http://localhost:4070/integrations/connect` immediately redirected to `http://localhost:4070/users/log-in`.

Screenshot: `.code_my_spec/qa/434/screenshots/09-unauthenticated-redirect.png`

### Scenario 10 — Integration page is scoped to logged-in account

PASS

Logged in as `qa@example.com`. Navigated to `/integrations`:
- Page shows integrations for "QA Test Account" only (Google Analytics, Google Ads, Facebook Ads connected; QuickBooks not connected)
- No "Transfer to agency", "Assign to agency", or "Move to agency" text

Navigated to `/integrations/connect/google`:
- Page shows Google provider detail for QA account
- No transfer or assignment options
- Content is scoped to current user's account

Screenshots:
- `.code_my_spec/qa/434/screenshots/10-integrations-scoped.png`
- `.code_my_spec/qa/434/screenshots/10b-connect-detail-no-transfer.png`

## Evidence

- `.code_my_spec/qa/434/screenshots/01-platform-selection.png` — Provider selection grid with Google, Facebook, QuickBooks cards
- `.code_my_spec/qa/434/screenshots/02-connect-buttons.png` — Connect buttons on platform selection page
- `.code_my_spec/qa/434/screenshots/03-google-detail.png` — Google provider detail view with Reconnect OAuth button
- `.code_my_spec/qa/434/screenshots/03b-facebook-detail.png` — Facebook provider detail view with Reconnect OAuth button
- `.code_my_spec/qa/434/screenshots/05-google-accounts.png` — Google account selection page (manual entry mode)
- `.code_my_spec/qa/434/screenshots/05b-facebook-accounts.png` — Facebook accounts page with incorrect GA4 help text (bug)
- `.code_my_spec/qa/434/screenshots/06-not-saved-before-oauth.png` — Google detail for member user (not connected, no premature save)
- `.code_my_spec/qa/434/screenshots/07-callback-error-result.png` — OAuth callback error result view (access_denied)
- `.code_my_spec/qa/434/screenshots/08-access-denied-error.png` — OAuth callback error with error_description parameter
- `.code_my_spec/qa/434/screenshots/09-unauthenticated-redirect.png` — Unauthenticated redirect to login page
- `.code_my_spec/qa/434/screenshots/10-integrations-scoped.png` — Integrations index scoped to QA account
- `.code_my_spec/qa/434/screenshots/10b-connect-detail-no-transfer.png` — Connect detail page with no transfer options

## Issues

### Non-Google provider account selection pages display Google Analytics-specific help text

#### Severity

MEDIUM

#### Scope

app

#### Description

The account selection page at `/integrations/connect/facebook_ads/accounts` renders incorrect help text for the manual property ID field. The field label is correctly "Ad Account ID" but the supplemental help text reads "Find this in Google Analytics under Admin → Property Settings" — this is Google Analytics-specific guidance that is meaningless and misleading for a Facebook Ads user.

The same issue would affect `/integrations/connect/quickbooks/accounts` when a QuickBooks integration exists.

**Steps to reproduce:**
1. Log in with an account that has a facebook_ads integration
2. Navigate to `/integrations/connect/facebook_ads/accounts`
3. Observe the help text below the manual entry field reads "Find this in Google Analytics under Admin → Property Settings"

**Expected:** Help text should say something like "Find this in your Facebook Business Manager" or similar Facebook-specific guidance.

**Actual:** "Find this in Google Analytics under Admin → Property Settings"

**Location:** `lib/metric_flow_web/live/integration_live/connect.ex`, `render_account_selection/1` — the `label-text-alt` span is hardcoded to Google Analytics instructions and is not provider-aware.

Screenshot: `.code_my_spec/qa/434/screenshots/05b-facebook-accounts.png`
