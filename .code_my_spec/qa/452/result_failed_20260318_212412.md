# QA Result

## Status

partial

## Scenarios

### Scenario 1: Unauthenticated access redirects to login

**Result: pass**

Launched a fresh browser session (no session cookies) and navigated to `http://localhost:4070/reports/generate`. The browser was redirected to `http://localhost:4070/users/log-in` as expected. The `/reports/generate` route is correctly guarded by `:require_authenticated_user`.

Evidence: `s0_unauthenticated_redirect.png`

### Scenario 2: Page renders with correct initial state

**Result: pass**

After authenticating as `qa@example.com`, navigated to `http://localhost:4070/reports/generate`. Verified:

- H1 "Generate Report" is present
- Subtitle "Describe the chart or report you want and AI will build it for you." is visible
- `[data-role="prompt-form"]` card is present
- `[data-role="prompt-input"]` textarea is visible and editable
- `[data-role="generate-btn"]` is present and disabled (blank prompt)
- `[data-role="empty-state"]` paragraph is visible
- `[data-role="chart-preview-section"]` is NOT present
- `[data-role="save-section"]` is NOT present

Evidence: `s1_initial_state.png`

### Scenario 3: Typing a prompt enables the Generate Chart button

**Result: pass**

Note: `browser_fill` failed on the textarea (Vibium limitation with textarea elements). Used `browser_type` after clicking to focus. Typed "Show me a bar chart of monthly revenue for the past 6 months" into `[data-role="prompt-input"]`.

After typing:
- `[data-role="generate-btn"]` became enabled (confirmed via `browser_is_enabled`)
- `[data-role="empty-state"]` remains visible (no chart generated yet)

Evidence: `s2_prompt_filled.png`

### Scenario 4: Submitting a prompt triggers LLM generation

**Result: partial — error path tested, success path not testable**

Clicked `[data-role="generate-btn"]` with prompt filled. The button submitted the form. A screenshot was captured showing the generating state (`s3a_generating_spinner.png`).

After 30 seconds, the LLM call timed out with the generic error path. Verified:
- `[data-role="generate-error"]` appeared with message "Something went wrong generating the chart. Please try again."
- `[data-role="chart-preview-section"]` is NOT present (correct)
- `[data-role="empty-state"]` is also NOT present when error is shown (correct per spec: empty state shown only when `vega_spec` is nil AND not generating AND no error)

The success path (chart preview, vega-lite spec render) could not be tested because the `ANTHROPIC_API_KEY` environment variable is not set in this environment. The LLM API call returns a generic error. The error UI behavior is correctly implemented.

Additional observation: Typing additional text into the prompt after an error correctly clears the `[data-role="generate-error"]` element and restores the `[data-role="empty-state"]`. This matches the `update_prompt` handler behavior.

Evidence: `s3a_generating_spinner.png`, `s3_generate_error.png`, `s3_after_timeout.png`

### Scenario 5: Save section — validation prevents save with blank name

**Result: not tested**

Could not be tested because the save section only appears after a successful LLM generation (`vega_spec` must be non-nil). No LLM generation succeeded due to missing API key.

### Scenario 6: Save section — saving with a valid name produces confirmation

**Result: not tested**

Same blocker as Scenario 5 — requires successful chart generation.

### Scenario 7: "Generate Another" resets state

**Result: not tested**

Same blocker — requires saved confirmation state, which requires prior successful generation.

### Scenario 8: "View in Visualizations" link works

**Result: fail — route does not exist**

Navigated directly to `http://localhost:4070/visualizations` to verify the destination of the "View in Visualizations" link. The route returned a `Phoenix.Router.NoRouteError`:

```
no route found for GET /visualizations (MetricFlowWeb.Router)
```

The router has `/visualizations/new` and `/visualizations/:id/edit` but no `/visualizations` index route. The "View in Visualizations" link in `[data-role="save-confirmation"]` hardcodes `navigate="/visualizations"` which leads to a 500 error page.

Evidence: `s7_visualizations_page.png` (404 error), `s8_visualizations_route_missing.png`

## Evidence

- `.code_my_spec/qa/452/screenshots/s0_unauthenticated_redirect.png` — unauthenticated redirect to `/users/log-in`
- `.code_my_spec/qa/452/screenshots/s1_initial_state.png` — initial page state (authenticated)
- `.code_my_spec/qa/452/screenshots/s2_prompt_filled.png` — prompt filled, generate button enabled
- `.code_my_spec/qa/452/screenshots/s3a_generating_spinner.png` — generating state after form submit
- `.code_my_spec/qa/452/screenshots/s3_generate_error.png` — error message shown after LLM failure
- `.code_my_spec/qa/452/screenshots/s3_after_timeout.png` — page state after 30s LLM timeout
- `.code_my_spec/qa/452/screenshots/s7_visualizations_page.png` — /visualizations 500 error page
- `.code_my_spec/qa/452/screenshots/s8_visualizations_route_missing.png` — Phoenix NoRouteError for /visualizations

## Issues

### "View in Visualizations" link points to non-existent route /visualizations

#### Severity
HIGH

#### Description
The save confirmation section (`[data-role="save-confirmation"]`) contains a "View in Visualizations" link that navigates to `/visualizations`. This route does not exist in the router. Navigating to it produces a `Phoenix.Router.NoRouteError` (500 error page in production).

The router defines `/visualizations/new` and `/visualizations/:id/edit` but no index route. The link in `report_generator.ex` line 135:

```elixir
<.link navigate="/visualizations" class="link block mb-3">
  View in Visualizations
</.link>
```

After a user saves a visualization, the confirmation prompt tells them to view it — but clicking the link crashes. This breaks the save flow's happy path and could mislead users into thinking their visualization was not saved.

The fix is either: (a) add a `/visualizations` index LiveView route, or (b) update the link to point to a valid existing route such as `/dashboards`.

Reproduced by navigating to `http://localhost:4070/visualizations` while authenticated.

### LLM generation scenarios untestable without ANTHROPIC_API_KEY

#### Severity
INFO

#### Scope
QA

#### Description
Scenarios 4 (success path), 5, 6, 7, and 8 require a successful LLM chart generation. The `ANTHROPIC_API_KEY` environment variable is not set in the QA environment, so all calls to `Ai.generate_vega_spec/3` return the generic error: "Something went wrong generating the chart. Please try again."

To test the full flow, set `ANTHROPIC_API_KEY` in the environment before starting the Phoenix server. The error UI behavior (Scenario 4 error branch) was fully verified.

### browser_fill fails on textarea elements

#### Severity
LOW

#### Scope
QA

#### Description
`mcp__vibium__browser_fill(selector: "[data-role='prompt-input']", ...)` returned `"failed to fill: fill:"` when targeting the `<textarea>` element. The workaround is to use `browser_click` to focus, then `browser_type` to type. The QA plan's `fill` examples use `<input>` elements; the cheat sheet should note that `browser_fill` may not work for `<textarea>` and to use `browser_click` + `browser_type` instead.
