# QA Result

## Status

fail

## Scenarios

### Scenario 1: Unauthenticated access redirects to login

pass

Navigated to `http://localhost:4070/reports/generate` without authentication. The router's `:require_authenticated_user` pipeline redirected to `http://localhost:4070/users/log-in`. After successful login as `qa@example.com`, the browser was redirected back to `http://localhost:4070/reports/generate` as expected.

Evidence: `.code_my_spec/qa/452/screenshots/s0_unauthenticated_redirect.png` (captured in prior run)

### Scenario 2: Page renders with correct initial state

pass

After authenticating and navigating to `/reports/generate`:

- H1 "Generate Report" is present
- Subtitle "Describe the chart or report you want and AI will build it for you." is visible
- `[data-role="prompt-form"]` card is present and visible
- `[data-role="prompt-input"]` textarea is visible and editable
- `[data-role="generate-btn"]` is present and disabled (blank prompt, button greyed out)
- `[data-role="empty-state"]` shows "Enter a prompt above and click Generate Chart to get started."
- `[data-role="chart-preview-section"]` is NOT present
- `[data-role="save-section"]` is NOT present

Evidence: `.code_my_spec/qa/452/screenshots/s1_initial_state.png`

### Scenario 3: Typing a prompt enables the Generate Chart button

pass

Typed "Show me a bar chart of monthly revenue for the past 6 months" into `[data-role="prompt-input"]`. The `phx-change="update_prompt"` event fired (textarea inside `<form phx-change="update_prompt">`). The Generate Chart button (`[data-role="generate-btn"]`) became enabled (turned purple/active). The `[data-role="empty-state"]` remained visible.

Evidence: `.code_my_spec/qa/452/screenshots/s2_prompt_filled.png`

### Scenario 4: Submitting a prompt triggers LLM generation and shows chart preview

pass (success path)

Clicked `[data-role="generate-btn"]`. The LLM call completed and `[data-role="chart-preview-section"]` appeared. Verified:
- "Chart Preview" label with "AI-generated — review before saving" note
- `[data-role="vega-lite-chart"]` rendered a bar chart titled "Monthly Revenue - Past 6 Months" with Month on X axis, Revenue ($) on Y axis
- `[data-role="save-section"]` became visible
- `[data-role="empty-state"]` is no longer visible

The "Generating..." spinner state could not be captured — the LLM responded faster than the screenshot could be taken.

Evidence: `.code_my_spec/qa/452/screenshots/s3_chart_generated.png`

Note: The generate error path (`[data-role="generate-error"]`) was observed on a prior LLM call attempt when the ANTHROPIC_API_KEY was not set in the environment. The error message shown was "Something went wrong generating the chart. Please try again." — which is a clear user-facing message. See `.code_my_spec/qa/452/screenshots/s3_generate_error.png`.

### Scenario 5: Save section — validation prevents save with blank name

pass (disabled-state verification)

After chart generation, `[data-role="save-visualization-btn"]` was confirmed disabled with blank save name input (button greyed out, not clickable). This matches the spec.

The brief also requests triggering `[data-role="save-error"]` with "Name is required." by clicking the disabled button. Since the button has `disabled=""`, the browser does not fire click events on it, and pressing Enter in the empty name field did not submit the form. The "Name is required." error path (`[data-role="save-error"]`) cannot be triggered via normal browser interaction when the name is blank — the disabled button effectively prevents access to this code path. This may be intentional UX but is worth noting.

Evidence: `.code_my_spec/qa/452/screenshots/s4_save_validation_blank_name.png`

### Scenario 6: Save section — saving with a valid name produces confirmation

pass

Filled `[data-role="save-name-input"]` with "QA Test Report" using `browser_fill`. The `phx-change="update_save_name"` event fired correctly (input is inside `<form phx-change="update_save_name">`), the server-side `save_name` assign was updated, and `[data-role="save-visualization-btn"]` became enabled.

Clicked `[data-role="save-visualization-btn"]`. The `[data-role="save-confirmation"]` section appeared with:
- "Saved" badge (green, `badge-success`) and "Visualization saved!" text
- "View in Visualizations" link (see issue below — link points to `/dashboards`)
- "Generate Another" button
- `[data-role="save-section"]` was no longer visible

Note: The "View in Visualizations" link href is `/dashboards`, not `/visualizations` as specified in the brief. See Issues section.

Note: The previous QA run (result_failed_20260318_215819.md) reported the save-name input did not update server-side state. This was a misdiagnosis — the input was already inside a form with `phx-change`. The bug is not reproducible now. The save flow works correctly.

Evidence: `.code_my_spec/qa/452/screenshots/s5_save_name_filled.png`, `.code_my_spec/qa/452/screenshots/s5_save_confirmation.png`

### Scenario 7: "Generate Another" resets state

pass

From the save confirmation state, clicked "Generate Another" button (`phx-click="generate_another"`). Verified:
- URL remains `http://localhost:4070/reports/generate` (push_patch, no full reload)
- Page returned to initial state: prompt textarea is empty, empty state visible, no chart preview, no save section
- Generate Chart button is disabled (blank prompt)

Evidence: `.code_my_spec/qa/452/screenshots/s6_generate_another_reset.png`

### Scenario 8: "View in Visualizations" link works

fail

The "View in Visualizations" link in the save confirmation section navigates to `/dashboards` (not `/visualizations` as the brief specifies). The `/dashboards` page loads without error (HTTP 200, proper Phoenix LiveView render).

However, the `/visualizations` route does not exist in the router — `GET http://localhost:4070/visualizations` returns HTTP 404. Additionally, the saved visualization "QA Test Report" does not appear in the "My Dashboards" section on the `/dashboards` page — visualizations are a separate entity from dashboards and have no index listing page in the UI.

Evidence: `.code_my_spec/qa/452/screenshots/s7_visualizations_page.png`, `.code_my_spec/qa/452/screenshots/s_visualizations_route_missing.png`

## Evidence

- `.code_my_spec/qa/452/screenshots/s0_unauthenticated_redirect.png` — unauthenticated access redirected to login page
- `.code_my_spec/qa/452/screenshots/s1_initial_state.png` — initial page state: empty prompt, disabled button, empty-state visible, no chart/save sections
- `.code_my_spec/qa/452/screenshots/s2_prompt_filled.png` — prompt filled, Generate Chart button enabled
- `.code_my_spec/qa/452/screenshots/s3_chart_generated.png` — chart preview section visible after LLM generation, save section visible
- `.code_my_spec/qa/452/screenshots/s3_generate_error.png` — LLM error state when ANTHROPIC_API_KEY is not configured
- `.code_my_spec/qa/452/screenshots/s4_save_validation_blank_name.png` — save section with blank input, Save Visualization button disabled
- `.code_my_spec/qa/452/screenshots/s5_save_name_filled.png` — "QA Test Report" filled in save name input, Save Visualization button enabled
- `.code_my_spec/qa/452/screenshots/s5_save_confirmation.png` — save confirmation state: Saved badge, "Visualization saved!", "View in Visualizations" link, "Generate Another" button
- `.code_my_spec/qa/452/screenshots/s6_generate_another_reset.png` — page reset to initial state after clicking "Generate Another"
- `.code_my_spec/qa/452/screenshots/s7_visualizations_page.png` — `/dashboards` page loads but saved visualization is not listed there
- `.code_my_spec/qa/452/screenshots/s_visualizations_route_missing.png` — confirmation that `/visualizations` route does not exist

## Issues

### "View in Visualizations" link navigates to /dashboards, not /visualizations

#### Severity
MEDIUM

#### Description
The save confirmation section's "View in Visualizations" link (source: `lib/metric_flow_web/live/ai_live/report_generator.ex`, line 136) navigates to `/dashboards`, not `/visualizations`. The spec and brief specify the link should point to `/visualizations`.

The `/visualizations` route does not exist in the router — `GET /visualizations` returns HTTP 404. The router only defines `/visualizations/new` and `/visualizations/:id/edit` (no index route). Users who save a visualization and click "View in Visualizations" land on the Dashboards page, where their saved visualization is not visible (visualizations are a separate entity from dashboards and have no index listing in the UI).

Reproduction steps:
1. Log in as `qa@example.com`
2. Navigate to `http://localhost:4070/reports/generate`
3. Type a prompt and click "Generate Chart"
4. After chart preview appears, fill the save name input with any name
5. Click "Save Visualization"
6. In the confirmation section, click "View in Visualizations"
7. Observe: navigates to `/dashboards`, not `/visualizations`
8. Observe: saved visualization is not visible anywhere on the Dashboards page

### Saved visualizations have no index page in the UI

#### Severity
HIGH

#### Description
After saving a visualization via the Report Generator, there is no UI page where users can browse, find, or manage their saved visualizations. The `/visualizations` route returns 404. The `/dashboards` page only lists Dashboard entities (not Visualization entities). The "My Dashboards" section shows "No dashboards yet" for the QA user even after saving a visualization.

This means visualizations created via the Report Generator are effectively write-only from a user perspective — they can be saved but not retrieved or managed through the UI. The `Dashboards.list_visualizations/1` context function exists but is not wired to any LiveView.

Affected routes: `/visualizations` (404), `/dashboards` (does not show visualizations).
Source: `lib/metric_flow_web/router.ex` — no `live "/visualizations"` index route defined.
