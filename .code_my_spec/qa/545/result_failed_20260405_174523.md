# QA Result: Agency Stripe Connect (Story 545)

## Status

fail

## Scenarios

### 1. Navigate to Stripe Connect page (criterion 4999)

**Status:** partial

The route `/agency/stripe-connect` exists and the LiveView renders. However, the page is a stub — it shows "Stripe Connect onboarding will be available here" instead of actual connect functionality.

### 2. Initiate Stripe Connect flow (criterion 5000)

**Status:** fail

No connect button exists on the page. The LiveView is a stub without event handlers for Stripe Connect onboarding.

### 3. Stripe account stored after completion (criterion 5001)

**Status:** fail

Cannot test — no onboarding flow is implemented.

### 4. Abandoned onboarding allows retry (criterion 5002)

**Status:** fail

Cannot test — no onboarding flow exists.

### 5. Connection status display (criterion 5003)

**Status:** fail

No status badges or connection status display exists on the stub page.

### 6. Disconnect Stripe account (criterion 5004)

**Status:** fail

No disconnect functionality exists.

### 7. Unconnected agency billing fallback (criterion 5005)

**Status:** partial

The `/subscriptions/checkout` page shows "MetricFlow Pro" plan with a Subscribe button, which is the platform default. However, there's no logic to detect agency affiliation and show different plans.

### 8. Stripe state stored on schema (criterion 5006)

**Status:** fail

The `billing_stripe_accounts` schema exists but the LiveView doesn't read or display it.

## Issues

### StripeConnect LiveView is a stub

#### Severity

HIGH

#### Scope

APP

#### Description

The `MetricFlowWeb.AgencyLive.StripeConnect` LiveView at `/agency/stripe-connect` is a placeholder. It needs full implementation: reading Stripe account status from `BillingRepository`, displaying connection status badges, a connect button that initiates Express onboarding via `StripeClient`, a disconnect button, and return URL handling after Stripe onboarding completes.
