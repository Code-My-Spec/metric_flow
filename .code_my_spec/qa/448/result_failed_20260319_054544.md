# QA Result

## Status

partial

## Scenarios

### Scenario 1 — Navigation link exists

pass

Navigated to `http://localhost:4070/dashboard` after login. Found an `<a href="/correlations">Correlations</a>` link in the main navigation. The link was visible and correctly pointed to `/correlations`.

Evidence: `.code_my_spec/qa/448/screenshots/scenario1_dashboard_nav.png`

### Scenario 2 — Correlations page is accessible via navigation

pass

Navigated directly to `http://localhost:4070/correlations`. The URL matched `/correlations`. The H1 heading "Correlations" and the subtitle "Which metrics drive your goal?" were both visible on the page.

Evidence: `.code_my_spec/qa/448/screenshots/scenario2_correlations_page.png`

### Scenario 3 — Unauthenticated redirect

pass

Logged out by clicking the "Log out" link. Then navigated directly to `http://localhost:4070/correlations`. The browser was redirected to `http://localhost:4070/users/log-in` (URL contains "log-in"), confirming access control is working correctly.

Evidence: `.code_my_spec/qa/448/screenshots/scenario3_unauthenticated_redirect.png`

### Scenario 4 — No-data empty state

partial

The QA test account (`qa@example.com` / "QA Test Account") already has correlation data seeded — 27 rows across Google Analytics, Google Ads, Facebook Ads, QuickBooks, and Derived providers. The `[data-role="no-data-state"]` element was not rendered because `@summary.no_data` is false. The no-data empty state path could not be tested in this environment without a fresh account with no correlation history.

The page loaded correctly and displayed the Raw mode table with correlation results, confirming that the data-present path works correctly.

Evidence: `.code_my_spec/qa/448/screenshots/scenario4_correlations_with_data.png`

### Scenario 5 — Mode toggle is present

pass

On the correlations page, `[data-role="mode-toggle"]` was present. `[data-role="mode-raw"]` had class `btn btn-primary btn-sm` (active/primary styling). `[data-role="mode-smart"]` had class `btn btn-ghost btn-sm` (inactive/ghost styling). The Raw button is correctly styled as the active default.

Evidence: `.code_my_spec/qa/448/screenshots/scenario5_mode_toggle.png`

### Scenario 6 — Switch to Smart mode

pass

Clicked `[data-role="mode-smart"]`. The `[data-role="smart-mode"]` element became visible. The `[data-role="enable-ai-suggestions"]` button ("Enable AI Suggestions") was present inside the smart mode panel. The `[data-role="raw-mode"]` section was no longer visible (hidden by LiveView's `:if` condition on `@mode == :raw and not @summary.no_data`).

Evidence: `.code_my_spec/qa/448/screenshots/scenario6_smart_mode.png`

### Scenario 7 — Switch back to Raw mode

pass

While in Smart mode, clicked `[data-role="mode-raw"]`. The `[data-role="smart-mode"]` element became hidden. The `[data-role="mode-raw"]` button returned to `btn btn-primary btn-sm` styling (active). The Raw mode data table was visible again.

Evidence: `.code_my_spec/qa/448/screenshots/scenario7_back_to_raw.png`

### Scenario 8 — Run Now button and insufficient data

partial

Clicked `[data-role="run-correlations"]`. Because the QA account has sufficient data (90 data points, 30+ days), the run returned `{:ok, _job}` — not `{:error, :insufficient_data}`. The correlation job started successfully: the job-running banner appeared ("Correlation analysis is running..."), the Run Now button was disabled, and the flash info message "Correlation analysis started. Results will appear once complete." was shown.

The `:insufficient_data` path (which would show the `[data-role="insufficient-data-warning"]` badge and error flash) could not be triggered because the QA account has sufficient integration data. The happy path (job started) was verified instead.

Evidence: `.code_my_spec/qa/448/screenshots/scenario8_run_now_started.png`

### Scenario 9 — Configure Goals link

pass

`[data-role="configure-goals"]` was present on the correlations page with `href="/correlations/goals"`. Clicking the link navigated to `http://localhost:4070/correlations/goals` successfully.

Evidence: `.code_my_spec/qa/448/screenshots/scenario9_configure_goals.png`

## Evidence

- `.code_my_spec/qa/448/screenshots/scenario1_dashboard_nav.png` — dashboard page showing Correlations link in navigation
- `.code_my_spec/qa/448/screenshots/scenario2_correlations_page.png` — correlations page with H1 and subtitle
- `.code_my_spec/qa/448/screenshots/scenario3_unauthenticated_redirect.png` — login page after unauthenticated redirect from /correlations
- `.code_my_spec/qa/448/screenshots/scenario4_correlations_with_data.png` — correlations page with data (no-data state absent)
- `.code_my_spec/qa/448/screenshots/scenario5_mode_toggle.png` — mode toggle with Raw as active (primary) and Smart as ghost
- `.code_my_spec/qa/448/screenshots/scenario6_smart_mode.png` — Smart mode panel with Enable AI Suggestions button
- `.code_my_spec/qa/448/screenshots/scenario7_back_to_raw.png` — back to Raw mode with table visible
- `.code_my_spec/qa/448/screenshots/scenario8_run_now_started.png` — job-running banner and flash after Run Now
- `.code_my_spec/qa/448/screenshots/scenario9_configure_goals.png` — /correlations/goals page after clicking Configure Goals
- `.code_my_spec/qa/448/screenshots/exploratory_duplicate_rows.png` — full-page screenshot showing duplicate Derived metric rows
- `.code_my_spec/qa/448/screenshots/exploratory_platform_filter.png` — Google Analytics filter active showing only 1 row
- `.code_my_spec/qa/448/screenshots/exploratory_ai_enabled.png` — Smart mode with AI Suggestions enabled badge and recommendations
- `.code_my_spec/qa/448/screenshots/exploratory_feedback_confirmed.png` — AI feedback confirmation after clicking Helpful

## Issues

### Duplicate Derived metric rows in correlation results table

#### Severity
MEDIUM

#### Scope
APP

#### Description
The Raw mode results table displays duplicate rows for several "Derived" provider metrics. The following metrics appear twice in the table with near-identical (but occasionally slightly different) coefficient values:

- `screenPageViews` (Derived): 0.99 and 0.99
- `newUsers` (Derived): 0.97 and 0.97
- `clicks` (Derived): 0.87 and 0.86
- `ctr` (Derived): 0.82 and 0.80
- `cost` (Derived): 0.78 and 0.77
- `impressions` (Derived): 0.78 and 0.77
- `conversions` (Derived): 0.74 and 0.73
- `average_cpc` (Derived): 0.67 and 0.65
- `bounceRate` (Derived): 0.52 and 0.50
- `averageSessionDuration` (Derived): 0.31 and 0.29

This indicates duplicate `CorrelationResult` records exist in the database for the same metric name under the "Derived" provider, likely from multiple correlation runs creating new records rather than upserting. The deduplication should happen either at the database level (unique index on account_id + metric_name + provider) or in `get_latest_correlation_summary/1` by filtering to the most recent run's results only.

Reproduced at: `http://localhost:4070/correlations` with `qa@example.com`.

### No-data empty state not testable with QA seed data

#### Severity
INFO

#### Scope
QA

#### Description
Scenarios 4 and 8 (no-data empty state and insufficient data flash/badge) require a fresh account with no correlation data. The QA seed account (`qa@example.com` / "QA Test Account") already has 90 data points and 27 correlation result rows, so `@summary.no_data` is always false and `run_correlations/2` never returns `{:error, :insufficient_data}`.

To test the no-data path, either:
1. Add a second QA user (e.g., `qa-nodata@example.com`) with no integration data and no correlation records, or
2. Add a seed script that creates a fresh account without correlation data for testing the empty state and insufficient data paths.
