# QA Result

Story 431: Agency Views and Manages Client Accounts

## Status

fail

## Scenarios

### Scenario 1: Agency sees list of client accounts (criterion 3991)

**Result: pass**

Logged in as `qa@example.com` and navigated to `http://localhost:4070/accounts`. All 5 accounts were visible: "Client Account Manager", "Client Read Only", "Client Beta", "Client Alpha", and "QA Test Account". No accounts belonging to other users appeared.

Note: The client accounts appear because the agency grant propagation logic adds qa@example.com as an AccountMember on each client account. This is the expected mechanism per the Agencies context implementation.

Evidence: `.code_my_spec/qa/431/screenshots/01-accounts-page-owner.png`

### Scenario 2: Each client listing shows access level and origination status (criterion 3992)

**Result: pass**

Each client account card correctly displays the access level and origination status badges:
- Client Alpha: "admin" role badge + "Admin" (badge-accent) + "Originator" (badge-info). PASS.
- Client Beta: "admin" role badge + "Admin" (badge-accent) + "Invited" (badge-info). PASS.
- Client Read Only: "read_only" role badge + "Read Only" (badge-accent) + "Invited" (badge-info). PASS.
- Client Account Manager: "account_manager" role badge + "Account Manager" (badge-accent) + "Invited" (badge-info). PASS.
- QA Test Account: only "owner" badge — no agency badges (own account). PASS.

Evidence: `.code_my_spec/qa/431/screenshots/01-accounts-page-owner.png`

### Scenario 3: Account switcher is present on each client card (criterion 3993)

**Result: partial pass**

`[data-role="switch-account"]` elements are present on all 5 account cards. Each card has `data-role="account-card"`, `data-account-id`, and `data-active` attributes. PASS.

Clicking "Client Beta" switch button triggered a `Switched to Client Beta` flash and the nav updated to show "Client Beta" on the accounts page. PASS.

However, the switch only updates the local LiveView state. Navigating away from `/accounts` (e.g., to `/accounts/settings` or `/integrations`) resets the active account back to the primary account ("QA Test Account"). The `switch_account` handler in the source contains `_ = scope` (a no-op) instead of calling `Accounts.select_active_account/2` as specified. The switch does not persist to the session.

Evidence: `.code_my_spec/qa/431/screenshots/01-accounts-page-owner.png`, `.code_my_spec/qa/431/screenshots/02-after-switch-client-beta.png`

### Scenario 4: Current client context shown in navigation (criterion 3994)

**Result: pass**

`[data-role="current-account-name"]` is present and visible on all three pages checked:
- `/accounts` — shows "QA Test Account". PASS.
- `/accounts/settings` — shows "QA Test Account". PASS.
- `/integrations` — shows "QA Test Account". PASS.

The element is rendered by the app layout (`lib/metric_flow_web/components/layouts.ex` line 101) conditionally when `@active_account_name` is non-nil.

Evidence: `.code_my_spec/qa/431/screenshots/03-current-account-name-nav.png`

### Scenario 5: Read-only access restrictions (criterion 3995)

**Result: pass**

Logged in as `qa-431-readonly@example.com` (read_only member of "Client Read Only"). Navigated to `/accounts/settings`.

- Active account shown: "Client Read Only". PASS.
- `input[readonly]` attribute IS present on account name input. PASS.
- `#account-settings-form` NOT present. PASS.
- `[data-role="delete-account"]` NOT present. PASS.
- `[data-role="transfer-ownership"]` NOT present. PASS.

Evidence: `.code_my_spec/qa/431/screenshots/04-settings-readonly-user.png`

### Scenario 6: Account manager access restrictions (criterion 3996)

**Result: pass**

Logged in as `qa-431-acctmgr@example.com` (account_manager member of "Client Account Manager").

Integrations page (`/integrations`):
- Accessible with "Integrations" heading. PASS.

Settings page (`/accounts/settings`):
- `input[readonly]` IS present. PASS.
- `#account-settings-form` NOT present. PASS.
- `[data-role="delete-account"]` NOT present. PASS.
- `[data-role="transfer-ownership"]` NOT present. PASS.

Evidence: `.code_my_spec/qa/431/screenshots/06-integrations-acctmgr.png`, `.code_my_spec/qa/431/screenshots/07-settings-acctmgr.png`

### Scenario 7: Admin access capabilities (criterion 3997)

**Result: pass**

Logged in as `qa-431-admin@example.com` (admin member of "Client Alpha").

Settings page (`/accounts/settings`):
- `#account-settings-form` IS present. PASS.
- "Save Changes" button is visible. PASS.
- `[data-role="delete-account"]` NOT present. PASS.
- `[data-role="transfer-ownership"]` NOT present. PASS.

Integrations page (`/integrations`): Accessible with "Integrations" heading. PASS.

Members page (`/accounts/members`): Full member list visible with 5 members including Change/Remove actions. PASS.

Evidence: `.code_my_spec/qa/431/screenshots/09-settings-admin.png`, `.code_my_spec/qa/431/screenshots/10-members-admin.png`

### Scenario 8: Agency cannot see other users without admin access (criterion 3998)

**Result: pass**

- **Read-only user** (`qa-431-readonly@example.com`) navigated to `/accounts/members`: Only shows "Members / Client Read Only" heading — no member list. `[data-role="members-list"]` NOT present. `[data-role="member-row"]` NOT present. PASS.
- **Account manager user** (`qa-431-acctmgr@example.com`) navigated to `/accounts/members`: Only heading visible — no member list. `[data-role="members-list"]` NOT present. PASS.
- **Admin user** (`qa-431-admin@example.com`) navigated to `/accounts/members`: `[data-role="members-list"]` IS present. `[data-role="member-row"]` IS present with 5 member rows. PASS.

Evidence: `.code_my_spec/qa/431/screenshots/05-members-readonly-user.png`, `.code_my_spec/qa/431/screenshots/08-members-acctmgr.png`, `.code_my_spec/qa/431/screenshots/10-members-admin.png`

### Scenario 9: Originator badge (criterion 3999)

**Result: pass**

On the `/accounts` page as `qa@example.com`:
- "Client Alpha" card shows "Originator" badge (badge-info). PASS.
- "Client Beta" card shows "Invited" badge (no "Originator"). PASS.

The HTML confirms: Client Alpha has `origination_status: originator` correctly mapped to "Originator" label; Client Beta has `origination_status: invited` mapped to "Invited".

Evidence: `.code_my_spec/qa/431/screenshots/11-originator-vs-invited-badges.png`

## Evidence

- `.code_my_spec/qa/431/screenshots/01-accounts-page-owner.png` — Accounts page as qa@example.com: all 5 accounts with access level and origination badges
- `.code_my_spec/qa/431/screenshots/02-after-switch-client-beta.png` — After clicking Client Beta switch: flash "Switched to Client Beta", nav updated to Client Beta
- `.code_my_spec/qa/431/screenshots/03-current-account-name-nav.png` — Integrations page showing current-account-name element in nav
- `.code_my_spec/qa/431/screenshots/04-settings-readonly-user.png` — Settings page as read-only user: readonly inputs, no edit form, no delete/transfer sections
- `.code_my_spec/qa/431/screenshots/05-members-readonly-user.png` — Members page as read-only user: no member list shown
- `.code_my_spec/qa/431/screenshots/06-integrations-acctmgr.png` — Integrations page as account manager: accessible
- `.code_my_spec/qa/431/screenshots/07-settings-acctmgr.png` — Settings page as account manager: read-only inputs, no edit form
- `.code_my_spec/qa/431/screenshots/08-members-acctmgr.png` — Members page as account manager: no member list shown
- `.code_my_spec/qa/431/screenshots/09-settings-admin.png` — Settings page as admin: editable form with Save Changes, no delete/transfer
- `.code_my_spec/qa/431/screenshots/10-members-admin.png` — Members page as admin: full member list with Change/Remove controls
- `.code_my_spec/qa/431/screenshots/11-originator-vs-invited-badges.png` — Accounts page showing Originator vs Invited badges on Client Alpha and Client Beta

## Issues

### Account switch does not persist across page navigations

#### Severity
HIGH

#### Description
The `switch_account` event handler in `AccountLive.Index` updates the local LiveView assigns (`active_account_id`, `active_account_name`) and shows a "Switched to {account name}" flash, but does not persist the selection to the user's session. The source code at `lib/metric_flow_web/live/account_live/index.ex` line 174 contains `_ = scope` (a no-op) where it should call `Accounts.select_active_account/2`.

As a result, switching to "Client Beta" on the `/accounts` page shows "Client Beta" in the nav on that page. But navigating to `/accounts/settings` or `/integrations` resets the displayed active account back to the primary account ("QA Test Account") because the `ActiveAccountHook` re-derives the active account from `Accounts.list_accounts/1` on each LiveView mount.

The spec at `.code_my_spec/spec/metric_flow_web/account_live/index.spec.md` states that `phx-click="switch_account"` "Calls `Accounts.select_active_account/2` with the given account ID."

Reproduction: Log in as `qa@example.com`, navigate to `/accounts`, click "Client Beta" switch button (observe "Switched to Client Beta" flash), then navigate to `/integrations` — nav shows "QA Test Account" not "Client Beta".

### Seed script cannot be run when Phoenix server is already running

#### Severity
LOW

#### Scope
QA

#### Description
`priv/repo/qa_seeds_story_431.exs` cannot be executed with `mix run` when the Phoenix server is running, because `mix run` tries to start the full application (including the Cloudflare tunnel GenServer) which conflicts with the running instance.

The story seeds were not present in the database at the start of this QA run (only `agency_client_access_grants` records were missing; the client accounts themselves existed from a prior run). The grants and account memberships were inserted directly via `psql` to unblock testing.

The QA plan notes this limitation and suggests verifying seed data exists rather than re-running seeds. However, when grants are absent (as was the case here), there is no documented workaround. The plan should document the psql fallback or provide a way to run seeds against the running app (e.g., via IEx remote console).
