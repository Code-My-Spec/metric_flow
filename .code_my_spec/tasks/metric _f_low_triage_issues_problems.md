1 untriaged issue(s) remain at medium+ severity:

- `google_ads integration seed cannot be run while server is active`

Use `accept-issue <id>` or `dismiss-issue <id> <reason>` for each remaining issue.