Test requirements not met:
- Test file implements all spec test assertions: Test alignment issues — Missing test assertion for normalize_user/1: spec requires test for 'returns error missing_provider_user_id when sub is nil' (normalized: 'returns error missing provider user id when sub is nil'). Existing tests normalized: ["returns ok map with normalized user data for valid quickbooks user data", "extracts provider user id from the sub field", "accepts a string sub value asis", "converts an integer sub value to string", "extracts email from the email field", "extracts name from the name field", "falls back to givenname when name is absent", "sets username to the email value", "sets avatar url to nil", "extracts realm id from the realmid field", "handles missing optional fields gracefully returning nil for each", "handles minimal user data containing only the sub field", "falls back to quickbooks user when sub is nil", "falls back to quickbooks user when sub field is absent", "falls back to quickbooks user for an empty map", "falls back to quickbooks user when sub is a nonstring noninteger type", "returns error invalid user data when input is not a map", "normalized map uses only atom keys"]; Missing test assertion for normalize_user/1: spec requires test for 'returns error missing_provider_user_id when sub field is absent' (normalized: 'returns error missing provider user id when sub field is absent'). Existing tests normalized: ["returns ok map with normalized user data for valid quickbooks user data", "extracts provider user id from the sub field", "accepts a string sub value asis", "converts an integer sub value to string", "extracts email from the email field", "extracts name from the name field", "falls back to givenname when name is absent", "sets username to the email value", "sets avatar url to nil", "extracts realm id from the realmid field", "handles missing optional fields gracefully returning nil for each", "handles minimal user data containing only the sub field", "falls back to quickbooks user when sub is nil", "falls back to quickbooks user when sub field is absent", "falls back to quickbooks user for an empty map", "falls back to quickbooks user when sub is a nonstring noninteger type", "returns error invalid user data when input is not a map", "normalized map uses only atom keys"]; Missing test assertion for normalize_user/1: spec requires test for 'returns error missing_provider_user_id for an empty map' (normalized: 'returns error missing provider user id for an empty map'). Existing tests normalized: ["returns ok map with normalized user data for valid quickbooks user data", "extracts provider user id from the sub field", "accepts a string sub value asis", "converts an integer sub value to string", "extracts email from the email field", "extracts name from the name field", "falls back to givenname when name is absent", "sets username to the email value", "sets avatar url to nil", "extracts realm id from the realmid field", "handles missing optional fields gracefully returning nil for each", "handles minimal user data containing only the sub field", "falls back to quickbooks user when sub is nil", "falls back to quickbooks user when sub field is absent", "falls back to quickbooks user for an empty map", "falls back to quickbooks user when sub is a nonstring noninteger type", "returns error invalid user data when input is not a map", "normalized map uses only atom keys"]; Missing test assertion for normalize_user/1: spec requires test for 'returns error invalid_provider_user_id when sub is a non-string non-integer type' (normalized: 'returns error invalid provider user id when sub is a nonstring noninteger type'). Existing tests normalized: ["returns ok map with normalized user data for valid quickbooks user data", "extracts provider user id from the sub field", "accepts a string sub value asis", "converts an integer sub value to string", "extracts email from the email field", "extracts name from the name field", "falls back to givenname when name is absent", "sets username to the email value", "sets avatar url to nil", "extracts realm id from the realmid field", "handles missing optional fields gracefully returning nil for each", "handles minimal user data containing only the sub field", "falls back to quickbooks user when sub is nil", "falls back to quickbooks user when sub field is absent", "falls back to quickbooks user for an empty map", "falls back to quickbooks user when sub is a nonstring noninteger type", "returns error invalid user data when input is not a map", "normalized map uses only atom keys"]; Missing test assertion for strategy/0: spec requires test for 'returns Assent.Strategy.OAuth2' (normalized: 'returns assentstrategyoauth2'). Existing tests normalized: ["returns the custom quickbooks oauth2 strategy"]; Extra test found for normalize_user/1: 'falls back to quickbooks_user when sub is nil' is not documented in spec (normalized: 'falls back to quickbooks user when sub is nil'). Spec assertions normalized: ["returns ok map with normalized user data for valid quickbooks user data", "extracts provider user id from the sub field", "accepts a string sub value asis", "converts an integer sub value to string", "extracts email from the email field", "extracts name from the name field", "falls back to givenname when name is absent", "sets username to the email value", "sets avatar url to nil", "extracts realm id from the realmid field", "handles missing optional fields gracefully returning nil for each", "handles minimal user data containing only the sub field", "returns error missing provider user id when sub is nil", "returns error missing provider user id when sub field is absent", "returns error missing provider user id for an empty map", "returns error invalid provider user id when sub is a nonstring noninteger type", "returns error invalid user data when input is not a map", "normalized map uses only atom keys"]; Extra test found for normalize_user/1: 'falls back to quickbooks_user when sub field is absent' is not documented in spec (normalized: 'falls back to quickbooks user when sub field is absent'). Spec assertions normalized: ["returns ok map with normalized user data for valid quickbooks user data", "extracts provider user id from the sub field", "accepts a string sub value asis", "converts an integer sub value to string", "extracts email from the email field", "extracts name from the name field", "falls back to givenname when name is absent", "sets username to the email value", "sets avatar url to nil", "extracts realm id from the realmid field", "handles missing optional fields gracefully returning nil for each", "handles minimal user data containing only the sub field", "returns error missing provider user id when sub is nil", "returns error missing provider user id when sub field is absent", "returns error missing provider user id for an empty map", "returns error invalid provider user id when sub is a nonstring noninteger type", "returns error invalid user data when input is not a map", "normalized map uses only atom keys"]; Extra test found for normalize_user/1: 'falls back to quickbooks_user for an empty map' is not documented in spec (normalized: 'falls back to quickbooks user for an empty map'). Spec assertions normalized: ["returns ok map with normalized user data for valid quickbooks user data", "extracts provider user id from the sub field", "accepts a string sub value asis", "converts an integer sub value to string", "extracts email from the email field", "extracts name from the name field", "falls back to givenname when name is absent", "sets username to the email value", "sets avatar url to nil", "extracts realm id from the realmid field", "handles missing optional fields gracefully returning nil for each", "handles minimal user data containing only the sub field", "returns error missing provider user id when sub is nil", "returns error missing provider user id when sub field is absent", "returns error missing provider user id for an empty map", "returns error invalid provider user id when sub is a nonstring noninteger type", "returns error invalid user data when input is not a map", "normalized map uses only atom keys"]; Extra test found for normalize_user/1: 'falls back to quickbooks_user when sub is a non-string non-integer type' is not documented in spec (normalized: 'falls back to quickbooks user when sub is a nonstring noninteger type'). Spec assertions normalized: ["returns ok map with normalized user data for valid quickbooks user data", "extracts provider user id from the sub field", "accepts a string sub value asis", "converts an integer sub value to string", "extracts email from the email field", "extracts name from the name field", "falls back to givenname when name is absent", "sets username to the email value", "sets avatar url to nil", "extracts realm id from the realmid field", "handles missing optional fields gracefully returning nil for each", "handles minimal user data containing only the sub field", "returns error missing provider user id when sub is nil", "returns error missing provider user id when sub field is absent", "returns error missing provider user id for an empty map", "returns error invalid provider user id when sub is a nonstring noninteger type", "returns error invalid user data when input is not a map", "normalized map uses only atom keys"]; Extra test found for strategy/0: 'returns the custom QuickBooks OAuth2 strategy' is not documented in spec (normalized: 'returns the custom quickbooks oauth2 strategy'). Spec assertions normalized: ["returns assentstrategyoauth2"]