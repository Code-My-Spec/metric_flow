Test requirements not met:
- Test file implements all spec test assertions: Test alignment issues — Missing test assertion for get_latest_correlation_summary/1: spec requires test for 'includes  last_calculated_at  matching the completed job's  completed_at  timestamp' (normalized: 'includes last calculated at matching the completed jobs completed at timestamp'). Existing tests normalized: ["returns summary with results sorted by absolute coefficient", "includes last calculated at timestamp from the job", "includes data window date range", "includes data points count", "returns empty results list with no data flag when no correlations exist", "includes goal metric information"]; Missing test assertion for get_latest_correlation_summary/1: spec requires test for 'includes  goal_metric_name  from the job record' (normalized: 'includes goal metric name from the job record'). Existing tests normalized: ["returns summary with results sorted by absolute coefficient", "includes last calculated at timestamp from the job", "includes data window date range", "includes data points count", "returns empty results list with no data flag when no correlations exist", "includes goal metric information"]; Missing test assertion for get_latest_correlation_summary/1: spec requires test for 'returns  no_data: true  with empty results when no completed job exists' (normalized: 'returns no data true with empty results when no completed job exists'). Existing tests normalized: ["returns summary with results sorted by absolute coefficient", "includes last calculated at timestamp from the job", "includes data window date range", "includes data points count", "returns empty results list with no data flag when no correlations exist", "includes goal metric information"]; Missing test assertion for get_latest_correlation_summary/1: spec requires test for 'returns  last_calculated_at: nil  when no completed job exists' (normalized: 'returns last calculated at nil when no completed job exists'). Existing tests normalized: ["returns summary with results sorted by absolute coefficient", "includes last calculated at timestamp from the job", "includes data window date range", "includes data points count", "returns empty results list with no data flag when no correlations exist", "includes goal metric information"]; Missing test assertion for get_latest_correlation_summary/1: spec requires test for 'does not return results belonging to another user's scope' (normalized: 'does not return results belonging to another users scope'). Existing tests normalized: ["returns summary with results sorted by absolute coefficient", "includes last calculated at timestamp from the job", "includes data window date range", "includes data points count", "returns empty results list with no data flag when no correlations exist", "includes goal metric information"]; Missing test assertion for run_correlations/2: spec requires test for 'returns  {:ok, %CorrelationJob{}}  when the user has sufficient data and no running job' (normalized: 'returns ok correlationjob when the user has sufficient data and no running job'). Existing tests normalized: ["returns ok tuple with correlation job when valid", "creates job with status pending", "oban job is enqueued with correct args", "returns error insufficient data when fewer than 30 days of metric data", "returns error already running when a job is already in progress", "stores goal metric name in the job record"]; Missing test assertion for run_correlations/2: spec requires test for 'created job has status  :pending' (normalized: 'created job has status pending'). Existing tests normalized: ["returns ok tuple with correlation job when valid", "creates job with status pending", "oban job is enqueued with correct args", "returns error insufficient data when fewer than 30 days of metric data", "returns error already running when a job is already in progress", "stores goal metric name in the job record"]; Missing test assertion for run_correlations/2: spec requires test for 'enqueues an Oban CorrelationWorker job with correct job_id and user_id args' (normalized: 'enqueues an oban correlationworker job with correct job id and user id args'). Existing tests normalized: ["returns ok tuple with correlation job when valid", "creates job with status pending", "oban job is enqueued with correct args", "returns error insufficient data when fewer than 30 days of metric data", "returns error already running when a job is already in progress", "stores goal metric name in the job record"]; Missing test assertion for run_correlations/2: spec requires test for 'returns  {:error, :insufficient_data}  when the user has fewer than two distinct metrics' (normalized: 'returns error insufficient data when the user has fewer than two distinct metrics'). Existing tests normalized: ["returns ok tuple with correlation job when valid", "creates job with status pending", "oban job is enqueued with correct args", "returns error insufficient data when fewer than 30 days of metric data", "returns error already running when a job is already in progress", "stores goal metric name in the job record"]; Missing test assertion for run_correlations/2: spec requires test for 'returns  {:error, :already_running}  when a job with status  :running  already exists for the user' (normalized: 'returns error already running when a job with status running already exists for the user'). Existing tests normalized: ["returns ok tuple with correlation job when valid", "creates job with status pending", "oban job is enqueued with correct args", "returns error insufficient data when fewer than 30 days of metric data", "returns error already running when a job is already in progress", "stores goal metric name in the job record"]; Missing test assertion for schedule_daily_correlations/0: spec requires test for 'schedules jobs for users with at least two distinct metrics and active integrations' (normalized: 'schedules jobs for users with at least two distinct metrics and active integrations'). Existing tests normalized: ["schedules jobs for users with sufficient data", "skips users with fewer than 30 days of metric data", "skips users who already have recent correlation results within 24 hours", "returns count of scheduled jobs"]; Missing test assertion for schedule_daily_correlations/0: spec requires test for 'skips users with fewer than two distinct metrics' (normalized: 'skips users with fewer than two distinct metrics'). Existing tests normalized: ["schedules jobs for users with sufficient data", "skips users with fewer than 30 days of metric data", "skips users who already have recent correlation results within 24 hours", "returns count of scheduled jobs"]; Missing test assertion for schedule_daily_correlations/0: spec requires test for 'skips users whose most recent completed job was completed within the last 24 hours' (normalized: 'skips users whose most recent completed job was completed within the last 24 hours'). Existing tests normalized: ["schedules jobs for users with sufficient data", "skips users with fewer than 30 days of metric data", "skips users who already have recent correlation results within 24 hours", "returns count of scheduled jobs"]; Missing test assertion for schedule_daily_correlations/0: spec requires test for 'enqueues a CorrelationWorker Oban job for each eligible user' (normalized: 'enqueues a correlationworker oban job for each eligible user'). Existing tests normalized: ["schedules jobs for users with sufficient data", "skips users with fewer than 30 days of metric data", "skips users who already have recent correlation results within 24 hours", "returns count of scheduled jobs"]; Missing test assertion for schedule_daily_correlations/0: spec requires test for 'returns  {:ok, 0}  when no users are eligible' (normalized: 'returns ok 0 when no users are eligible'). Existing tests normalized: ["schedules jobs for users with sufficient data", "skips users with fewer than 30 days of metric data", "skips users who already have recent correlation results within 24 hours", "returns count of scheduled jobs"]; Extra test found for get_latest_correlation_summary/1: 'includes last_calculated_at timestamp from the job' is not documented in spec (normalized: 'includes last calculated at timestamp from the job'). Spec assertions normalized: ["returns a map with results sorted by absolute coefficient descending strongest correlation first", "includes last calculated at matching the completed jobs completed at timestamp", "includes data window as a start date end date tuple from the job record", "includes data points count matching the jobs data points field", "includes goal metric name from the job record", "returns no data true with empty results when no completed job exists", "returns last calculated at nil when no completed job exists", "does not return results belonging to another users scope"]; Extra test found for get_latest_correlation_summary/1: 'returns empty results list with no_data flag when no correlations exist' is not documented in spec (normalized: 'returns empty results list with no data flag when no correlations exist'). Spec assertions normalized: ["returns a map with results sorted by absolute coefficient descending strongest correlation first", "includes last calculated at matching the completed jobs completed at timestamp", "includes data window as a start date end date tuple from the job record", "includes data points count matching the jobs data points field", "includes goal metric name from the job record", "returns no data true with empty results when no completed job exists", "returns last calculated at nil when no completed job exists", "does not return results belonging to another users scope"]; Extra test found for get_latest_correlation_summary/1: 'includes goal metric information' is not documented in spec (normalized: 'includes goal metric information'). Spec assertions normalized: ["returns a map with results sorted by absolute coefficient descending strongest correlation first", "includes last calculated at matching the completed jobs completed at timestamp", "includes data window as a start date end date tuple from the job record", "includes data points count matching the jobs data points field", "includes goal metric name from the job record", "returns no data true with empty results when no completed job exists", "returns last calculated at nil when no completed job exists", "does not return results belonging to another users scope"]; Extra test found for run_correlations/2: 'returns ok tuple with correlation job when valid' is not documented in spec (normalized: 'returns ok tuple with correlation job when valid'). Spec assertions normalized: ["returns ok correlationjob when the user has sufficient data and no running job", "created job has status pending", "stores goal metric name in the job record", "enqueues an oban correlationworker job with correct job id and user id args", "returns error insufficient data when the user has fewer than two distinct metrics", "returns error already running when a job with status running already exists for the user", "returns error already running when a job with status pending already exists for the user"]; Extra test found for run_correlations/2: 'Oban job is enqueued with correct args' is not documented in spec (normalized: 'oban job is enqueued with correct args'). Spec assertions normalized: ["returns ok correlationjob when the user has sufficient data and no running job", "created job has status pending", "stores goal metric name in the job record", "enqueues an oban correlationworker job with correct job id and user id args", "returns error insufficient data when the user has fewer than two distinct metrics", "returns error already running when a job with status running already exists for the user", "returns error already running when a job with status pending already exists for the user"]; Extra test found for run_correlations/2: 'returns error :insufficient_data when fewer than 30 days of metric data' is not documented in spec (normalized: 'returns error insufficient data when fewer than 30 days of metric data'). Spec assertions normalized: ["returns ok correlationjob when the user has sufficient data and no running job", "created job has status pending", "stores goal metric name in the job record", "enqueues an oban correlationworker job with correct job id and user id args", "returns error insufficient data when the user has fewer than two distinct metrics", "returns error already running when a job with status running already exists for the user", "returns error already running when a job with status pending already exists for the user"]; Extra test found for run_correlations/2: 'returns error :already_running when a job is already in progress' is not documented in spec (normalized: 'returns error already running when a job is already in progress'). Spec assertions normalized: ["returns ok correlationjob when the user has sufficient data and no running job", "created job has status pending", "stores goal metric name in the job record", "enqueues an oban correlationworker job with correct job id and user id args", "returns error insufficient data when the user has fewer than two distinct metrics", "returns error already running when a job with status running already exists for the user", "returns error already running when a job with status pending already exists for the user"]; Extra test found for schedule_daily_correlations/0: 'schedules jobs for users with sufficient data' is not documented in spec (normalized: 'schedules jobs for users with sufficient data'). Spec assertions normalized: ["schedules jobs for users with at least two distinct metrics and active integrations", "skips users with fewer than two distinct metrics", "skips users whose most recent completed job was completed within the last 24 hours", "enqueues a correlationworker oban job for each eligible user", "returns ok count with the correct count of scheduled jobs", "returns ok 0 when no users are eligible"]; Extra test found for schedule_daily_correlations/0: 'skips users with fewer than 30 days of metric data' is not documented in spec (normalized: 'skips users with fewer than 30 days of metric data'). Spec assertions normalized: ["schedules jobs for users with at least two distinct metrics and active integrations", "skips users with fewer than two distinct metrics", "skips users whose most recent completed job was completed within the last 24 hours", "enqueues a correlationworker oban job for each eligible user", "returns ok count with the correct count of scheduled jobs", "returns ok 0 when no users are eligible"]; Extra test found for schedule_daily_correlations/0: 'skips users who already have recent correlation results (within 24 hours)' is not documented in spec (normalized: 'skips users who already have recent correlation results within 24 hours'). Spec assertions normalized: ["schedules jobs for users with at least two distinct metrics and active integrations", "skips users with fewer than two distinct metrics", "skips users whose most recent completed job was completed within the last 24 hours", "enqueues a correlationworker oban job for each eligible user", "returns ok count with the correct count of scheduled jobs", "returns ok 0 when no users are eligible"]