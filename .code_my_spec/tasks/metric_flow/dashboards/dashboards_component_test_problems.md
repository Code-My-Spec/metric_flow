Test requirements not met:
- Test file implements all spec test assertions: Test alignment issues — Missing test assertion for build_chart_spec/2: spec requires test for 'returned map includes a "mark" key configured for a line chart with type "line"' (normalized: 'returned map includes a mark key configured for a line chart with type line'). Existing tests normalized: ["returns a map with schema key pointing to a vegalite schema url", "returned map includes mark set to line", "returned map includes encoding with x mapped to the date field and y mapped to the value field", "returned map title uses the metric name argument", "returns a valid vegalite spec for an empty data list", "data points are serialized with date as iso 8601 string"]; Missing test assertion for build_chart_spec/2: spec requires test for 'title in the returned spec matches the metric_name argument' (normalized: 'title in the returned spec matches the metric name argument'). Existing tests normalized: ["returns a map with schema key pointing to a vegalite schema url", "returned map includes mark set to line", "returned map includes encoding with x mapped to the date field and y mapped to the value field", "returned map title uses the metric name argument", "returns a valid vegalite spec for an empty data list", "data points are serialized with date as iso 8601 string"]; Missing test assertion for build_chart_spec/2: spec requires test for 'data points in the spec have dates serialized as ISO 8601 strings' (normalized: 'data points in the spec have dates serialized as iso 8601 strings'). Existing tests normalized: ["returns a map with schema key pointing to a vegalite schema url", "returned map includes mark set to line", "returned map includes encoding with x mapped to the date field and y mapped to the value field", "returned map title uses the metric name argument", "returns a valid vegalite spec for an empty data list", "data points are serialized with date as iso 8601 string"]; Missing test assertion for get_dashboard_data/2: spec requires test for 'returns an ok tuple with a map containing all five keys: time_series, summary_stats, available_filters, connected_platforms, applied_filters' (normalized: 'returns an ok tuple with a map containing all five keys time series summary stats available filters connected platforms applied filters'). Existing tests normalized: ["returns ok tuple with dashboard data map when user has integrations and metrics", "time series contains one entry per distinct metric name", "summary stats contains one entry per distinct metric name with sum avg min max count", "connected platforms reflects the users current integrations", "available filtersplatforms is derived from connected integrations", "available filtersmetric names contains all distinct metric names for the user", "applies platform filter to both time series and summary stats queries", "applies date range filter to both time series and summary stats queries", "applies metric type filter to both time series and summary stats queries", "uses default date range0 when no date range option provided", "returns empty time series and summary stats when user has no metrics", "applied filters in the result reflects the opts used for the query", "does not return data belonging to other users"]; Extra test found for build_chart_spec/2: 'returned map includes "mark" set to "line"' is not documented in spec (normalized: 'returned map includes mark set to line'). Spec assertions normalized: ["returns a map with a schema key pointing to a vegalite schema url", "returned map includes a mark key configured for a line chart with type line", "returned map includes an encoding key with x mapped to the date field and y mapped to the value field", "title in the returned spec matches the metric name argument", "returns a valid vegalite spec for an empty data list", "data points in the spec have dates serialized as iso 8601 strings"]; Extra test found for build_chart_spec/2: 'returned map title uses the metric_name argument' is not documented in spec (normalized: 'returned map title uses the metric name argument'). Spec assertions normalized: ["returns a map with a schema key pointing to a vegalite schema url", "returned map includes a mark key configured for a line chart with type line", "returned map includes an encoding key with x mapped to the date field and y mapped to the value field", "title in the returned spec matches the metric name argument", "returns a valid vegalite spec for an empty data list", "data points in the spec have dates serialized as iso 8601 strings"]; Extra test found for build_chart_spec/2: 'data points are serialized with date as ISO 8601 string' is not documented in spec (normalized: 'data points are serialized with date as iso 8601 string'). Spec assertions normalized: ["returns a map with a schema key pointing to a vegalite schema url", "returned map includes a mark key configured for a line chart with type line", "returned map includes an encoding key with x mapped to the date field and y mapped to the value field", "title in the returned spec matches the metric name argument", "returns a valid vegalite spec for an empty data list", "data points in the spec have dates serialized as iso 8601 strings"]; Extra test found for get_dashboard_data/2: 'returns ok tuple with dashboard data map when user has integrations and metrics' is not documented in spec (normalized: 'returns ok tuple with dashboard data map when user has integrations and metrics'). Spec assertions normalized: ["returns an ok tuple with a map containing all five keys time series summary stats available filters connected platforms applied filters", "time series contains one entry per distinct metric name for the scoped user", "summary stats contains one entry per distinct metric name with sum avg min max and count keys", "connected platforms reflects the provider atoms of the users current integrations", "available filtersplatforms is derived from connected integrations", "available filtersmetric names contains all distinct metric names for the user", "applies platform filter to both time series and summary stats excluding metrics from other providers", "applies date range filter to both time series and summary stats excluding metrics outside the range", "applies metric type filter to time series and summary stats excluding metrics of other types", "uses default date range0 when no date range option is provided", "returns empty time series and empty summary stats when the user has no metrics", "applied filters in the result reflects all opts used including the resolved date range", "does not return data belonging to other users"]