Test requirements not met:
- Test file implements all spec test assertions: Test alignment issues — Missing test assertion for build_area_chart_spec/2: spec requires test for 'returns a map with a "$schema" key pointing to a Vega-Lite schema URL' (normalized: 'returns a map with a schema key pointing to a vegalite schema url'). Existing tests normalized: []; Missing test assertion for build_area_chart_spec/2: spec requires test for 'returned map includes a "mark" key configured for an area chart' (normalized: 'returned map includes a mark key configured for an area chart'). Existing tests normalized: []; Missing test assertion for build_area_chart_spec/2: spec requires test for 'returned map includes encoding with "x" temporal and "y" quantitative' (normalized: 'returned map includes encoding with x temporal and y quantitative'). Existing tests normalized: []; Missing test assertion for build_area_chart_spec/2: spec requires test for 'title matches the metric_name argument' (normalized: 'title matches the metric name argument'). Existing tests normalized: []; Missing test assertion for build_area_chart_spec/2: spec requires test for 'returns a valid spec for empty data' (normalized: 'returns a valid spec for empty data'). Existing tests normalized: []; Missing test assertion for build_multi_series_spec/2: spec requires test for 'renders one colored line per metric in the legend' (normalized: 'renders one colored line per metric in the legend'). Existing tests normalized: ["returns a map with a schema key pointing to a vegalite schema url", "returned map includes a mark key configured for a line chart", "encoding includes color mapped to the metric field with nominal type", "flattened data contains entries from all provided metrics with a metric field", "title in the returned spec matches the title argument", "returns a valid vegalite spec when given an empty metrics list", "returns a valid vegalite spec when given a single metric with data", "dates in data values are serialized as iso 8601 strings", "returns a valid spec with multiple metrics having overlapping date ranges"]