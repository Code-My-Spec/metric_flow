7 untriaged issue(s) remain at info+ severity:

- `Sync failure flash message exposes raw Elixir tuple for Google sync exceptions`
- `Google sync fails with database truncation error (string_data_right_truncation)`
- `Sync Now button never enters disabled/loading state in practice`
- `Facebook sync fails with "All data providers failed"`
- `Sync Now disabled state not observable in dev environment`
- `Syncing badge and loading spinner not observable in dev`
- `Sync failure flash message says "All data providers failed" — unclear to end users`

Use `.code_my_spec/tools/issues/accept-issue <id>` or `.code_my_spec/tools/issues/dismiss-issue <id> <reason>` for each remaining issue.