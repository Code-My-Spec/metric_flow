# Vibium MCP browser tools unavailable — browser-based QA scenarios could not execute

## Severity

high

## Scope

qa

## Disposition

Dismissed as duplicate. This issue was already triaged:

- **Accepted issue:** `qa-426-vibium_mcp_server_not_configured_browser_.md` — same root cause (vibium MCP tools unavailable to QA subagent), already has a resolution (added `mcpServers: vibium` to agent frontmatter).
- **Previous dismissal:** `qa-455-vibium_mcp_browser_tools_unavailable_brow.md` — this exact issue from Story 455 was already dismissed as a duplicate with additional notes about deferred tool loading via `ToolSearch`.

## Source

QA Story 455 — `.code_my_spec/qa/455/result.md`
